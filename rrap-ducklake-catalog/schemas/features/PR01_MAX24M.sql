CREATE TABLE
    IF NOT EXISTS FEATURES.PR01_MAX24M (
        OBSN_DT DATE NOT NULL,
        BASEL_CUST_ID BIGINT NOT NULL,
        PR01_MAX24M INT
    )
;

ALTER TABLE FEATURES.PR01_MAX24M
SET
    PARTITIONED BY (OBSN_DT)
;