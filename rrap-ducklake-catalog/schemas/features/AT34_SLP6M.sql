CREATE TABLE
    IF NOT EXISTS FEATURES.AT34_SLP6M (
        OBSN_DT DATE NOT NULL,
        BASEL_CUST_ID BIGINT NOT NULL,
        AT34_SLP6M DECIMAL(11, 4)
    )
;

ALTER TABLE FEATURES.AT34_SLP6M
SET
    PARTITIONED BY (OBSN_DT)
;

