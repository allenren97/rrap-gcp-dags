CREATE TABLE
    IF NOT EXISTS FEATURES.OS_BAL_AMT_IF (
        OBSN_DT DATE NOT NULL,
        BASEL_ACCT_ID BIGINT NOT NULL,
        OS_BAL_AMT_IF DECIMAL(17, 3),
        SRC_SYS_CD VARCHAR
    )
;

ALTER TABLE FEATURES.OS_BAL_AMT_IF
SET
    PARTITIONED BY (OBSN_DT, SRC_SYS_CD)
;