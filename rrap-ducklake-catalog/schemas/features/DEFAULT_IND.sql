CREATE TABLE
    IF NOT EXISTS FEATURES.DEFAULT_IND (
        OBSN_DT DATE NOT NULL,
        MORTGAGE_NO BIGINT NOT NULL,
        OBSVTN_MTH_TM_ID INTEGER NOT NULL,
        DEFAULT_IND INTEGER,
        SRC_SYS_CD VARCHAR
    )
;

ALTER TABLE FEATURES.DEFAULT_IND
SET
    PARTITIONED BY (OBSN_DT, SRC_SYS_CD)
;

-- Rekeyed account -> mortgage to match the SAS scan grain (by mortgage_no).
ALTER TABLE FEATURES.DEFAULT_IND DROP COLUMN IF EXISTS BASEL_ACCT_ID;
