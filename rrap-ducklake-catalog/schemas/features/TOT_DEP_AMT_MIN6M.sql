CREATE TABLE
    IF NOT EXISTS FEATURES.TOT_DEP_AMT_MIN6M (
        OBSN_DT DATE NOT NULL,
        BASEL_CUST_ID BIGINT NOT NULL,
        TOT_DEP_AMT_MIN6M DECIMAL(17,3)
    )
;

ALTER TABLE FEATURES.TOT_DEP_AMT_MIN6M
SET
    PARTITIONED BY (OBSN_DT)
;