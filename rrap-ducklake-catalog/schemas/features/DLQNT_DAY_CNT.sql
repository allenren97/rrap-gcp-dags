CREATE TABLE
    IF NOT EXISTS FEATURES.DLQNT_DAY_CNT (
        OBSN_DT DATE NOT NULL,
        BASEL_ACCT_ID BIGINT NOT NULL,
        DLQNT_DAY_CNT INTEGER,
        SRC_SYS_CD VARCHAR
    )
;

ALTER TABLE FEATURES.DLQNT_DAY_CNT
SET
    PARTITIONED BY (OBSN_DT)
;
