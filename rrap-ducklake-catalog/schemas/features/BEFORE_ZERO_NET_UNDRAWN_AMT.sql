CREATE TABLE
    IF NOT EXISTS FEATURES.BEFORE_ZERO_NET_UNDRAWN_AMT (
        OBSN_DT DATE NOT NULL,
        BASEL_ACCT_ID BIGINT NOT NULL,
        BEFORE_ZERO_NET_UNDRAWN_AMT DECIMAL(17,3)
    )
;

ALTER TABLE FEATURES.BEFORE_ZERO_NET_UNDRAWN_AMT
SET
    PARTITIONED BY (OBSN_DT)
;