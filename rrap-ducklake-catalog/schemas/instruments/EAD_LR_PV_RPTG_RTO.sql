CREATE TABLE
    IF NOT EXISTS instruments.EAD_LR_PV_RPTG_RTO (
        BASEL_ACCT_ID BIGINT NOT NULL,
        OBSN_DT DATE NOT NULL,
        EAD_LR_PV_RPTG_RTO DECIMAL(28,8),
        SRC_SYS_CD VARCHAR,
        STREAM VARCHAR
    )
;

ALTER TABLE instruments.EAD_LR_PV_RPTG_RTO
SET
    PARTITIONED BY (OBSN_DT, STREAM)
;