CREATE TABLE
    IF NOT EXISTS instruments.EAD_MODEL_NM (
        OBSN_DT DATE NOT NULL,
        BASEL_ACCT_ID BIGINT NOT NULL,
        EAD_MODEL_NM VARCHAR,
        EAD_MODEL_ID VARCHAR,
        STREAM VARCHAR
    )
;

ALTER TABLE instruments.EAD_MODEL_NM
SET
    PARTITIONED BY (OBSN_DT, STREAM)
;