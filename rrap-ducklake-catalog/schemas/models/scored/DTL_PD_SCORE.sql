CREATE TABLE
    IF NOT EXISTS MODELS.DTL_PD_SCORE (
        OBSN_DT DATE NOT NULL,
        BASEL_ACCT_ID BIGINT NOT NULL,
        VAR_NAME VARCHAR,
        VAR_SCORE INTEGER,
        VAR_INPUT VARCHAR,
        VAR_BIN INTEGER,
        STREAM VARCHAR,
        MODEL VARCHAR NOT NULL
    )
;

ALTER TABLE MODELS.DTL_PD_SCORE
SET
    PARTITIONED BY (OBSN_DT, MODEL)
; 