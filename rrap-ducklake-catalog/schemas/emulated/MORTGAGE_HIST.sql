-- Stacked BNS mortgage history (FRG_USER_DATA.MORTGAGE_HIST ingress).
-- One row per (MORTGAGE_NO, PROCESS_DATE).
CREATE TABLE IF NOT EXISTS emulated.MORTGAGE_HIST (
    MORTGAGE_NO BIGINT NOT NULL,
    COMM_TYPE VARCHAR,
    FLOAT_LKP_ID INTEGER,
    GENL_LKP_ID INTEGER,
    CURRENT_BAL DOUBLE,
    DELQ_DAYS INTEGER,
    DELQ_MONTHS INTEGER,
    FLOAT_IND VARCHAR,
    FORECLOSE_IND VARCHAR,
    INT_ACCR_AMT DOUBLE,
    LRA_STATUS VARCHAR,
    PAID_OFF_DATE TIMESTAMP,
    PAID_OFF_IND VARCHAR,
    PROCESS_DATE TIMESTAMP NOT NULL,
    TOTAL_SUSPENSE DOUBLE,
    UNPD_MTH_PAY_DATE TIMESTAMP,
    UNPD_WKL_PAY_DATE TIMESTAMP,
    MADE_DATE TIMESTAMP,
    TIME_KEY TIMESTAMP,
    INT_ADJ_DATE TIMESTAMP,
    YYMTH VARCHAR,
    INSURANCE VARCHAR,
    CLASS VARCHAR,
    EFF_TMSTMP TIMESTAMP,
    PRPTY_DESC_1 VARCHAR,
    PRPTY_DESC_2 VARCHAR,
    PRPTY_DESC_3 VARCHAR,
    LEND_VAL2 DOUBLE,
    TRNST VARCHAR,
    AUTH_AMT DOUBLE,
    MAT_DT TIMESTAMP,
    CAB VARCHAR,
    TOT_ADVNC_AMT DOUBLE,
    PRIM_CUST_ID BIGINT,
    CIF_KEY VARCHAR,
    STEP_F VARCHAR,
    INSUR_GRP VARCHAR,
    BULK_IND VARCHAR,
    AMORT INTEGER,
    PREPAY_YTD DOUBLE,
    PROVINCE VARCHAR
);

ALTER TABLE emulated.MORTGAGE_HIST
SET PARTITIONED BY (PROCESS_DATE);
