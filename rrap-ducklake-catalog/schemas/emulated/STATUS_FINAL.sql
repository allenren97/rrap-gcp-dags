CREATE TABLE IF NOT EXISTS emulated.STATUS_FINAL (
    OBSN_DT DATE NOT NULL,
    STREAM VARCHAR NOT NULL,
    MTH_TM_ID INTEGER NOT NULL,

    MORTGAGE_NO BIGINT NOT NULL,
    COMM_TYPE VARCHAR,
    FLOAT_LKP_ID INTEGER,
    GENL_LKP_ID INTEGER,
    CURRENT_BAL DOUBLE,
    DELQ_DAYS INTEGER,
    DELQ_MONTHS INTEGER,
    FLOAT_IND VARCHAR,
    FORECLOSE_IND VARCHAR,
    INT_ACCR_AMT DOUBLE,
    LRA_STATUS VARCHAR,
    PAID_OFF_DATE TIMESTAMP,
    PAID_OFF_IND VARCHAR,
    PROCESS_DATE TIMESTAMP NOT NULL,
    TOTAL_SUSPENSE DOUBLE,
    UNPD_MTH_PAY_DATE TIMESTAMP,
    UNPD_WKL_PAY_DATE TIMESTAMP,
    MADE_DATE TIMESTAMP,
    TIME_KEY TIMESTAMP,
    INT_ADJ_DATE TIMESTAMP,
    YYMTH VARCHAR,
    INSURANCE DOUBLE,
    CLASS VARCHAR,
    EFF_TMSTMP TIMESTAMP,
    PRPTY_DESC_1 VARCHAR,
    PRPTY_DESC_2 VARCHAR,
    PRPTY_DESC_3 VARCHAR,
    LEND_VAL2 DOUBLE,
    TRNST VARCHAR,
    AUTH_AMT DOUBLE,
    MAT_DT TIMESTAMP,
    CAB VARCHAR,
    TOT_ADVNC_AMT DOUBLE,
    PRIM_CUST_ID BIGINT,
    CIF_KEY VARCHAR,
    STEP_F VARCHAR,
    INSUR_GRP VARCHAR,
    BULK_IND VARCHAR,
    AMORT INTEGER,
    PREPAY_YTD DOUBLE,
    PROVINCE VARCHAR,

    DELQ_DAYS_2 INTEGER,
    DELQ_MONTHS_2 INTEGER,
    STATUS VARCHAR,
    MODEL_EXCL VARCHAR,

    INSRT_PROCESS_TMSTMP TIMESTAMP NOT NULL,
    UPDT_PROCESS_TMSTMP TIMESTAMP
);

ALTER TABLE emulated.STATUS_FINAL
SET PARTITIONED BY (OBSN_DT, STREAM);
