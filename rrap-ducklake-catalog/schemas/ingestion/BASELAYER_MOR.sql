CREATE TABLE IF NOT EXISTS ingestion.BASELAYER_MOR (
    MTH_END_DT DATE,
    MORT_NUM BIGINT,
    UNIQUEACCOUNTIDENTIFIER VARCHAR,
    NCR_EXPOSURE_SIZE VARCHAR,
    NCR_GEOGRAPHY VARCHAR,
    NCR_RATE VARCHAR,
    NCR_DELINQUENCY_BUCKET VARCHAR,
    ACCOUNTOPENDATE DATE,
    TRANSIT VARCHAR,
    ADVANCEDAMOUNT DECIMAL(18,2),
    AUTHORIZEDAMOUNT DECIMAL(18,2),
    GL_BALANCING_ADJUSTMENT DECIMAL(18,2),
    DAYSDELINQUENT INTEGER,
    BF_ZERO_NET_DRAWN_AMT DECIMAL(18,2),
    BF_ZERO_NET_UNDRAWN_AMT DECIMAL(18,2),
    ORIGINALPROPERTYVALUE DECIMAL(18,2),
    CCAR_EXPOSURE_CLASS VARCHAR,
    TREATMENT_FLAG VARCHAR,
    PRIMARY_CUSTOMER_CID VARCHAR,
    TNIF_REGION_CODE VARCHAR,
    DELINQ_STAGE VARCHAR,
    BASEL_CIF_KEY VARCHAR,
    CAB VARCHAR,
    LEGAL_ENTITY VARCHAR,
    MATURITY_DATE DATE,
    STEP_FLAG VARCHAR,
    RESIDUAL_MATURITY INTEGER,
    TRANSIT_EXCLUSION_FLAG VARCHAR,
    AF_ZERO_NET_DRAWN_AMT DECIMAL(18,2),
    AF_ZERO_NET_UNDRAWN_AMT DECIMAL(18,2)
);
ALTER TABLE ingestion.BASELAYER_MOR SET PARTITIONED BY (MTH_END_DT);