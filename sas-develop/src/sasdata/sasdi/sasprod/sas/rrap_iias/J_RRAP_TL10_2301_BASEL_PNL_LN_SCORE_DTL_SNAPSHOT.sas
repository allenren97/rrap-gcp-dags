/**************************************************************************** 
 * Job:             J_RRAP_TL10_2301_BASEL_PNL_LN_SCORE_  A57SWEI7.BH0009IL * 
 *                  DTL_SNAPSHOT                                            * 
 * Description:     LOADS BASEL_SPL_SCORE_DTL_SNAPSHOT                      * 
 *                                                                          * 
 * Metadata Server: 10.56.70.48                                             * 
 * Port:            8561                                                    * 
 * Location:        /Projects/RRAP/Jobs/02 - Term Loans/2.3                 * 
 *                                                                          * 
 * Server:          SASApp                                A57SWEI7.AT000002 * 
 *                                                                          * 
 * Source Tables:   TM_DIM - NZRRAP.TM_DIM                A57SWEI7.BE000412 * 
 *                  BASEL_MODEL_SCORECRD_DTL -            A57SWEI7.BE00043Y * 
 *                   NZRRAP.BASEL_MODEL_SCORECRD_DTL                        * 
 *                  BASEL_MODEL_SCORECRD_HDR -            A57SWEI7.BE00043Z * 
 *                   NZRRAP.BASEL_MODEL_SCORECRD_HDR                        * 
 *                  BASEL_MODEL_SCORECRD_DTL -            A57SWEI7.BE00043Y * 
 *                   NZRRAP.BASEL_MODEL_SCORECRD_DTL                        * 
 *                  BASEL_MODEL_SCORECRD_HDR -            A57SWEI7.BE00043Z * 
 *                   NZRRAP.BASEL_MODEL_SCORECRD_HDR                        * 
 * Target Tables:   BASEL_PNL_LN_SCORE_DTL_SNAPSHOT -     A57SWEI7.BE000444 * 
 *                  NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPS                     * 
 *                  HOT                                                     * 
 *                  BASEL_PNL_LN_SCORE_DTL_SNAPSHOT -     A57SWEI7.BE000444 * 
 *                  NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPS                     * 
 *                  HOT                                                     * 
 *                                                                          * 
 * Generated on:    Thursday, June 29, 2017 2:39:33 PM EDT                  * 
 * Generated by:    owprdsas                                                * 
 * Version:         SAS Data Integration Studio 4.8                         * 
 ****************************************************************************/ 

/* Generate the process id for job  */ 
%put Process ID: &SYSJOBID;

/* General macro variables  */ 
%let jobID = %quote(A57SWEI7.BH0009IL);
%let etls_jobName = %nrquote(J_RRAP_TL10_2301_BASEL_PNL_LN_SCORE_DTL_SNAPSHOT);
%let etls_userID = %nrquote(owprdsas);

/* Setup to capture return codes  */ 
%global job_rc trans_rc sqlrc syscc;
%let sysrc = 0;
%let job_rc = 0;
%let trans_rc = 0;
%let sqlrc = 0;
%let syscc = 0;
%global etls_stepStartTime; 
/* initialize syserr to 0 */
data _null_; run;

%macro rcSet(error); 
   %if (&error gt &trans_rc) %then 
      %let trans_rc = &error;
   %if (&error gt &job_rc) %then 
      %let job_rc = &error;
%mend rcSet; 

%macro rcSetDS(error); 
   if &error gt input(symget('trans_rc'),12.) then 
      call symput('trans_rc',trim(left(put(&error,12.))));
   if &error gt input(symget('job_rc'),12.) then 
      call symput('job_rc',trim(left(put(&error,12.))));
%mend rcSetDS; 

/* Create metadata macro variables */
%let IOMServer      = %nrquote(SASApp);
%let metaPort       = %nrquote(8561);
%let metaServer     = %nrquote(iw503);

/* Set metadata options */
options metaport       = &metaPort 
        metaserver     = "&metaServer"; 

/* Setup for capturing job status  */ 
%let etls_startTime = %sysfunc(datetime(),datetime.);
%let etls_recordsBefore = 0;
%let etls_recordsAfter = 0;
%let etls_lib = 0;
%let etls_table = 0;

%global etls_debug; 
%macro etls_setDebug; 
   %if %str(&etls_debug) ne 0 %then 
      OPTIONS MPRINT%str(;); 
%mend; 
%etls_setDebug; 

/*---- Start of Pre-Process Code  ----*/ 

%GLOBAL TIME_DIM_ID;
%GLOBAL MTH_TM_ID;
%GLOBAL MTH_END_DT;
%GLOBAL SESSIONTIME;
%GLOBAL DEL_MTH_TM_ID;
%LET SESSIONTIME=%SYSFUNC(DATETIME(),BEST18.);

/*---- End of Pre-Process Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 

/*==========================================================================* 
 * Step:            AM_rrap_spl_autoexec                  A57SWEI7.BK000YWE * 
 * Transform:       AM_rrap_spl_autoexec                                    * 
 * Description:     Autoexec file for termloan jobs. This points to auto    * 
 *                   call macros                                            * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YWE);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0; 
%let _OUTPUT_count = 0; 


%put WORK LOCATION: %sysfunc(getoption(work));
%rrap_spl_autoexec;


%rcSet(&syserr); 
%rcSet(&sysrc); 
%rcSet(&sqlrc); 

%rcSet(&syscc); 



/**  Step end AM_rrap_spl_autoexec **/

/*---- Start of User Written Code  ----*/ 

/*DEFINE AND CREATE TABLE STRUCTURE TO STORE RESULTING ACCOUNTS FOR EACH CRITERIA*/
/*DATA CRITERIAS_DTL_CC;*/
/*LENGTH BASEL_MODEL_SCORECRD_DTL_ID 8 MTH_TM_ID 8 BASEL_ACCT_ID 8 BASEL_MODEL_ID 8 BASEL_MODEL_SCORECRD_HDR_ID 8 BIN 8 PT_CNT 8 INSRT_PROCESS_TMSTMP 8;*/
/*FORMAT INSRT_PROCESS_TMSTMP DATETIME.;*/
/*RUN;*/

PROC SQL;
CREATE TABLE CRITERIAS_DTL_CC
(
BASEL_MODEL_SCORECRD_DTL_ID NUM,
MTH_TM_ID NUM,
BASEL_ACCT_ID NUM,
BASEL_MODEL_ID NUM,
BASEL_MODEL_SCORECRD_HDR_ID NUM,
BIN NUM,
PT_CNT NUM,
INSRT_PROCESS_TMSTMP NUM INFORMAT=DATETIME. FORMAT=DATETIME.
);
QUIT;
/*---- End of User Written Code  ----*/ 

/*==========================================================================* 
 * Step:            GET_TIME_KEYS                         A57SWEI7.BK000YWG * 
 * Transform:       User Written                                            * 
 * Description:                                                             * 
 *                                                                          * 
 * Source Table:    TM_DIM - NZRRAP.TM_DIM                A57SWEI7.BE000412 * 
 * Target Table:    User Written - work.TIME_KEY          A57SWEI7.BN000NHU * 
 *                                                                          * 
 * User Written:    SourceCode                                              * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YWG);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let etls_recnt = 0;
%macro etls_recordCheck; 
   %let etls_recCheckExist = %eval(%sysfunc(exist(NZRRAP.TM_DIM, DATA)) or 
         %sysfunc(exist(NZRRAP.TM_DIM, VIEW))); 
   
   %if (&etls_recCheckExist) %then
   %do;
      proc sql noprint;
         select count(*) into :etls_recnt from NZRRAP.TM_DIM;
      quit;
   %end;
%mend etls_recordCheck;
%etls_recordCheck;

%let SYSLAST = %nrquote(NZRRAP.TM_DIM); 

%let _INPUT_count = 1;
%let _INPUT = NZRRAP.TM_DIM;
%let _INPUT_connect =  DATABASE=XXX SERVER=cs2iwntzp01 AUTHDOMAIN="NZ_Auth" 
;
%let _INPUT_engine = NETEZZA;
%let _INPUT_memtype = DATA;
%let _INPUT_options = %nrquote();
%let _INPUT_alter = %nrquote();
%let _INPUT_path = %nrquote(/Shared Data/Sources/Netezza/Tables/NZRRAP/TM_DIM%(Table%));
%let _INPUT_type = 1;
%let _INPUT_label = %nrquote();

%let _INPUT1 = NZRRAP.TM_DIM;
%let _INPUT1_connect =  DATABASE=XXX SERVER=cs2iwntzp01 AUTHDOMAIN="NZ_Auth" 
;
%let _INPUT1_engine = NETEZZA;
%let _INPUT1_memtype = DATA;
%let _INPUT1_options = %nrquote();
%let _INPUT1_alter = %nrquote();
%let _INPUT1_path = %nrquote(/Shared Data/Sources/Netezza/Tables/NZRRAP/TM_DIM%(Table%));
%let _INPUT1_type = 1;
%let _INPUT1_label = %nrquote();

%let _OUTPUT_count = 1;
%let _OUTPUT = work.TIME_KEY;
%let _OUTPUT_connect = ;
%let _OUTPUT_engine = ;
%let _OUTPUT_memtype = DATA;
%let _OUTPUT_options = %nrquote();
%let _OUTPUT_alter = %nrquote();
%let _OUTPUT_path = %nrquote(/User Written_A57SWEI7.BN000NHU%(WorkTable%));
%let _OUTPUT_type = 1;
%let _OUTPUT_label = %nrquote();
/* List of target columns to keep  */ 
%let _OUTPUT_keep = TM_ID TM_LVL_END_DT;
%let _OUTPUT_col_count = 2;
%let _OUTPUT_col0_name = TM_ID;
%let _OUTPUT_col0_table = work.TIME_KEY;
%let _OUTPUT_col0_length = 8;
%let _OUTPUT_col0_type = ;
%let _OUTPUT_col0_format = 11.;
%let _OUTPUT_col0_informat = 11.;
%let _OUTPUT_col0_label = %nrquote(TM_ID);
%let _OUTPUT_col0_input0 = TM_ID;
%let _OUTPUT_col0_exp = ;
%let _OUTPUT_col0_input = TM_ID;
%let _OUTPUT_col0_input_count = 1;
%let _OUTPUT_col1_name = TM_LVL_END_DT;
%let _OUTPUT_col1_table = work.TIME_KEY;
%let _OUTPUT_col1_length = 8;
%let _OUTPUT_col1_type = ;
%let _OUTPUT_col1_format = DATE9.;
%let _OUTPUT_col1_informat = DATE9.;
%let _OUTPUT_col1_label = %nrquote(TM_LVL_END_DT);
%let _OUTPUT_col1_input0 = TM_LVL_END_DT;
%let _OUTPUT_col1_exp = ;
%let _OUTPUT_col1_input = TM_LVL_END_DT;
%let _OUTPUT_col1_input_count = 1;

%let _OUTPUT1 = work.TIME_KEY;
%let _OUTPUT1_connect = ;
%let _OUTPUT1_engine = ;
%let _OUTPUT1_memtype = DATA;
%let _OUTPUT1_options = %nrquote();
%let _OUTPUT1_alter = %nrquote();
%let _OUTPUT1_path = %nrquote(/User Written_A57SWEI7.BN000NHU%(WorkTable%));
%let _OUTPUT1_type = 1;
%let _OUTPUT1_label = %nrquote();
/* List of target columns to keep  */ 
%let _OUTPUT1_keep = TM_ID TM_LVL_END_DT;
%let _OUTPUT1_col_count = 2;
%let _OUTPUT1_col0_name = TM_ID;
%let _OUTPUT1_col0_table = work.TIME_KEY;
%let _OUTPUT1_col0_length = 8;
%let _OUTPUT1_col0_type = ;
%let _OUTPUT1_col0_format = 11.;
%let _OUTPUT1_col0_informat = 11.;
%let _OUTPUT1_col0_label = %nrquote(TM_ID);
%let _OUTPUT1_col0_input0 = TM_ID;
%let _OUTPUT1_col0_exp = ;
%let _OUTPUT1_col0_input = TM_ID;
%let _OUTPUT1_col0_input_count = 1;
%let _OUTPUT1_col1_name = TM_LVL_END_DT;
%let _OUTPUT1_col1_table = work.TIME_KEY;
%let _OUTPUT1_col1_length = 8;
%let _OUTPUT1_col1_type = ;
%let _OUTPUT1_col1_format = DATE9.;
%let _OUTPUT1_col1_informat = DATE9.;
%let _OUTPUT1_col1_label = %nrquote(TM_LVL_END_DT);
%let _OUTPUT1_col1_input0 = TM_LVL_END_DT;
%let _OUTPUT1_col1_exp = ;
%let _OUTPUT1_col1_input = TM_LVL_END_DT;
%let _OUTPUT1_col1_input_count = 1;

/*---- Start of User Written Code  ----*/ 

%get_model_period_dates(product=spl);
/*%let start_period_dt=31JUL2004;*/
/*%let start_period_dt=31OCT2014;*/
/*%let end_period_dt=31AUG2015;*/

%put Start and End Dates for SPL Models:;
%put start_period_dt: &start_period_dt ;
%put end_period_dt: &end_period_dt;

proc sql;
	create table time_key as
	select TM_ID, tm_lvl_end_dt from 
	nzrrap.TM_DIM
	where tm_lvl='Month' and tm_lvl_end_dt between "&start_period_dt"d and "&end_period_dt"d
/*	WHERE TM_LVL='Month' AND TM_ID>=14596 AND TM_ID<=16036*/
/*	WHERE TM_ID IN (15916,15956,15996)*/
/*	WHERE TM_ID=14596*/
	order by 1;
quit;


/*---- End of User Written Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 
%rcSet(&syscc); 



/**  Step end GET_TIME_KEYS **/

/*==========================================================================* 
 * Step:            Loop                                  A57SWEI7.BK000YWH * 
 * Transform:       Loop                                                    * 
 * Description:                                                             * 
 *                                                                          * 
 * Source Table:    User Written - work.TIME_KEY          A57SWEI7.BN000NHU * 
 * Target Table:    Loop - work.W3XPB3S0                  A57SWEI7.BN000NHV * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YWH);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let etls_recCheckExist = 0; 
%let etls_recnt = 0; 
%macro etls_recordCheck; 
   %let etls_recCheckExist = %eval(%sysfunc(exist(work.TIME_KEY, DATA)) or 
         %sysfunc(exist(work.TIME_KEY, VIEW))); 
   
   %if (&etls_recCheckExist) %then
   %do;
      %local etls_syntaxcheck; 
      %let etls_syntaxcheck = %sysfunc(getoption(syntaxcheck)); 
      /* Turn off syntaxcheck option to perform following steps  */ 
      options nosyntaxcheck;
      
      proc contents data = work.TIME_KEY out = work.etls_contents(keep = nobs) noprint; 
      run; 
      
      data _null_; 
         set work.etls_contents (obs = 1); 
         call symput("etls_recnt", left(put(nobs,32.))); 
      run;
      
      proc datasets lib = work nolist nowarn memtype = (data view);
         delete etls_contents;
      quit;
      
      /* Reset syntaxcheck option to previous setting  */ 
      options &etls_syntaxcheck; 
   %end;
%mend etls_recordCheck;
%etls_recordCheck;

%let SYSLAST = %nrquote(work.TIME_KEY); 

/* Capture summary statistics about this step for performance reporting  */ 
%put "DIS_SUMM";
%macro etls_getHandle(statusTable=, handleVariable=, row=); 
   %let etls_dsid = %sysfunc(open(&statusTable)); 
   %if (&etls_dsid = 0) %then 
      %put %sysfunc(sysmsg()); 
   %else 
   %do; 
      %let rc = %sysfunc(fetchobs(&etls_dsid, &row)); 
      %if (&rc ne 0) %then 
         %put %sysfunc(sysmsg()); 
      %else 
      %do; 
         %let etls_varnum = %sysfunc(varnum(&etls_dsid,&handleVariable)); 
         %if (&etls_varnum > 0) %then 
            %sysfunc(getvarc(&etls_dsid,&etls_varnum)); 
         %else 
            %put %sysfunc(sysmsg()); 
      %end; 
      %let rc = %sysfunc(close(&etls_dsid)); 
   %end; 
%mend etls_getHandle; 

%macro etls_freeHandle(statusTable=, statusVariable=, handleVariable=, 
   handleName=, statusSetting="Finished", 
   endTimeVariable=endTime, startTimeVariable=startTime, signoff=1, 
   returnCodeVariable=, returnCodeMacroVariable=, setMainJobRC=1, 
   statusUnknownReturnCode=., startTimeMacroVariable=, endTimeMacroVariable= ); 

   %if (&statusTable ne ) %then 
   %do; 
      %local etls_rcMacroVarExisted; 
      %let etls_rcMacroVarExisted = 0; 
      %if ("&returnCodeMacroVariable" ne "") %then 
      %do; 
         proc sql noprint; 
            select '1' into: etls_rcMacroVarExisted from dictionary.macros 
            where name=upcase("&returnCodeMacroVariable"); 
         quit; 

         %rcSet(&sqlrc); 
         %if (&etls_rcMacroVarExisted = 0) %then 
         %do; 
            %put WARNING%QUOTE(:) Return code from inner job not found.  Setting status to Unknown.;
            %let &returnCodeMacroVariable=&statusUnknownReturnCode; 
         %end; 
         %if (&setMainJobRC eq 1) %then 
            %rcSet(&&&returnCodeMacroVariable); 
      %end; 
      %else 
      %do; 
         %let returnCodeMacroVariable=etls_rcmacvar; 
         %let &returnCodeMacroVariable=&statusUnknownReturnCode; 
      %end; 

      %local etls_startTimeMacroVarExisted; 
      %let etls_startTimeMacroVarExisted = 0; 
      %if ("&startTimeMacroVariable" ne "") %then 
      %do; 
         proc sql noprint; 
            select '1' into: etls_startTimeMacroVarExisted from dictionary.macros 
            where name=upcase("&startTimeMacroVariable"); 
         quit; 

         %rcSet(&sqlrc); 
         %if (&etls_startTimeMacroVarExisted = 0) %then 
         %do; 
            %put WARNING%QUOTE(:) Start time from inner job not found.  No value will be set.;
            %let &startTimeMacroVariable=; 
         %end; 
      %end; 

      %local etls_endTimeMacroVarExisted; 
      %let etls_endTimeMacroVarExisted = 0; 
      %if ("&endTimeMacroVariable" ne "") %then 
      %do; 
         proc sql noprint; 
            select '1' into: etls_endTimeMacroVarExisted from dictionary.macros 
            where name=upcase("&endTimeMacroVariable"); 
         quit; 

         %rcSet(&sqlrc); 
         %if (&etls_endTimeMacroVarExisted = 0) %then 
         %do; 
            %put WARNING%QUOTE(:) End time from inner job not found.  Setting end time to current
      time.;
            %let &endTimeMacroVariable=%sysfunc(datetime()); 
         %end; 
      %end; 

      data &statusTable; 
         modify &statusTable(where=(&handleVariable = &handleName)); 
         %if ("&startTimeMacroVariable" ne "") %then 
         %do; 
            &startTimeVariable = input(symget("&startTimeMacroVariable"),32.);; 
         %end; 
         %if ("&endTimeVariable" ne "") %then 
         %do; 
            %if ("&endTimeMacroVariable" ne "") %then 
            %do; 
               &endTimeVariable = input(symget("&endTimeMacroVariable"),32.);; 
            %end; 
            %else 
               &endTimeVariable = datetime();; 
         %end; 
         %if ("&returnCodeVariable" ne "") %then 
            &returnCodeVariable = input(symget("&returnCodeMacroVariable"),32.);; 
         %if ("&statusVariable" ne "") %then 
         %do; 
            if (symget("etls_rcMacroVarExisted") eq "0") then 
               &statusVariable = "Unknown Status"; 
            else
               &statusVariable = &statusSetting;
         %end;
         call symput('handle',&handleVariable); 
         replace; 
         stop; 
      run; 

      %rcSet(&syserr); 

      %if (&signoff eq 1) %then 
         %etls_signoff(handleName=&handle); 
   %end; 
%mend etls_freeHandle; 

%macro etls_createHandle(statusTable=,
                         statusVariable=,
                         handleVariable=,
                         handlePrefix=rmt,
                         gridOptionSet=,
                         workloadMacroVariable=,
                         row=,
                         machineVariable=,
                         statusSetting="Running", 
                         startTimeVariable=startTime,
                         signon=1,
                         useGrid=1,
                         log=,
                         output=,
                         gridRC=,
                         cmacvar=etls_signonStatus,
                         additionalSignonOptions=,
                         signonRetries= ); 

   %local remoteSessionId; 
   %let remoteSessionId = &handlePrefix.&row; 
   %let &cmacvar = 1; 
   %local etls_machineId; 
   %if (&signon eq 1) %then 

      %etls_signon(handleName=&remoteSessionId,
                   useGrid=&useGrid,
                   machineIdMacroVariable=etls_machineId,
                   gridOptionSet=&etls_gridOptionSet,
                   workloadMacroVariable=&workloadMacroVariable,
                   log=&log,
                   output=&output,
                   cmacvar=&cmacvar,
                   gridRC=&gridRC,
                   additionalSignonOptions=&additionalSignonOptions,
					signonRetries=&signonRetries,
                   gridJobName=DIS_&etls_jobName._&row); 

   %else %let &cmacvar=0; 

   data &statusTable; 
      retain ptr &row; 
      modify &statusTable point = ptr; 
      &handleVariable = "&remoteSessionId"; 
      %if (&signon eq 1) %then 
         &machineVariable = "&etls_machineId";; 
      %if (&&&cmacvar ne 0) %then 
      %do; 
         &statusVariable = "Failed Signon"; 
      %end; 
      %else 
         &statusVariable = &statusSetting;; 
      %if (&startTimeVariable ne ) %then 
         &startTimeVariable = datetime();; 
      replace; 
      stop; 
   run; 

   %rcSet(&syserr); 
%mend etls_createHandle; 

%macro etls_getParameterNames(parameterTable=, parameterVariableMacro=, startingColumnNumber=1);
   %let &parameterVariableMacro = ;
   %let dsid = %sysfunc(open(&parameterTable));
   %if (&dsid gt 0) %then 
   %do; 
      %do i=&startingColumnNumber %to %sysfunc(attrn(&dsid,nvars)); 
         %let &parameterVariableMacro = &&&parameterVariableMacro %sysfunc(varname(&dsid,&i)); 
      %end; 
      %let dsid = %sysfunc(close(&dsid)); 
   %end; 
   %else 
      %put %sysfunc(sysmsg()); 
   %rcSet(&syserr); 
%mend etls_getParameterNames; 

%macro etls_getParameters(parameterTable=, row=, startingColumnNumber=1 , handleName=); 
   data _null_; 
      length vname $256 vtype $1 value $32767; 
      dsid = open("&parameterTable"); 
      if (dsid > 0) then 
      do; 
         do _i = 1 to &row; 
            fetchrc = fetch(dsid); 
         end; 
         do _i=&startingColumnNumber to attrn(dsid,'nvars'); 
            vname = varname(dsid,_i); 
            vtype = vartype(dsid,_i); 
            if (fetchrc = 0) then 
            do; 
               if (vtype = 'C') then 
               do; 
                  value = getvarc(dsid,_i); 
                  value = tranwrd(value,"%","%%"); 
                  value = tranwrd(value,"(","%("); 
                  value = tranwrd(value,")","%)"); 
                  value = tranwrd(value,'"','%"'); 
                  value = tranwrd(value,"'","%'"); 
               end; 
               else 
                  value = left(put(getvarn(dsid,_i),best32.)); 
            end; /* fetchrc = 0 */ 
            
            put ;
            if indexc(trimn(value),"+-*/<>=^~;, '()&%",'"')>0 then 
            do; 
               value='%nrstr('||trim(value)||')'; 
               put "ETLS_DIAG%QUOTE(:) Special characters encountered; References may require: %nrbquote(%)UNQUOTE(&" vname+(-1)').';
               put "NOTE: Special characters encountered; References may require: %nrbquote(%)UNQUOTE(&" vname+(-1)').';
            end; 
            %if %str(&handlename) ne %str() %then 
            %do; 
               value = '%syslput '||trim(vname)||'= '||trim(value)||" / remote = &handleName;"; 
            %end; 
            %else 
            %do; 
               value = '%let '||trim(vname)||'= '||trim(value)||';'; 
            %end; 
            
            put "NOTE: Setting macro variable " vname "with statement:" value ;
            call execute(value); 
         end; /* do i= */ 
         dsid = close(dsid); 
      end; /* dsid > 0 */ 
      else 
      do; 
         put "ERROR%QUOTE(:) Parameter table, &parameterTable., could not be"
              " opened.";
         abort; 
      end; 
      stop; 
   run; 
   
   %rcSet(&syserr); 
%mend etls_getParameters; 

%macro etls_loopW2G6RLP; 
   %local etls_filePrefix; 
   %let etls_filePrefix = ; 
   
   %macro etls_processToLoopW2G6RLZ(parameterTable=, row=, handleName=rmt);
      %local etls_parmvars; 
      %etls_getParameterNames(parameterTable=&parameterTable, 
         parameterVariableMacro=etls_parmvars, 
         startingColumnNumber=1); 
      %local &etls_parmvars; 
      %etls_getParameters(parameterTable=&parameterTable, row=&row, 
         startingColumnNumber=1); 
      %let etls_previousFilePrefix = &etls_filePrefix;
      %local etls_filePrefix; 
      %let etls_filePrefix = &etls_previousFilePrefix.&handleName; 
      %macro etls_jobW2G6RM9; 
      
         /*---- Start of User Written Code  ----*/ 
         
          %PUT DELETE TIME ID IS &DEL_MTH_TM_ID;
         %let MODULE=TL;
         %let BATCH=2.3;
         %etl_job_start(TARGET_TABLE=NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPSHOT);
         	
         	    proc sql;
               connect using NZRRAP as nzcon;
         
               execute 
               ( 
                  delete from &RRAP_DB.."BASEL_PNL_LN_SCORE_DTL_SNAPSHOT"
                  where
                     &RRAP_DB.."BASEL_PNL_LN_SCORE_DTL_SNAPSHOT"."MTH_TM_ID" = &DEL_MTH_TM_ID
               ) by nzcon; 
         
            quit;
         /*---- End of User Written Code  ----*/ 
         
         /*==========================================================================* 
          * Step:            Join                                  A57SWEI7.BK000YWJ * 
          * Transform:       Join                                                    * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Tables:   BASEL_MODEL_SCORECRD_DTL -            A57SWEI7.BE00043Y * 
          *                   NZRRAP.BASEL_MODEL_SCORECRD_DTL                        * 
          *                  BASEL_MODEL_SCORECRD_HDR -            A57SWEI7.BE00043Z * 
          *                   NZRRAP.BASEL_MODEL_SCORECRD_HDR                        * 
          * Target Table:    Join - work.CRITERIAS_CC_&TIME_DIM_ID A57SWEI7.BN000NHW * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YWJ);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let etls_recnt = 0;
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(NZRRAP.BASEL_MODEL_SCORECRD_DTL, DATA)) or 
                  %sysfunc(exist(NZRRAP.BASEL_MODEL_SCORECRD_DTL, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               proc sql noprint;
                  select count(*) into :etls_recnt from NZRRAP.BASEL_MODEL_SCORECRD_DTL;
               quit;
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         %let ETLS_SYSLAST = &SYSLAST;
         /*---- Start of Pre-Process Code  ----*/ 
         
         %LET TIME_DIM_ID = &MTH_TM_ID;
         /*---- End of Pre-Process Code  ----*/ 
         
         %rcSet(&syserr); 
         %rcSet(&sqlrc); 
         
         %let SYSLAST = &ETLS_SYSLAST;
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = -1;
         option DBIDIRECTEXEC;
         
         proc datasets lib = work nolist nowarn memtype = (data view);
            delete CRITERIAS_CC_&TIME_DIM_ID;
         quit;
         
         proc sql;
            create table work.CRITERIAS_CC_&TIME_DIM_ID as
            select
               MONOTONIC() as PRIM_ID length = 8   
                  label = 'PRIM_ID',
               BASEL_MODEL_SCORECRD_HDR.BASEL_MODEL_SCORECRD_HDR_ID length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BASEL_MODEL_SCORECRD_HDR_ID',
               BASEL_MODEL_SCORECRD_DTL.BASEL_MODEL_SCORECRD_DTL_ID length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BASEL_MODEL_SCORECRD_DTL_ID',
               BASEL_MODEL_SCORECRD_HDR.BASEL_MODEL_ID length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BASEL_MODEL_ID',
               BASEL_MODEL_SCORECRD_DTL.BIN length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BIN',
               BASEL_MODEL_SCORECRD_DTL.PT_CNT length = 8   
                  format = 11.
                  informat = 11.
                  label = 'PT_CNT',
               BASEL_MODEL_SCORECRD_DTL.BIN_CRTRIA_SQL_CD_STRG length = 2000   
                  format = $2000.
                  informat = $2000.
                  label = 'BIN_CRTRIA_SQL_CD_STRG',
               &TIME_DIM_ID as TIME_DIM_ID length = 8   
                  label = 'TIME_DIM_ID'
            from
               NZRRAP.BASEL_MODEL_SCORECRD_DTL as BASEL_MODEL_SCORECRD_DTL, 
               NZRRAP.BASEL_MODEL_SCORECRD_HDR as BASEL_MODEL_SCORECRD_HDR
            where
               BASEL_MODEL_SCORECRD_DTL.BASEL_MODEL_SCORECRD_HDR_ID = BASEL_MODEL_SCORECRD_HDR.BASEL_MODEL_SCORECRD_HDR_ID
               and BASEL_MODEL_SCORECRD_HDR.SRC_SYS_CD = 'SPL'
               and (BASEL_MODEL_SCORECRD_HDR.SCORECRD_END_DT = .
               or BASEL_MODEL_SCORECRD_HDR.SCORECRD_END_DT >= &MTH_END_DT)
			   and BASEL_MODEL_SCORECRD_DTL.BIN_CRTRIA_SQL_CD_STRG IS NOT NULL /*RRMSS-3187*/
            ;
         quit;
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = &sys_sql_ip_all;
         
         %rcSet(&sqlrc); 
         
         %rcSet(&syscc); 
         
         
         
         /**  Step end Join **/
         
         /*---- Start of User Written Code  ----*/ 
         
         /*==========================================================================* 
          * Step:            Insert Rows                           A57ZQ4A4.BO000TQJ * 
          * Transform:       Insert Rows                                             * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Tables:   Join - work.CRITERIAS_CC              A57ZQ4A4.BQ000SFF * 
          *                  BASEL_MODEL_SCORECRD_DTL -            A57ZQ4A4.BI0009T0 * 
          *                   NZRRAP.BASEL_MODEL_SCORECRD_DTL                        * 
          *                  BASEL_MODEL_SCORECRD_HDR -            A57ZQ4A4.BI0009T2 * 
          *                   NZRRAP.BASEL_MODEL_SCORECRD_HDR                        * 
          * Target Table:    Insert Rows - work.CRITERIAS_DTL_CC   A57ZQ4A4.BQ000SFJ * 
          *==========================================================================*/ 
         
         /*DELETE DATA FOR A MONTHLY REFRESH*/
         PROC SQL NOPRINT;
         DELETE FROM CRITERIAS_DTL_CC;
         QUIT;
          
         PROC SQL NOPRINT;
         /*SETTING UP PASS THROUGH CONNECTION TO NETEZZA*/
         CONNECT USING NZRRAP AS DBCON;
         
         /*SELECT THE TIME_DIM_ID TO RUN*/
         /*SELECT TIME_DIM_ID INTO :TIME_DIM_ID FROM CRITERIAS_CC;*/
         
         /*PERFORM A COUNT OF VALID CRITERIAS FOR WHICH THE ITERATIVE LOOP WILL EXECUTE*/
         SELECT COUNT(*) INTO :CRITERIA_COUNT FROM CRITERIAS_CC_&TIME_DIM_ID;
         
         %DO ii=1 %TO &CRITERIA_COUNT;
         
         /*FOR EVERY VALUE OF i, SELECT THE SUBQUERY INTO A SAS VARIABLE*/
         SELECT BIN_CRTRIA_SQL_CD_STRG INTO :VAR1 FROM CRITERIAS_CC_&TIME_DIM_ID WHERE PRIM_ID=&ii;
         
         /*INSERT ROW BY ROW INTO THE PREDEFINED SAS TABLE THE ACCOUNTS FOR EVERY CRITERIA*/
         INSERT INTO CRITERIAS_DTL_CC (BASEL_MODEL_SCORECRD_DTL_ID,MTH_TM_ID,BASEL_ACCT_ID,BASEL_MODEL_ID,BASEL_MODEL_SCORECRD_HDR_ID,BIN,PT_CNT,INSRT_PROCESS_TMSTMP)
         SELECT D.BASEL_MODEL_SCORECRD_DTL_ID, COALESCE(X.MTH_TM_ID,0) AS MTH_TM_ID, COALESCE(X.BASEL_ACCT_ID,0) AS BASEL_ACCT_ID, C.BASEL_MODEL_ID, C.BASEL_MODEL_SCORECRD_HDR_ID, D.BIN, D.PT_CNT, &SESSIONTIME AS INSRT_PROCESS_TMSTMP
         FROM NZRRAP.BASEL_MODEL_SCORECRD_HDR C, NZRRAP.BASEL_MODEL_SCORECRD_DTL D, (SELECT * FROM CONNECTION TO DBCON (&VAR1)) X
         WHERE C.BASEL_MODEL_SCORECRD_HDR_ID=D.BASEL_MODEL_SCORECRD_HDR_ID
         AND D.BASEL_MODEL_SCORECRD_HDR_ID=(SELECT BASEL_MODEL_SCORECRD_HDR_ID FROM CRITERIAS_CC_&TIME_DIM_ID WHERE PRIM_ID=&ii) 
         AND D.BASEL_MODEL_SCORECRD_DTL_ID=(SELECT BASEL_MODEL_SCORECRD_DTL_ID FROM CRITERIAS_CC_&TIME_DIM_ID WHERE PRIM_ID=&ii)
         AND D.BIN=(SELECT BIN FROM CRITERIAS_CC_&TIME_DIM_ID WHERE PRIM_ID=&ii);
         
         /*END THE DO LOOP*/
         %END;
         
         /*QUIT THE PROC SQL PROCEDURE*/
         DISCONNECT FROM DBCON;
         QUIT;
         /*---- End of User Written Code  ----*/ 
         
         /*==========================================================================* 
          * Step:            Table Loader                          A57SWEI7.BK000YWL * 
          * Transform:       Table Loader (Version 2.1)                              * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Table:    Insert Rows - work.CRITERIAS_DTL_CC   A57SWEI7.BN000NHX * 
          * Target Table:    BASEL_PNL_LN_SCORE_DTL_SNAPSHOT -     A57SWEI7.BE000444 * 
          *                  NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPS                     * 
          *                  HOT                                                     * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YWL);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let SYSLAST = %nrquote(work.CRITERIAS_DTL_CC); 
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = -1;
         option DBIDIRECTEXEC;
         
         %global etls_tableExist;
         %global etls_numIndex;
         %global etls_lastTable;
         %let etls_tableExist = -1; 
         %let etls_numIndex = -1; 
         %let etls_lastTable = &SYSLAST; 
         
         /*---- Define load data macro  ----*/ 
         
         /* --------------------------------------------------------------
            Load Technique Selection: AppendToExisting - AppendProc
            Constraint and index action selections: 'ASIS','ASIS','ASIS','ASIS'
            Additional options selections... 
               Set unmapped to missing on updates: false 
            -------------------------------------------------------------- */
         %macro etls_loader;
         
            %let etls_tableOptions = ;
            
            /*---- Map the columns  ----*/ 
            proc datasets lib = work nolist nowarn memtype = (data view);
               delete W2G6RMM;
            quit;
            
            %put %str(NOTE: Mapping columns ...);
            proc sql;
               create view work.W2G6RMM as
                  select
                     MTH_TM_ID,
                     BASEL_ACCT_ID   
                        format = 20.
                        informat = 20.,
                     BASEL_MODEL_ID,
                     BASEL_MODEL_SCORECRD_HDR_ID,
                     BASEL_MODEL_SCORECRD_DTL_ID,
                     PT_CNT,
                     BIN,
                     (%SYSFUNC(DATETIME(),BEST18.)) as INSRT_PROCESS_TMSTMP length = 8
                        format = DATETIME25.6
                        informat = DATETIME25.6,
                     (%SYSFUNC(DATETIME(),BEST18.)) as UPDT_PROCESS_TMSTMP length = 8
                        format = DATETIME25.6
                        informat = DATETIME25.6
               from &etls_lastTable
               ;
            quit;
            
            %let SYSLAST = work.W2G6RMM;
            
            %let etls_lastTable = &SYSLAST; 
            %let etls_tableOptions = ; 
            
            /* Determine if the target table exists  */ 
            %let etls_tableExist = %eval(%sysfunc(exist(NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPSHOT, DATA)) or 
                  %sysfunc(exist(NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPSHOT, VIEW))); 
            
            /*---- Create a new table  ----*/ 
            %if (&etls_tableExist eq 0) %then 
            %do;  /* if table does not exist  */ 
            
               %put %str(NOTE: Creating table ...);
               
               data NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPSHOT
                       (dbnull = (
                                  MTH_TM_ID = NO
                                  BASEL_ACCT_ID = NO
                                  BASEL_MODEL_ID = NO
                                  BASEL_MODEL_SCORECRD_HDR_ID = NO
                                  BASEL_MODEL_SCORECRD_DTL_ID = NO
                                  PT_CNT = YES
                                  BIN = YES
                                  INSRT_PROCESS_TMSTMP = NO
                                  UPDT_PROCESS_TMSTMP = YES));
                  attrib MTH_TM_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'MTH_TM_ID'; 
                  attrib BASEL_ACCT_ID length = 8
                     format = 20.
                     informat = 20.
                     label = 'BASEL_ACCT_ID'; 
                  attrib BASEL_MODEL_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_ID'; 
                  attrib BASEL_MODEL_SCORECRD_HDR_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_SCORECRD_HDR_ID'; 
                  attrib BASEL_MODEL_SCORECRD_DTL_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_SCORECRD_DTL_ID'; 
                  attrib PT_CNT length = 8
                     format = 11.
                     informat = 11.
                     label = 'PT_CNT'; 
                  attrib BIN length = 8
                     format = 11.
                     informat = 11.
                     label = 'BIN'; 
                  attrib INSRT_PROCESS_TMSTMP length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'INSRT_PROCESS_TMSTMP'; 
                  attrib UPDT_PROCESS_TMSTMP length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'UPDT_PROCESS_TMSTMP'; 
                  call missing(of _all_);
                  stop;
               run;
               
               %rcSet(&syserr); 
               
            %end;  /* if table does not exist  */ 
            
            /*---- Append  ----*/ 
            %put %str(NOTE: Appending data ...);
            
            proc append base = NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPSHOT (BULKLOAD=YES BL_METHOD=CLILOAD)
               data = &etls_lastTable (&etls_tableOptions)  force ; 
             run; 
            
            %rcSet(&syserr); 
            
            proc datasets lib = work nolist nowarn memtype = (data view);
               delete W2G6RMM;
            quit;
           
         %mend etls_loader;
         %etls_loader;
         
         %let etls_recnt = 0;
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPSHOT, DATA)) or 
                  %sysfunc(exist(NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPSHOT, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               proc sql noprint;
                  select count(*) into :etls_recnt from NZRRAP.BASEL_PNL_LN_SCORE_DTL_SNAPSHOT;
               quit;
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         /*---- Start of Post-Process Code  ----*/ 
         
         %etl_job_end;
         /*---- End of Post-Process Code  ----*/ 
         
         %rcSet(&syserr); 
         %rcSet(&sqlrc); 
         
         %rcSet(&syscc); 
         
         
         
         /**  Step end Table Loader **/
         
      %mend etls_jobW2G6RM9; 
      
      %etls_jobW2G6RM9; 
      
      %let handleName = %etls_getHandle(statusTable=&etls_statusTable,
         handleVariable=etls_handleName, row=&&&etls_controlName);
         
      %let job_rc&handleName. = &job_rc; 
      
      %etls_freeHandle(statusTable=&etls_statusTable, statusVariable=etls_status, 
         handleVariable=etls_handleName, handleName="&handleName", 
         startTimeVariable=etls_startTime, endTimeVariable=etls_endTime, signoff=0, 
         returnCodeVariable=etls_jobRC, returnCodeMacroVariable=job_rc&handleName., setMainJobRc=1); 
         
   %mend etls_processToLoopW2G6RLZ;
   
   %local etls_controlTable etls_statusTable etls_controlName 
      etls_processesRunning etls_maxProcesses etls_parameterTable 
      etls_additionalSignonOptions etls_signonRetries;
   %let etls_controlName = J_; 
   %let etls_statusTable = work.W3XPB3S0; 
   %let etls_parameterTable = work.W2G6RMY; 
   %let &etls_controlName = 0;
   %let etls_controlTable = work.TIME_KEY;
   
   %put %str(NOTE: Creating status table...);
   data &etls_statusTable 
      (keep = etls_handleName etls_machineId etls_startTime etls_endTime etls_status 
              etls_jobRC TM_ID TM_LVL_END_DT
      );
      attrib etls_handleName length = $32
         label = 'Name of handle to remote session'; 
      attrib etls_machineId length = $32
         label = 'Name of machine executing the task'; 
      attrib etls_startTime length = 8
         format = nldatmap.
         label = 'Start time of task'; 
      attrib etls_endTime length = 8
         format = nldatmap.
         label = 'End time of task'; 
      attrib etls_status length = $32
         label = 'Current status of task'; 
      attrib etls_jobRC length = 8
         label = 'Return code of task'; 
      attrib TM_ID length = 8
         format = 11.
         informat = 11.
         label = 'TM_ID'; 
      attrib TM_LVL_END_DT length = 8
         format = DATE9.
         informat = DATE9.
         label = 'TM_LVL_END_DT'; 
      set &etls_controlTable;
   run;
   
   %rcSet(&syserr); 
   
   %put %str(NOTE: Creating parameter table...);
   proc sql; 
      create table &etls_parameterTable as 
         select TM_ID as MTH_TM_ID, 
                TM_LVL_END_DT as MTH_END_DT, 
                TM_ID as TIME_DIM_ID, 
                TM_ID as DEL_MTH_TM_ID 
         from &etls_controlTable; 
   quit; 
   
   %rcSet(&sqlrc); 
   
   /* Get the number of times to iterate from the number of rows in the source  */ 
   /*  table                                                                    */ 
   proc sql noprint; 
      select count(*) into :&etls_controlName._max from &etls_statusTable;
      %let &etls_controlName._max = &&&etls_controlName._max;
   quit;
   
   %rcSet(&sqlrc); 
   
   %let etls_maxProcesses = 1; 
   
   %if (&etls_maxProcesses > 0) %then 
   %do; 
      %do %until (&&&etls_controlName ge &&&etls_controlName._max); 
      
         %let etls_lastLoopPtr = &&&etls_controlName;
         
         %let etls_processesRunning = 0; 
         
         %do %while(&etls_processesRunning lt &&&etls_controlName._max 
            and &etls_processesRunning lt &etls_maxProcesses 
            and &&&etls_controlName lt &&&etls_controlName._max);
            
            %let &etls_controlName = %eval(&&&etls_controlName+1);
            
            %let job_rcLast=&job_rc; 
            
            %etls_createHandle(statusTable=&etls_statusTable,
                               statusVariable=etls_status,
                               handleVariable=etls_handleName,
                               handlePrefix=&etls_controlName,
                               statusSetting="Running",
                               row=&&&etls_controlName,
                               machineVariable=etls_machineId,
                               startTimeVariable=etls_startTime,
                               signon=0,
                               useGrid=0);
            
         %let etls_processesRunning = 1; 
         %etls_processToLoopW2G6RLZ(parameterTable=&etls_parameterTable, row=&&&etls_controlName,
            handleName=%etls_getHandle(statusTable=&etls_statusTable,
            handleVariable=etls_handleName, row=&&&etls_controlName)); 
         
         /* Reset main Job_RC and Trans_RC to max return code of all iterations */ 
         %let job_rcThisIter=&job_rc; 
         %let job_rc=&job_rcLast; 
         %let trans_rc=&job_rcLast; 
         %rcSet(&job_rcThisIter); 
      %end; 
      
   %end; 
   
%end;

proc datasets lib = work nolist nowarn memtype = (data view);
   delete W2G6RMY;
quit;

%mend etls_loopW2G6RLP; 

%etls_loopW2G6RLP; 

%rcSet(&syscc); 



/**  Step end Loop **/

/*==========================================================================* 
 * Step:            Loop End                              A57SWEI7.BK000YWM * 
 * Transform:       Loop End                                                * 
 * Description:                                                             * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YWM);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%rcSet(&syscc); 



/**  Step end Loop End **/

%let etls_endTime = %sysfunc(datetime(),datetime.);

