/**************************************************************************** 
 * Job:             J_RRAP_CCAR_00_RRAP_DATA_SYNC_FROM_P  A57ZQ4A4.BF0011P6 * 
 *                  ROD                                                     * 
 * Description:                                                             * 
 *                                                                          * 
 * Metadata Server: cs2iw503.bns                                            * 
 * Port:            8561                                                    * 
 * Location:        /Projects/RRAP/06 - DLGD/CCAR_ECAP                      * 
 *                                                                          * 
 * Server:          SASApp                                A57ZQ4A4.AT000002 * 
 *                                                                          * 
 * Generated on:    Thursday, July 20, 2017 4:54:01 PM EDT                  * 
 * Generated by:    s2809211                                                * 
 * Version:         SAS Data Integration Studio 4.8                         * 
 ****************************************************************************/ 

/* Generate the process id for job  */ 
%put Process ID: &SYSJOBID;

/* General macro variables  */ 
%let jobID = %quote(A57ZQ4A4.BF0011P6);
%let etls_jobName = %nrquote(J_RRAP_CCAR_00_RRAP_DATA_SYNC_FROM_PROD);
%let etls_userID = %nrquote(s2809211);

/* Setup to capture return codes  */ 
%global job_rc trans_rc sqlrc syscc;
%let sysrc = 0;
%let job_rc = 0;
%let trans_rc = 0;
%let sqlrc = 0;
%let syscc = 0;
%global etls_stepStartTime; 
/* initialize syserr to 0 */
data _null_; run;

%macro rcSet(error); 
   %if (&error gt &trans_rc) %then 
      %let trans_rc = &error;
   %if (&error gt &job_rc) %then 
      %let job_rc = &error;
%mend rcSet; 

%macro rcSetDS(error); 
   if &error gt input(symget('trans_rc'),12.) then 
      call symput('trans_rc',trim(left(put(&error,12.))));
   if &error gt input(symget('job_rc'),12.) then 
      call symput('job_rc',trim(left(put(&error,12.))));
%mend rcSetDS; 

/* Create metadata macro variables */
%let IOMServer      = %nrquote(SASApp);
%let metaPort       = %nrquote(8561);
%let metaServer     = %nrquote(cs2iw503);

/* Set metadata options */
options metaport       = &metaPort 
        metaserver     = "&metaServer"; 

/* Setup for capturing job status  */ 
%let etls_startTime = %sysfunc(datetime(),datetime.);
%let etls_recordsBefore = 0;
%let etls_recordsAfter = 0;
%let etls_lib = 0;
%let etls_table = 0;

%global etls_debug; 
%macro etls_setDebug; 
   %if %str(&etls_debug) ne 0 %then 
      OPTIONS MPRINT%str(;); 
%mend; 
%etls_setDebug; 

/*---- Start of User Written Code  ----*/ 

libname P_db2 db2 datasrc=DM1P1D schema=EDRRAPT AUTHDOMAIN=DB2_FC_AUTH access=readonly;
libname D_db2 db2 datasrc=DM1D1D schema=EDRRAPT authdomain=db2_auth ;

%MACRO LD(TBL=);
PROC SQL;
CONNECT USING D_DB2 AS DBCON;
EXECUTE(DELETE FROM EDRRAPT.&TBL.) BY DBCON;
QUIT;

PROC APPEND BASE=D_DB2.&TBL. ( BULKLOAD=YES BL_METHOD=CLILOAD ) DATA=P_DB2.&TBL. FORCE; RUN;
%MEND LD;

%LD(TBL=BASEL_SUBSIDIARY_ACL_LKP);
%LD(TBL=BASEL_AIRB_PRD_LKP);
%LD(TBL=BASEL_AIRB_GUARNT_LKP);
%LD(TBL=BASEL_NCR_BD_RECD_TP_DIM);
%LD(TBL=BASEL_NCR_EAD_SEG_DIM);
%LD(TBL=BASEL_NCR_EXPSR_SIZE_DIM);
%LD(TBL=Basel_expsr_cl_dim);
%LD(TBL=Dlqnt_dim);
%LD(TBL=BASEL_NCR_GEO_DIM);
%LD(TBL=BASEL_NCR_HIERARCHY_LKP);

%LD(TBL=BASEL_NCR_LGD_SEG_DIM);
%LD(TBL=BASEL_NCR_LTV_DIM);
%LD(TBL=BASEL_NCR_PD_BAND_DIM);
%LD(TBL=BASEL_NCR_PD_SEG_DIM);
%LD(TBL=BASEL_NCR_RISK_RT_SYS_DIM);
%LD(TBL=BASEL_NCR_RT_DIM);
%LD(TBL=BASEL_NCR_SECRTZTN_DIM);
/*---- End of User Written Code  ----*/ 

%let etls_endTime = %sysfunc(datetime(),datetime.);

