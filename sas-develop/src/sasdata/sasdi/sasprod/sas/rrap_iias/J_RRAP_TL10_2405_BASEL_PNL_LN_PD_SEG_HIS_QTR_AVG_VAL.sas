/**************************************************************************** 
 * Job:             J_RRAP_TL10_2405_BASEL_PNL_LN_PD_SEG  A57SWEI7.BH0009IV * 
 *                  _HIS_QTR_AVG_VAL                                        * 
 * Description:     target table is BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL        * 
 *                                                                          * 
 * Metadata Server: 10.56.70.48                                             * 
 * Port:            8561                                                    * 
 * Location:        /Projects/RRAP/Jobs/02 - Term Loans/2.4                 * 
 *                                                                          * 
 * Server:          SASApp                                A57SWEI7.AT000002 * 
 *                                                                          * 
 * Source Tables:   TM_DIM - NZRRAP.TM_DIM                A57SWEI7.BE000412 * 
 *                  BASEL_MODEL - NZRRAP.BASEL_MODEL      A57SWEI7.BE00041K * 
 *                  BASEL_SEG - NZRRAP.BASEL_SEG          A57SWEI7.BE00041L * 
 *                  BASEL_SEG_HISTRC_AVG_LKP -            A57SWEI7.BE000447 * 
 *                   NZRRAP.BASEL_SEG_HISTRC_AVG_LKP                        * 
 *                  TM_DIM - NZRRAP.TM_DIM                A57SWEI7.BE000412 * 
 *                  BASEL_PNL_LN_PD_SEG_MTH_RLZ_VAL -     A57SWEI7.BE000443 * 
 *                  NZRRAP.BASEL_PNL_LN_PD_SEG_MTH_RLZ_                     * 
 *                  VAL                                                     * 
 * Target Tables:   BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL -    A57SWEI7.BE000446 * 
 *                  NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG                     * 
 *                  _VAL                                                    * 
 *                  BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL -    A57SWEI7.BE000446 * 
 *                  NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG                     * 
 *                  _VAL                                                    * 
 *                                                                          * 
 * Generated on:    Thursday, June 29, 2017 2:39:52 PM EDT                  * 
 * Generated by:    owprdsas                                                * 
 * Version:         SAS Data Integration Studio 4.8                         * 
 ****************************************************************************/ 

/* Generate the process id for job  */ 
%put Process ID: &SYSJOBID;

/* General macro variables  */ 
%let jobID = %quote(A57SWEI7.BH0009IV);
%let etls_jobName = %nrquote(J_RRAP_TL10_2405_BASEL_PNL_LN_PD_SEG_HIS_QTR_AVG_VAL);
%let etls_userID = %nrquote(owprdsas);

/* Setup to capture return codes  */ 
%global job_rc trans_rc sqlrc syscc;
%let sysrc = 0;
%let job_rc = 0;
%let trans_rc = 0;
%let sqlrc = 0;
%let syscc = 0;
%global etls_stepStartTime; 
/* initialize syserr to 0 */
data _null_; run;

%macro rcSet(error); 
   %if (&error gt &trans_rc) %then 
      %let trans_rc = &error;
   %if (&error gt &job_rc) %then 
      %let job_rc = &error;
%mend rcSet; 

%macro rcSetDS(error); 
   if &error gt input(symget('trans_rc'),12.) then 
      call symput('trans_rc',trim(left(put(&error,12.))));
   if &error gt input(symget('job_rc'),12.) then 
      call symput('job_rc',trim(left(put(&error,12.))));
%mend rcSetDS; 

/* Create metadata macro variables */
%let IOMServer      = %nrquote(SASApp);
%let metaPort       = %nrquote(8561);
%let metaServer     = %nrquote(iw503);

/* Set metadata options */
options metaport       = &metaPort 
        metaserver     = "&metaServer"; 

/* Setup for capturing job status  */ 
%let etls_startTime = %sysfunc(datetime(),datetime.);
%let etls_recordsBefore = 0;
%let etls_recordsAfter = 0;
%let etls_lib = 0;
%let etls_table = 0;

%global etls_debug; 
%macro etls_setDebug; 
   %if %str(&etls_debug) ne 0 %then 
      OPTIONS MPRINT%str(;); 
%mend; 
%etls_setDebug; 

/*---- Start of Pre-Process Code  ----*/ 

%GLOBAL TIME_DIM_ID;
%GLOBAL MTH_TM_ID;
%GLOBAL MTH_END_DT;
%GLOBAL DEL_MTH_TM_ID;
%GLOBAL SESSIONTIME;
%GLOBAL EARLIEST_FISCAL;
%GLOBAL RECENT_FISCAL;
%GLOBAL EARLIEST_FISCAL_DT;
%GLOBAL RECENT_FISCAL_DT;
%GLOBAL INCLUSION_KEYS;
%GLOBAL EXCLUSION_KEYS;
%GLOBAL MONTH_NUMBER;
%LET SESSIONTIME=%SYSFUNC(DATETIME(),BEST18.);
%PUT %SYSFUNC(GETOPTION(WORK));

/*---- End of Pre-Process Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 

/*==========================================================================* 
 * Step:            AM_rrap_spl_autoexec                  A57SWEI7.BK000YZ4 * 
 * Transform:       AM_rrap_spl_autoexec                                    * 
 * Description:     Autoexec file for termloan jobs. This points to auto    * 
 *                   call macros                                            * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YZ4);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0; 
%let _OUTPUT_count = 0; 


%put WORK LOCATION: %sysfunc(getoption(work));
%rrap_spl_autoexec;


%rcSet(&syserr); 
%rcSet(&sysrc); 
%rcSet(&sqlrc); 

%rcSet(&syscc); 



/**  Step end AM_rrap_spl_autoexec **/

/*==========================================================================* 
 * Step:            Extract_run_dates                     A57SWEI7.BK000YZ5 * 
 * Transform:       Extract                                                 * 
 * Description:                                                             * 
 *                                                                          * 
 * Source Table:    TM_DIM - NZRRAP.TM_DIM                A57SWEI7.BE000412 * 
 * Target Table:    Extract - work.TIME_KEY               A57SWEI7.BN000NJ3 * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YZ5);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let etls_recnt = 0;
%macro etls_recordCheck; 
   %let etls_recCheckExist = %eval(%sysfunc(exist(NZRRAP.TM_DIM, DATA)) or 
         %sysfunc(exist(NZRRAP.TM_DIM, VIEW))); 
   
   %if (&etls_recCheckExist) %then
   %do;
      proc sql noprint;
         select count(*) into :etls_recnt from NZRRAP.TM_DIM;
      quit;
   %end;
%mend etls_recordCheck;
%etls_recordCheck;

%let SYSLAST = %nrquote(NZRRAP.TM_DIM); 

%let ETLS_SYSLAST = &SYSLAST;
/*---- Start of Pre-Process Code  ----*/ 

%get_model_period_dates(product=spl);
/*%let start_period_dt=31jul2012;*/
/*%let end_period_dt=31jul2012;*/
%put Start and End Dates for SPL Models:;
%put start_period_dt: &start_period_dt ;
%put end_period_dt: &end_period_dt;

/*---- End of Pre-Process Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 

%let SYSLAST = &ETLS_SYSLAST;

%global etls_sql_pushDown;
%let etls_sql_pushDown = -1;
option DBIDIRECTEXEC;

/*---- Map the columns  ----*/ 
proc datasets lib = work nolist nowarn memtype = (data view);
   delete TIME_KEY;
quit;

%put %str(NOTE: Mapping columns ...);
proc sql;
   create table work.TIME_KEY as
      select
         TM_ID,
         TM_YR_SEQ_NUM,
         TM_LVL_END_DT
   from &SYSLAST
      where TM_LVL='Month' & TM_LVL_END_DT BETWEEN "&start_period_dt"d and "&end_period_dt"d
   order by
      TM_ID
   ;
quit;

%let SYSLAST = work.TIME_KEY;

%global etls_sql_pushDown;
%let etls_sql_pushDown = &sys_sql_ip_all;

%rcSet(&sqlrc); 

/*---- Start of Post-Process Code  ----*/ 

PROC SQL NOPRINT;
SELECT TM_YR_SEQ_NUM INTO :MONTH_NUMBER FROM TIME_KEY;
QUIT;
/*---- End of Post-Process Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 

%rcSet(&syscc); 



/**  Step end Extract_run_dates **/

/*==========================================================================* 
 * Step:            Conditional Start                     A57SWEI7.BK000YZ6 * 
 * Transform:       Conditional Start                                       * 
 * Description:                                                             * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YZ6);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let ETLS_SYSLAST = &SYSLAST;
/*---- Start of Pre-Process Code  ----*/ 

%PUT MONTH_NUMBER IS &MONTH_NUMBER;
/*---- End of Pre-Process Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 

%let SYSLAST = &ETLS_SYSLAST;

%macro etls_conditionW2J8VPXI;
   %local etls_condition;
   %let etls_conditionTrue = %eval((&MONTH_NUMBER=1 OR &MONTH_NUMBER=4 OR &MONTH_NUMBER=7 OR &MONTH_NUMBER=10));
   %if (&etls_conditionTrue=0) %then
   %do;
      %put ETLS_DIAG: Condition flow did NOT execute, condition was (&MONTH_NUMBER=1 OR &MONTH_NUMBER=4 OR &MONTH_NUMBER=7 OR &MONTH_NUMBER=10);
      %goto exitetls_conditionW2J8VPXI;
   %end;
   %else
   %do;
      %put ETLS_DIAG: Condition flow did execute, condition was (&MONTH_NUMBER=1 OR &MONTH_NUMBER=4 OR &MONTH_NUMBER=7 OR &MONTH_NUMBER=10);
   %end;
/*---- Start of User Written Code  ----*/ 

%put &sysver;
OPTIONS MINOPERATOR;
%LET MONTH_NUMBER=0;
%MACRO QUARTER_END /mindelimiter=',';

PROC SQL NOPRINT;
CREATE TABLE LOOP_LIST_RESULTS
(
BASEL_SEG_ID NUM,
RPTG_QTR_MTH_TM_ID NUM,
BASEL_MODEL_RLTNP_ID NUM,
BASEL_MODEL_ID NUM,
HISTRC_AVG_SEG_RTO NUM FORMAT=30.8,
HISTRC_AVG_FINAL_RPTG_RTO NUM FORMAT=30.8,
QTR_ST_MTH_TM_ID NUM,
QTR_END_MTH_TM_ID NUM,
INSRT_PROCESS_TMSTMP NUM INFORMAT=DATETIME. FORMAT=DATETIME.
);
QUIT;

PROC SQL NOPRINT;
SELECT MIN(MTH_TM_ID), MAX(MTH_TM_ID) INTO :EARLIEST_FISCAL, :RECENT_FISCAL FROM NZRRAP.BASEL_PNL_LN_PD_SEG_MTH_RLZ_VAL;
SELECT TM_LVL_END_DT INTO :EARLIEST_FISCAL_DT FROM NZRRAP.TM_DIM WHERE TM_ID =&EARLIEST_FISCAL;
SELECT TM_LVL_END_DT INTO :RECENT_FISCAL_DT FROM NZRRAP.TM_DIM WHERE TM_ID =&RECENT_FISCAL;
SELECT TM_ID INTO :EXCLUSION_KEYS SEPARATED BY ',' FROM NZRRAP.TM_DIM WHERE TM_LVL='Month' AND TM_LVL_END_DT IN (SELECT EXCLD_MTH_END_DT FROM NZRRAP.DT_EXCLSN_LKP);
QUIT;

%PUT EARLIEST FISCAL IS &EARLIEST_FISCAL;
%PUT RECENT FISCAL IS &RECENT_FISCAL;
%PUT EARLIEST FISCAL DATE IS &EARLIEST_FISCAL_DT;
%PUT RECENT FISCAL DATE IS &RECENT_FISCAL_DT;
%PUT EXCLUSION TIME KEYS ARE &EXCLUSION_KEYS;

%MEND;
%QUARTER_END;


/*---- End of User Written Code  ----*/ 

/*==========================================================================* 
 * Step:            Loop                                  A57SWEI7.BK000YZ8 * 
 * Transform:       Loop                                                    * 
 * Description:                                                             * 
 *                                                                          * 
 * Source Table:    Extract - work.TIME_KEY               A57SWEI7.BN000NJ3 * 
 * Target Table:    Loop - work.W2KPZCVC                  A57SWEI7.BN000NJ4 * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YZ8);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let etls_recCheckExist = 0; 
%let etls_recnt = 0; 
%macro etls_recordCheck; 
   %let etls_recCheckExist = %eval(%sysfunc(exist(work.TIME_KEY, DATA)) or 
         %sysfunc(exist(work.TIME_KEY, VIEW))); 
   
   %if (&etls_recCheckExist) %then
   %do;
      %local etls_syntaxcheck; 
      %let etls_syntaxcheck = %sysfunc(getoption(syntaxcheck)); 
      /* Turn off syntaxcheck option to perform following steps  */ 
      options nosyntaxcheck;
      
      proc contents data = work.TIME_KEY out = work.etls_contents(keep = nobs) noprint; 
      run; 
      
      data _null_; 
         set work.etls_contents (obs = 1); 
         call symput("etls_recnt", left(put(nobs,32.))); 
      run;
      
      proc datasets lib = work nolist nowarn memtype = (data view);
         delete etls_contents;
      quit;
      
      /* Reset syntaxcheck option to previous setting  */ 
      options &etls_syntaxcheck; 
   %end;
%mend etls_recordCheck;
%etls_recordCheck;

%let SYSLAST = %nrquote(work.TIME_KEY); 

/* Capture summary statistics about this step for performance reporting  */ 
%put "DIS_SUMM";
%macro etls_getHandle(statusTable=, handleVariable=, row=); 
   %let etls_dsid = %sysfunc(open(&statusTable)); 
   %if (&etls_dsid = 0) %then 
      %put %sysfunc(sysmsg()); 
   %else 
   %do; 
      %let rc = %sysfunc(fetchobs(&etls_dsid, &row)); 
      %if (&rc ne 0) %then 
         %put %sysfunc(sysmsg()); 
      %else 
      %do; 
         %let etls_varnum = %sysfunc(varnum(&etls_dsid,&handleVariable)); 
         %if (&etls_varnum > 0) %then 
            %sysfunc(getvarc(&etls_dsid,&etls_varnum)); 
         %else 
            %put %sysfunc(sysmsg()); 
      %end; 
      %let rc = %sysfunc(close(&etls_dsid)); 
   %end; 
%mend etls_getHandle; 

%macro etls_freeHandle(statusTable=, statusVariable=, handleVariable=, 
   handleName=, statusSetting="Finished", 
   endTimeVariable=endTime, startTimeVariable=startTime, signoff=1, 
   returnCodeVariable=, returnCodeMacroVariable=, setMainJobRC=1, 
   statusUnknownReturnCode=., startTimeMacroVariable=, endTimeMacroVariable= ); 

   %if (&statusTable ne ) %then 
   %do; 
      %local etls_rcMacroVarExisted; 
      %let etls_rcMacroVarExisted = 0; 
      %if ("&returnCodeMacroVariable" ne "") %then 
      %do; 
         proc sql noprint; 
            select '1' into: etls_rcMacroVarExisted from dictionary.macros 
            where name=upcase("&returnCodeMacroVariable"); 
         quit; 

         %rcSet(&sqlrc); 
         %if (&etls_rcMacroVarExisted = 0) %then 
         %do; 
            %put WARNING%QUOTE(:) Return code from inner job not found.  Setting status to Unknown.;
            %let &returnCodeMacroVariable=&statusUnknownReturnCode; 
         %end; 
         %if (&setMainJobRC eq 1) %then 
            %rcSet(&&&returnCodeMacroVariable); 
      %end; 
      %else 
      %do; 
         %let returnCodeMacroVariable=etls_rcmacvar; 
         %let &returnCodeMacroVariable=&statusUnknownReturnCode; 
      %end; 

      %local etls_startTimeMacroVarExisted; 
      %let etls_startTimeMacroVarExisted = 0; 
      %if ("&startTimeMacroVariable" ne "") %then 
      %do; 
         proc sql noprint; 
            select '1' into: etls_startTimeMacroVarExisted from dictionary.macros 
            where name=upcase("&startTimeMacroVariable"); 
         quit; 

         %rcSet(&sqlrc); 
         %if (&etls_startTimeMacroVarExisted = 0) %then 
         %do; 
            %put WARNING%QUOTE(:) Start time from inner job not found.  No value will be set.;
            %let &startTimeMacroVariable=; 
         %end; 
      %end; 

      %local etls_endTimeMacroVarExisted; 
      %let etls_endTimeMacroVarExisted = 0; 
      %if ("&endTimeMacroVariable" ne "") %then 
      %do; 
         proc sql noprint; 
            select '1' into: etls_endTimeMacroVarExisted from dictionary.macros 
            where name=upcase("&endTimeMacroVariable"); 
         quit; 

         %rcSet(&sqlrc); 
         %if (&etls_endTimeMacroVarExisted = 0) %then 
         %do; 
            %put WARNING%QUOTE(:) End time from inner job not found.  Setting end time to current
      time.;
            %let &endTimeMacroVariable=%sysfunc(datetime()); 
         %end; 
      %end; 

      data &statusTable; 
         modify &statusTable(where=(&handleVariable = &handleName)); 
         %if ("&startTimeMacroVariable" ne "") %then 
         %do; 
            &startTimeVariable = input(symget("&startTimeMacroVariable"),32.);; 
         %end; 
         %if ("&endTimeVariable" ne "") %then 
         %do; 
            %if ("&endTimeMacroVariable" ne "") %then 
            %do; 
               &endTimeVariable = input(symget("&endTimeMacroVariable"),32.);; 
            %end; 
            %else 
               &endTimeVariable = datetime();; 
         %end; 
         %if ("&returnCodeVariable" ne "") %then 
            &returnCodeVariable = input(symget("&returnCodeMacroVariable"),32.);; 
         %if ("&statusVariable" ne "") %then 
         %do; 
            if (symget("etls_rcMacroVarExisted") eq "0") then 
               &statusVariable = "Unknown Status"; 
            else
               &statusVariable = &statusSetting;
         %end;
         call symput('handle',&handleVariable); 
         replace; 
         stop; 
      run; 

      %rcSet(&syserr); 

      %if (&signoff eq 1) %then 
         %etls_signoff(handleName=&handle); 
   %end; 
%mend etls_freeHandle; 

%macro etls_createHandle(statusTable=,
                         statusVariable=,
                         handleVariable=,
                         handlePrefix=rmt,
                         gridOptionSet=,
                         workloadMacroVariable=,
                         row=,
                         machineVariable=,
                         statusSetting="Running", 
                         startTimeVariable=startTime,
                         signon=1,
                         useGrid=1,
                         log=,
                         output=,
                         gridRC=,
                         cmacvar=etls_signonStatus,
                         additionalSignonOptions=,
                         signonRetries= ); 

   %local remoteSessionId; 
   %let remoteSessionId = &handlePrefix.&row; 
   %let &cmacvar = 1; 
   %local etls_machineId; 
   %if (&signon eq 1) %then 

      %etls_signon(handleName=&remoteSessionId,
                   useGrid=&useGrid,
                   machineIdMacroVariable=etls_machineId,
                   gridOptionSet=&etls_gridOptionSet,
                   workloadMacroVariable=&workloadMacroVariable,
                   log=&log,
                   output=&output,
                   cmacvar=&cmacvar,
                   gridRC=&gridRC,
                   additionalSignonOptions=&additionalSignonOptions,
					signonRetries=&signonRetries,
                   gridJobName=DIS_&etls_jobName._&row); 

   %else %let &cmacvar=0; 

   data &statusTable; 
      retain ptr &row; 
      modify &statusTable point = ptr; 
      &handleVariable = "&remoteSessionId"; 
      %if (&signon eq 1) %then 
         &machineVariable = "&etls_machineId";; 
      %if (&&&cmacvar ne 0) %then 
      %do; 
         &statusVariable = "Failed Signon"; 
      %end; 
      %else 
         &statusVariable = &statusSetting;; 
      %if (&startTimeVariable ne ) %then 
         &startTimeVariable = datetime();; 
      replace; 
      stop; 
   run; 

   %rcSet(&syserr); 
%mend etls_createHandle; 

%macro etls_getParameterNames(parameterTable=, parameterVariableMacro=, startingColumnNumber=1);
   %let &parameterVariableMacro = ;
   %let dsid = %sysfunc(open(&parameterTable));
   %if (&dsid gt 0) %then 
   %do; 
      %do i=&startingColumnNumber %to %sysfunc(attrn(&dsid,nvars)); 
         %let &parameterVariableMacro = &&&parameterVariableMacro %sysfunc(varname(&dsid,&i)); 
      %end; 
      %let dsid = %sysfunc(close(&dsid)); 
   %end; 
   %else 
      %put %sysfunc(sysmsg()); 
   %rcSet(&syserr); 
%mend etls_getParameterNames; 

%macro etls_getParameters(parameterTable=, row=, startingColumnNumber=1 , handleName=); 
   data _null_; 
      length vname $256 vtype $1 value $32767; 
      dsid = open("&parameterTable"); 
      if (dsid > 0) then 
      do; 
         do _i = 1 to &row; 
            fetchrc = fetch(dsid); 
         end; 
         do _i=&startingColumnNumber to attrn(dsid,'nvars'); 
            vname = varname(dsid,_i); 
            vtype = vartype(dsid,_i); 
            if (fetchrc = 0) then 
            do; 
               if (vtype = 'C') then 
               do; 
                  value = getvarc(dsid,_i); 
                  value = tranwrd(value,"%","%%"); 
                  value = tranwrd(value,"(","%("); 
                  value = tranwrd(value,")","%)"); 
                  value = tranwrd(value,'"','%"'); 
                  value = tranwrd(value,"'","%'"); 
               end; 
               else 
                  value = left(put(getvarn(dsid,_i),best32.)); 
            end; /* fetchrc = 0 */ 
            
            put ;
            if indexc(trimn(value),"+-*/<>=^~;, '()&%",'"')>0 then 
            do; 
               value='%nrstr('||trim(value)||')'; 
               put "ETLS_DIAG%QUOTE(:) Special characters encountered; References may require: %nrbquote(%)UNQUOTE(&" vname+(-1)').';
               put "NOTE: Special characters encountered; References may require: %nrbquote(%)UNQUOTE(&" vname+(-1)').';
            end; 
            %if %str(&handlename) ne %str() %then 
            %do; 
               value = '%syslput '||trim(vname)||'= '||trim(value)||" / remote = &handleName;"; 
            %end; 
            %else 
            %do; 
               value = '%let '||trim(vname)||'= '||trim(value)||';'; 
            %end; 
            
            put "NOTE: Setting macro variable " vname "with statement:" value ;
            call execute(value); 
         end; /* do i= */ 
         dsid = close(dsid); 
      end; /* dsid > 0 */ 
      else 
      do; 
         put "ERROR%QUOTE(:) Parameter table, &parameterTable., could not be"
              " opened.";
         abort; 
      end; 
      stop; 
   run; 
   
   %rcSet(&syserr); 
%mend etls_getParameters; 

%macro etls_loopW2GAVY2; 
   %local etls_filePrefix; 
   %let etls_filePrefix = ; 
   
   %macro etls_processToLoopW2GAVYC(parameterTable=, row=, handleName=rmt);
      %local etls_parmvars; 
      %etls_getParameterNames(parameterTable=&parameterTable, 
         parameterVariableMacro=etls_parmvars, 
         startingColumnNumber=1); 
      %local &etls_parmvars; 
      %etls_getParameters(parameterTable=&parameterTable, row=&row, 
         startingColumnNumber=1); 
      %let etls_previousFilePrefix = &etls_filePrefix;
      %local etls_filePrefix; 
      %let etls_filePrefix = &etls_previousFilePrefix.&handleName; 
      %macro etls_jobW2GAVYM; 
      
         /*==========================================================================* 
          * Step:            Inner Join                            A57SWEI7.BK000YZ9 * 
          * Transform:       Join                                                    * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Tables:   BASEL_MODEL - NZRRAP.BASEL_MODEL      A57SWEI7.BE00041K * 
          *                  BASEL_SEG - NZRRAP.BASEL_SEG          A57SWEI7.BE00041L * 
          * Target Table:    Join - work.SEGMENT_IDS               A57SWEI7.BN000NJ5 * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YZ9);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let etls_recnt = 0;
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(NZRRAP.BASEL_MODEL, DATA)) or 
                  %sysfunc(exist(NZRRAP.BASEL_MODEL, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               proc sql noprint;
                  select count(*) into :etls_recnt from NZRRAP.BASEL_MODEL;
               quit;
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = -1;
         option DBIDIRECTEXEC;
         
         proc datasets lib = work nolist nowarn memtype = (data view);
            delete SEGMENT_IDS;
         quit;
         
         proc sql;
            create table work.SEGMENT_IDS as
            select
               BASEL_MODEL.BASEL_MODEL_ID length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BASEL_MODEL_ID',
               BASEL_SEG.BASEL_SEG_ID length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BASEL_SEG_ID'
            from
               NZRRAP.BASEL_MODEL as BASEL_MODEL, 
               NZRRAP.BASEL_SEG as BASEL_SEG
            where
               BASEL_MODEL.BASEL_MODEL_ID = BASEL_SEG.BASEL_MODEL_ID
               and UPCASE(BASEL_MODEL.PD_F) = 'Y'
               and UPCASE(TRIM(BASEL_MODEL.SRC_SYS_CD)) = 'SPL'
               and (BASEL_SEG.SEG_END_DT = .
               or BASEL_SEG.SEG_END_DT >= &MTH_END_DT)
            order by
               BASEL_MODEL_ID,
               BASEL_SEG_ID
            ;
         quit;
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = &sys_sql_ip_all;
         
         %rcSet(&sqlrc); 
         
         %rcSet(&syscc); 
         
         
         
         /**  Step end Inner Join **/
         
         /*==========================================================================* 
          * Step:            Delete                                A57SWEI7.BK000YZA * 
          * Transform:       Delete                                                  * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Table:    Loop - work.W2KPZCVC                  A57SWEI7.BN000NJ4 * 
          * Target Table:    BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL -    A57SWEI7.BE000446 * 
          *                  NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG                     * 
          *                  _VAL                                                    * 
          *                                                                          * 
          * User Written:    Delete                                                  * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YZA);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let etls_recCheckExist = 0; 
         %let etls_recnt = 0; 
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(work.W2KPZCVC, DATA)) or 
                  %sysfunc(exist(work.W2KPZCVC, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               %local etls_syntaxcheck; 
               %let etls_syntaxcheck = %sysfunc(getoption(syntaxcheck)); 
               /* Turn off syntaxcheck option to perform following steps  */ 
               options nosyntaxcheck;
               
               proc contents data = work.W2KPZCVC out = work.etls_contents(keep = nobs) noprint; 
               run; 
               
               data _null_; 
                  set work.etls_contents (obs = 1); 
                  call symput("etls_recnt", left(put(nobs,32.))); 
               run;
               
               proc datasets lib = work nolist nowarn memtype = (data view);
                  delete etls_contents;
               quit;
               
               /* Reset syntaxcheck option to previous setting  */ 
               options &etls_syntaxcheck; 
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         %let SYSLAST = %nrquote(work.W2KPZCVC); 
         
         %let ETLS_SYSLAST = &SYSLAST;
         /*---- Start of Pre-Process Code  ----*/ 
         
         PROC SQL NOPRINT;
         SELECT DISTINCT BASEL_SEG_ID INTO :SEG_IDS SEPARATED BY ',' FROM SEGMENT_IDS;
         QUIT;
         
         %let MODULE=TL;
         %let BATCH=2.4;
         %etl_job_start(TARGET_TABLE = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL,PROCESS_MTH_TM_ID=RPTG_QTR_MTH_TM_ID,
         /*	CONDITION=%NRBQUOTE(BASEL_SEG_ID IN (SELECT DISTINCT BASEL_SEG_ID FROM NZRRAP.BASEL_SEG WHERE BASEL_SEG_NM LIKE %STR(%'%)LGD-D%STR(%%'))));*/
         CONDITION=%NRBQUOTE(BASEL_SEG_ID IN (&SEG_IDS)));
         /*---- End of Pre-Process Code  ----*/ 
         
         %rcSet(&syserr); 
         %rcSet(&sqlrc); 
         
         %let SYSLAST = &ETLS_SYSLAST;
         
         %let _INPUT_count = 1;
         %let _INPUT = work.W2KPZCVC;
         %let _INPUT_connect = ;
         %let _INPUT_engine = ;
         %let _INPUT_memtype = DATA;
         %let _INPUT_options = %nrquote();
         %let _INPUT_alter = %nrquote();
         %let _INPUT_path = %nrquote(/Loop_A57SWEI7.BN000NJ4%(WorkTable%));
         %let _INPUT_type = 1;
         %let _INPUT_label = %nrquote();
         
         %let _INPUT1 = work.W2KPZCVC;
         %let _INPUT1_connect = ;
         %let _INPUT1_engine = ;
         %let _INPUT1_memtype = DATA;
         %let _INPUT1_options = %nrquote();
         %let _INPUT1_alter = %nrquote();
         %let _INPUT1_path = %nrquote(/Loop_A57SWEI7.BN000NJ4%(WorkTable%));
         %let _INPUT1_type = 1;
         %let _INPUT1_label = %nrquote();
         
         %let _OUTPUT_count = 1;
         %let _OUTPUT = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_connect =  DATABASE=XXX SERVER=cs2iwntzp01 AUTHDOMAIN="NZ_Auth" 
         ;
         %let _OUTPUT_engine = NETEZZA;
         %let _OUTPUT_memtype = DATA;
         %let _OUTPUT_options = %nrquote();
         %let _OUTPUT_alter = %nrquote();
         %let _OUTPUT_path = %nrquote(/Shared Data/Sources/Netezza/Tables/NZRRAP/BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL%(Table%));
         %let _OUTPUT_type = 1;
         %let _OUTPUT_label = %nrquote();
         /* List of target columns to keep  */ 
         %let _OUTPUT_keep = BASEL_SEG_ID RPTG_QTR_MTH_TM_ID BASEL_MODEL_RLTNP_ID BASEL_MODEL_ID 
                 HISTRC_AVG_SEG_RTO HISTRC_AVG_FINAL_RPTG_RTO QTR_ST_MTH_TM_ID 
                 QTR_END_MTH_TM_ID INSRT_PROCESS_TMSTMP UPDT_PROCESS_TMSTMP;
         %let _OUTPUT_col_count = 10;
         %let _OUTPUT_col0_name = BASEL_SEG_ID;
         %let _OUTPUT_col0_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col0_length = 8;
         %let _OUTPUT_col0_type = ;
         %let _OUTPUT_col0_format = 11.;
         %let _OUTPUT_col0_informat = 11.;
         %let _OUTPUT_col0_label = %nrquote(BASEL_SEG_ID);
         %let _OUTPUT_col0_exp = ;
         %let _OUTPUT_col0_input_count = 0;
         %let _OUTPUT_col1_name = RPTG_QTR_MTH_TM_ID;
         %let _OUTPUT_col1_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col1_length = 8;
         %let _OUTPUT_col1_type = ;
         %let _OUTPUT_col1_format = 11.;
         %let _OUTPUT_col1_informat = 11.;
         %let _OUTPUT_col1_label = %nrquote(RPTG_QTR_MTH_TM_ID);
         %let _OUTPUT_col1_exp = ;
         %let _OUTPUT_col1_input_count = 0;
         %let _OUTPUT_col2_name = BASEL_MODEL_RLTNP_ID;
         %let _OUTPUT_col2_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col2_length = 8;
         %let _OUTPUT_col2_type = ;
         %let _OUTPUT_col2_format = 11.;
         %let _OUTPUT_col2_informat = 11.;
         %let _OUTPUT_col2_label = %nrquote(BASEL_MODEL_RLTNP_ID);
         %let _OUTPUT_col2_exp = ;
         %let _OUTPUT_col2_input_count = 0;
         %let _OUTPUT_col3_name = BASEL_MODEL_ID;
         %let _OUTPUT_col3_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col3_length = 8;
         %let _OUTPUT_col3_type = ;
         %let _OUTPUT_col3_format = 11.;
         %let _OUTPUT_col3_informat = 11.;
         %let _OUTPUT_col3_label = %nrquote(BASEL_MODEL_ID);
         %let _OUTPUT_col3_exp = ;
         %let _OUTPUT_col3_input_count = 0;
         %let _OUTPUT_col4_name = HISTRC_AVG_SEG_RTO;
         %let _OUTPUT_col4_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col4_length = 8;
         %let _OUTPUT_col4_type = ;
         %let _OUTPUT_col4_format = 30.8;
         %let _OUTPUT_col4_informat = 30.8;
         %let _OUTPUT_col4_label = %nrquote(HISTRC_AVG_SEG_RTO);
         %let _OUTPUT_col4_exp = ;
         %let _OUTPUT_col4_input_count = 0;
         %let _OUTPUT_col5_name = HISTRC_AVG_FINAL_RPTG_RTO;
         %let _OUTPUT_col5_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col5_length = 8;
         %let _OUTPUT_col5_type = ;
         %let _OUTPUT_col5_format = 30.8;
         %let _OUTPUT_col5_informat = 30.8;
         %let _OUTPUT_col5_label = %nrquote(HISTRC_AVG_FINAL_RPTG_RTO);
         %let _OUTPUT_col5_exp = ;
         %let _OUTPUT_col5_input_count = 0;
         %let _OUTPUT_col6_name = QTR_ST_MTH_TM_ID;
         %let _OUTPUT_col6_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col6_length = 8;
         %let _OUTPUT_col6_type = ;
         %let _OUTPUT_col6_format = 11.;
         %let _OUTPUT_col6_informat = 11.;
         %let _OUTPUT_col6_label = %nrquote(QTR_ST_MTH_TM_ID);
         %let _OUTPUT_col6_exp = ;
         %let _OUTPUT_col6_input_count = 0;
         %let _OUTPUT_col7_name = QTR_END_MTH_TM_ID;
         %let _OUTPUT_col7_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col7_length = 8;
         %let _OUTPUT_col7_type = ;
         %let _OUTPUT_col7_format = 11.;
         %let _OUTPUT_col7_informat = 11.;
         %let _OUTPUT_col7_label = %nrquote(QTR_END_MTH_TM_ID);
         %let _OUTPUT_col7_exp = ;
         %let _OUTPUT_col7_input_count = 0;
         %let _OUTPUT_col8_name = INSRT_PROCESS_TMSTMP;
         %let _OUTPUT_col8_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col8_length = 8;
         %let _OUTPUT_col8_type = ;
         %let _OUTPUT_col8_format = DATETIME25.6;
         %let _OUTPUT_col8_informat = DATETIME25.6;
         %let _OUTPUT_col8_label = %nrquote(INSRT_PROCESS_TMSTMP);
         %let _OUTPUT_col8_exp = ;
         %let _OUTPUT_col8_input_count = 0;
         %let _OUTPUT_col9_name = UPDT_PROCESS_TMSTMP;
         %let _OUTPUT_col9_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT_col9_length = 8;
         %let _OUTPUT_col9_type = ;
         %let _OUTPUT_col9_format = DATETIME25.6;
         %let _OUTPUT_col9_informat = DATETIME25.6;
         %let _OUTPUT_col9_label = %nrquote(UPDT_PROCESS_TMSTMP);
         %let _OUTPUT_col9_exp = ;
         %let _OUTPUT_col9_input_count = 0;
         
         %let _OUTPUT1 = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_connect =  DATABASE=XXX SERVER=cs2iwntzp01 AUTHDOMAIN="NZ_Auth" 
         ;
         %let _OUTPUT1_engine = NETEZZA;
         %let _OUTPUT1_memtype = DATA;
         %let _OUTPUT1_options = %nrquote();
         %let _OUTPUT1_alter = %nrquote();
         %let _OUTPUT1_path = %nrquote(/Shared Data/Sources/Netezza/Tables/NZRRAP/BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL%(Table%));
         %let _OUTPUT1_type = 1;
         %let _OUTPUT1_label = %nrquote();
         /* List of target columns to keep  */ 
         %let _OUTPUT1_keep = BASEL_SEG_ID RPTG_QTR_MTH_TM_ID BASEL_MODEL_RLTNP_ID BASEL_MODEL_ID 
                 HISTRC_AVG_SEG_RTO HISTRC_AVG_FINAL_RPTG_RTO QTR_ST_MTH_TM_ID 
                 QTR_END_MTH_TM_ID INSRT_PROCESS_TMSTMP UPDT_PROCESS_TMSTMP;
         %let _OUTPUT1_col_count = 10;
         %let _OUTPUT1_col0_name = BASEL_SEG_ID;
         %let _OUTPUT1_col0_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col0_length = 8;
         %let _OUTPUT1_col0_type = ;
         %let _OUTPUT1_col0_format = 11.;
         %let _OUTPUT1_col0_informat = 11.;
         %let _OUTPUT1_col0_label = %nrquote(BASEL_SEG_ID);
         %let _OUTPUT1_col0_exp = ;
         %let _OUTPUT1_col0_input_count = 0;
         %let _OUTPUT1_col1_name = RPTG_QTR_MTH_TM_ID;
         %let _OUTPUT1_col1_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col1_length = 8;
         %let _OUTPUT1_col1_type = ;
         %let _OUTPUT1_col1_format = 11.;
         %let _OUTPUT1_col1_informat = 11.;
         %let _OUTPUT1_col1_label = %nrquote(RPTG_QTR_MTH_TM_ID);
         %let _OUTPUT1_col1_exp = ;
         %let _OUTPUT1_col1_input_count = 0;
         %let _OUTPUT1_col2_name = BASEL_MODEL_RLTNP_ID;
         %let _OUTPUT1_col2_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col2_length = 8;
         %let _OUTPUT1_col2_type = ;
         %let _OUTPUT1_col2_format = 11.;
         %let _OUTPUT1_col2_informat = 11.;
         %let _OUTPUT1_col2_label = %nrquote(BASEL_MODEL_RLTNP_ID);
         %let _OUTPUT1_col2_exp = ;
         %let _OUTPUT1_col2_input_count = 0;
         %let _OUTPUT1_col3_name = BASEL_MODEL_ID;
         %let _OUTPUT1_col3_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col3_length = 8;
         %let _OUTPUT1_col3_type = ;
         %let _OUTPUT1_col3_format = 11.;
         %let _OUTPUT1_col3_informat = 11.;
         %let _OUTPUT1_col3_label = %nrquote(BASEL_MODEL_ID);
         %let _OUTPUT1_col3_exp = ;
         %let _OUTPUT1_col3_input_count = 0;
         %let _OUTPUT1_col4_name = HISTRC_AVG_SEG_RTO;
         %let _OUTPUT1_col4_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col4_length = 8;
         %let _OUTPUT1_col4_type = ;
         %let _OUTPUT1_col4_format = 30.8;
         %let _OUTPUT1_col4_informat = 30.8;
         %let _OUTPUT1_col4_label = %nrquote(HISTRC_AVG_SEG_RTO);
         %let _OUTPUT1_col4_exp = ;
         %let _OUTPUT1_col4_input_count = 0;
         %let _OUTPUT1_col5_name = HISTRC_AVG_FINAL_RPTG_RTO;
         %let _OUTPUT1_col5_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col5_length = 8;
         %let _OUTPUT1_col5_type = ;
         %let _OUTPUT1_col5_format = 30.8;
         %let _OUTPUT1_col5_informat = 30.8;
         %let _OUTPUT1_col5_label = %nrquote(HISTRC_AVG_FINAL_RPTG_RTO);
         %let _OUTPUT1_col5_exp = ;
         %let _OUTPUT1_col5_input_count = 0;
         %let _OUTPUT1_col6_name = QTR_ST_MTH_TM_ID;
         %let _OUTPUT1_col6_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col6_length = 8;
         %let _OUTPUT1_col6_type = ;
         %let _OUTPUT1_col6_format = 11.;
         %let _OUTPUT1_col6_informat = 11.;
         %let _OUTPUT1_col6_label = %nrquote(QTR_ST_MTH_TM_ID);
         %let _OUTPUT1_col6_exp = ;
         %let _OUTPUT1_col6_input_count = 0;
         %let _OUTPUT1_col7_name = QTR_END_MTH_TM_ID;
         %let _OUTPUT1_col7_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col7_length = 8;
         %let _OUTPUT1_col7_type = ;
         %let _OUTPUT1_col7_format = 11.;
         %let _OUTPUT1_col7_informat = 11.;
         %let _OUTPUT1_col7_label = %nrquote(QTR_END_MTH_TM_ID);
         %let _OUTPUT1_col7_exp = ;
         %let _OUTPUT1_col7_input_count = 0;
         %let _OUTPUT1_col8_name = INSRT_PROCESS_TMSTMP;
         %let _OUTPUT1_col8_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col8_length = 8;
         %let _OUTPUT1_col8_type = ;
         %let _OUTPUT1_col8_format = DATETIME25.6;
         %let _OUTPUT1_col8_informat = DATETIME25.6;
         %let _OUTPUT1_col8_label = %nrquote(INSRT_PROCESS_TMSTMP);
         %let _OUTPUT1_col8_exp = ;
         %let _OUTPUT1_col8_input_count = 0;
         %let _OUTPUT1_col9_name = UPDT_PROCESS_TMSTMP;
         %let _OUTPUT1_col9_table = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
         %let _OUTPUT1_col9_length = 8;
         %let _OUTPUT1_col9_type = ;
         %let _OUTPUT1_col9_format = DATETIME25.6;
         %let _OUTPUT1_col9_informat = DATETIME25.6;
         %let _OUTPUT1_col9_label = %nrquote(UPDT_PROCESS_TMSTMP);
         %let _OUTPUT1_col9_exp = ;
         %let _OUTPUT1_col9_input_count = 0;
         
         /*---- Start of User Written Code  ----*/ 
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = -1;
         option DBIDIRECTEXEC;
         
         %macro etls_sql_delete;
         
            /* Determine if the table exists  */ 
            %let etls_tableExist = %eval(%sysfunc(exist(NZRRAP."BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL"n, DATA)) or 
                  %sysfunc(exist(NZRRAP."BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL"n, VIEW))); 
            
            /*---- Create a new table  ----*/ 
            %if (&etls_tableExist eq 0) %then 
            %do;  /* if table does not exist  */ 
            
               %put %str(NOTE: Creating table ...);
               
               data NZRRAP."BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL"n
                       (dbnull = (
                                  "BASEL_SEG_ID"n = NO
                                  "RPTG_QTR_MTH_TM_ID"n = NO
                                  "BASEL_MODEL_RLTNP_ID"n = NO
                                  "BASEL_MODEL_ID"n = NO
                                  "HISTRC_AVG_SEG_RTO"n = YES
                                  "HISTRC_AVG_FINAL_RPTG_RTO"n = YES
                                  "QTR_ST_MTH_TM_ID"n = NO
                                  "QTR_END_MTH_TM_ID"n = NO
                                  "INSRT_PROCESS_TMSTMP"n = NO
                                  "UPDT_PROCESS_TMSTMP"n = YES));
                  attrib "BASEL_SEG_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_SEG_ID'; 
                  attrib "RPTG_QTR_MTH_TM_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'RPTG_QTR_MTH_TM_ID'; 
                  attrib "BASEL_MODEL_RLTNP_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_RLTNP_ID'; 
                  attrib "BASEL_MODEL_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_ID'; 
                  attrib "HISTRC_AVG_SEG_RTO"n length = 8
                     format = 30.8
                     informat = 30.8
                     label = 'HISTRC_AVG_SEG_RTO'; 
                  attrib "HISTRC_AVG_FINAL_RPTG_RTO"n length = 8
                     format = 30.8
                     informat = 30.8
                     label = 'HISTRC_AVG_FINAL_RPTG_RTO'; 
                  attrib "QTR_ST_MTH_TM_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'QTR_ST_MTH_TM_ID'; 
                  attrib "QTR_END_MTH_TM_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'QTR_END_MTH_TM_ID'; 
                  attrib "INSRT_PROCESS_TMSTMP"n length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'INSRT_PROCESS_TMSTMP'; 
                  attrib "UPDT_PROCESS_TMSTMP"n length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'UPDT_PROCESS_TMSTMP'; 
                  call missing(of _all_);
                  stop;
               run;
               
               %rcSet(&syserr); 
               
            %end;  /* if table does not exist  */ 
            
            /*---- Delete the target table  ----*/ 
            proc sql;
               delete from NZRRAP."BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL"n
               where
                  "BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL"n."RPTG_QTR_MTH_TM_ID"n = &DEL_MTH_TM_ID
                  and "BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL"n."BASEL_SEG_ID"n IN (SELECT DISTINCT BASEL_SEG_ID FROM SEGMENT_IDS)
            ;quit;
         
         %rcSet(&syscc); 
         
         %mend etls_sql_delete;
         
         /* execute etls_sql_delete  */ 
         %etls_sql_delete
         
         
         /*---- End of User Written Code  ----*/ 
         
         %rcSet(&syserr); 
         %rcSet(&sqlrc); 
         %rcSet(&syscc); 
         
         
         
         /**  Step end Delete **/
         
         /*==========================================================================* 
          * Step:            Left outer Join to fetch fiscal       A57SWEI7.BK000YZB * 
          *                   quarter dates                                          * 
          * Transform:       Join                                                    * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Tables:   Join - work.SEGMENT_IDS               A57SWEI7.BN000NJ5 * 
          *                  BASEL_SEG_HISTRC_AVG_LKP -            A57SWEI7.BE000447 * 
          *                   NZRRAP.BASEL_SEG_HISTRC_AVG_LKP                        * 
          * Target Table:    Join - work.W21KQEYM                  A57SWEI7.BN000NJ6 * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YZB);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let etls_recCheckExist = 0; 
         %let etls_recnt = 0; 
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(work.SEGMENT_IDS, DATA)) or 
                  %sysfunc(exist(work.SEGMENT_IDS, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               %local etls_syntaxcheck; 
               %let etls_syntaxcheck = %sysfunc(getoption(syntaxcheck)); 
               /* Turn off syntaxcheck option to perform following steps  */ 
               options nosyntaxcheck;
               
               proc contents data = work.SEGMENT_IDS out = work.etls_contents(keep = nobs) noprint; 
               run; 
               
               data _null_; 
                  set work.etls_contents (obs = 1); 
                  call symput("etls_recnt", left(put(nobs,32.))); 
               run;
               
               proc datasets lib = work nolist nowarn memtype = (data view);
                  delete etls_contents;
               quit;
               
               /* Reset syntaxcheck option to previous setting  */ 
               options &etls_syntaxcheck; 
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = -1;
         option DBIDIRECTEXEC;
         
         proc datasets lib = work nolist nowarn memtype = (data view);
            delete W21KQEYM;
         quit;
         
         proc sql;
            create table work.W21KQEYM as
            select
               SEGMENT_IDS.BASEL_MODEL_ID length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BASEL_MODEL_ID',
               SEGMENT_IDS.BASEL_SEG_ID length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BASEL_SEG_ID',
               BASEL_SEG_HISTRC_AVG_LKP.BASEL_MODEL_RLTNP_ID length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BASEL_MODEL_RLTNP_ID',
               CASE WHEN (BASEL_SEG_HISTRC_AVG_LKP.HISTRC_AVG_ST_DT IS NULL OR BASEL_SEG_HISTRC_AVG_LKP.HISTRC_AVG_ST_DT =.) 
               THEN "&EARLIEST_FISCAL_DT"d ELSE BASEL_SEG_HISTRC_AVG_LKP.HISTRC_AVG_ST_DT END as HISTRC_AVG_ST_DT length = 8   
                  format = DATE9.
                  informat = DATE9.
                  label = 'HISTRC_AVG_ST_DT',
               CASE 
               WHEN (BASEL_SEG_HISTRC_AVG_LKP.HISTRC_AVG_END_DT IS NULL OR BASEL_SEG_HISTRC_AVG_LKP.HISTRC_AVG_END_DT =.) THEN "&RECENT_FISCAL_DT"d
               WHEN BASEL_SEG_HISTRC_AVG_LKP.HISTRC_AVG_END_DT = '31DEC9999'd THEN &MTH_END_DT ELSE BASEL_SEG_HISTRC_AVG_LKP.HISTRC_AVG_END_DT END as HISTRC_AVG_END_DT length = 8   
                  format = DATE9.
                  informat = DATE9.
                  label = 'HISTRC_AVG_END_DT'
            from
               work.SEGMENT_IDS as SEGMENT_IDS left join 
               NZRRAP.BASEL_SEG_HISTRC_AVG_LKP as BASEL_SEG_HISTRC_AVG_LKP
                  on
                  (
                     SEGMENT_IDS.BASEL_MODEL_ID = BASEL_SEG_HISTRC_AVG_LKP.BASEL_MODEL_ID
                     and SEGMENT_IDS.BASEL_SEG_ID = BASEL_SEG_HISTRC_AVG_LKP.BASEL_SEG_ID
                  )
            where
               &MTH_END_DT >= BASEL_SEG_HISTRC_AVG_LKP.HISTRC_AVG_ST_DT
               and &MTH_END_DT <= BASEL_SEG_HISTRC_AVG_LKP.HISTRC_AVG_END_DT
            ;
         quit;
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = &sys_sql_ip_all;
         
         %rcSet(&sqlrc); 
         
         %rcSet(&syscc); 
         
         
         
         /**  Step end Left outer Join to fetch fiscal quarter dates **/
         
         /*==========================================================================* 
          * Step:            Lookup                                A57SWEI7.BK000YZC * 
          * Transform:       Lookup                                                  * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Tables:   Join - work.W21KQEYM                  A57SWEI7.BN000NJ6 * 
          *                  TM_DIM - NZRRAP.TM_DIM                A57SWEI7.BE000412 * 
          * Target Table:    Lookup - work.LOOP_LIST               A57SWEI7.BN000NJ7 * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YZC);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let etls_recCheckExist = 0; 
         %let etls_recnt = 0; 
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(work.W21KQEYM, DATA)) or 
                  %sysfunc(exist(work.W21KQEYM, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               %local etls_syntaxcheck; 
               %let etls_syntaxcheck = %sysfunc(getoption(syntaxcheck)); 
               /* Turn off syntaxcheck option to perform following steps  */ 
               options nosyntaxcheck;
               
               proc contents data = work.W21KQEYM out = work.etls_contents(keep = nobs) noprint; 
               run; 
               
               data _null_; 
                  set work.etls_contents (obs = 1); 
                  call symput("etls_recnt", left(put(nobs,32.))); 
               run;
               
               proc datasets lib = work nolist nowarn memtype = (data view);
                  delete etls_contents;
               quit;
               
               /* Reset syntaxcheck option to previous setting  */ 
               options &etls_syntaxcheck; 
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         %let runTime = %sysfunc(datetime()); 
         
         proc datasets lib = work nolist nowarn memtype = (data view);
            delete etls_temp_lookupview0;
         quit;
         
         proc sql; create view etls_temp_lookupview0 as 
            select * from NZRRAP.TM_DIM
            where TM_LVL ='Month';
         quit;
         /* Creating view to eliminate column name collisions for lookup table  */ 
         /*  NZRRAP.TM_DIM                                                      */ 
         proc datasets lib = work nolist nowarn memtype = (data view);
            delete etls_temp_lookupview1;
         quit;
         
         proc sql; create view etls_temp_lookupview1 as 
            select 
               TM_ID as ETLS_W2GAWDJ,
               TM_LVL_ST_DT
            from NZRRAP.TM_DIM
            where TM_LVL ='Month';
         quit;
         proc datasets lib = work nolist nowarn memtype = (data view);
            delete W2GAWDK;
         quit;
         
         /*---- Begin DATA step to perform lookups  ----*/ 
         DATA  work.W2GAWDK
                  (keep = MTH_TM_ID BASEL_MODEL_ID BASEL_SEG_ID BASEL_MODEL_RLTNP_ID 
                          FISCAL_START_TM_ID FISCAL_END_TM_ID PRIM_ID )
               ; 
            
            attrib MTH_TM_ID length=8 format=11. label="MTH_TM_ID"
                   BASEL_MODEL_ID length=8 format=11. label="BASEL_MODEL_ID"
                   BASEL_SEG_ID length=8 format=11. label="BASEL_SEG_ID"
                   BASEL_MODEL_RLTNP_ID length=8 format=11. label="BASEL_MODEL_RLTNP_ID"
                   FISCAL_START_TM_ID length=8 format=11. label="FISCAL_START_TM_ID"
                   FISCAL_END_TM_ID length=8 format=11. label="FISCAL_END_TM_ID"
                   PRIM_ID length=8 format=11. label="PRIM_ID"
                   TM_ID length= 8
                   TM_LVL_END_DT length= 8
                   ETLS_W2GAWDJ length= 8
                   TM_LVL_ST_DT length= 8
                   ; 
            
            retain missing0-missing1 0; 
            
            etls_assign_target_value = 0; 
            /* Build hash objects from lookup tables before reading first source row  */ 
            if (_n_ = 1) then 
            do; 
               /* Build hash h0 from lookup table etls_temp_lookupview0 */ 
               nlobs = .; 
               dsid = open("etls_temp_lookupview0"); 
               if (dsid > 0) then 
               do; 
                  if ( attrc(dsid, 'MTYPE') = 'DATA' ) then 
                     nlobs = attrn(dsid, 'NLOBS'); 
                  else 
                     nlobs = -1; 
                     
                  dsid = close(dsid); 
                  if (nlobs ^= 0) then 
                  do; 
                     if (nlobs > 0) then 
                        exponent = ceil(log2(nlobs)); 
                     else 
                        exponent = 8; 
                     declare hash h0(dataset: "etls_temp_lookupview0", hashexp: exponent);
                     h0.defineKey( "TM_LVL_END_DT");
                     h0.defineData( "TM_ID");
                     h0.defineDone();
                     if (nlobs = -1) then 
                     do; 
                        if (h0.Num_Items < 1) then
                        do; 
                           put "NOTE: Lookup table is empty: etls_temp_lookupview0";
                           put "NOTE: Abort action indicated, condition= Lookup table is empty:"
                                " etls_temp_lookupview0";
                           
                           abort 3; 
                        end; 
                     end; 
                  end; 
                  else 
                  do; 
                     put "NOTE: Lookup table is empty: etls_temp_lookupview0";
                     put "NOTE: Abort action indicated, condition= Lookup table is empty:"
                          " etls_temp_lookupview0";
                     
                     abort 3; 
                  end; 
               end; 
               else 
               do; 
                  put "NOTE: Lookup table does not exist or cannot be opened:"
                       " etls_temp_lookupview0";
                  
                  put "NOTE: Abort action indicated, condition= Lookup table missing:"
                       " etls_temp_lookupview0";
                  
                  abort 3; 
               end; 
               
               /* Build hash h1 from lookup table etls_temp_lookupview1 */ 
               nlobs = .; 
               dsid = open("etls_temp_lookupview1"); 
               if (dsid > 0) then 
               do; 
                  if ( attrc(dsid, 'MTYPE') = 'DATA' ) then 
                     nlobs = attrn(dsid, 'NLOBS'); 
                  else 
                     nlobs = -1; 
                     
                  dsid = close(dsid); 
                  if (nlobs ^= 0) then 
                  do; 
                     if (nlobs > 0) then 
                        exponent = ceil(log2(nlobs)); 
                     else 
                        exponent = 8; 
                     declare hash h1(dataset: "etls_temp_lookupview1", hashexp: exponent);
                     h1.defineKey( "TM_LVL_ST_DT");
                     h1.defineData( "ETLS_W2GAWDJ");
                     h1.defineDone();
                     if (nlobs = -1) then 
                     do; 
                        if (h1.Num_Items < 1) then
                        do; 
                           put "NOTE: Lookup table is empty: etls_temp_lookupview1";
                           put "NOTE: Abort action indicated, condition= Lookup table is empty:"
                                " etls_temp_lookupview1";
                           
                           abort 3; 
                        end; 
                     end; 
                  end; 
                  else 
                  do; 
                     put "NOTE: Lookup table is empty: etls_temp_lookupview1";
                     put "NOTE: Abort action indicated, condition= Lookup table is empty:"
                          " etls_temp_lookupview1";
                     
                     abort 3; 
                  end; 
               end; 
               else 
               do; 
                  put "NOTE: Lookup table does not exist or cannot be opened:"
                       " etls_temp_lookupview1";
                  
                  put "NOTE: Abort action indicated, condition= Lookup table missing:"
                       " etls_temp_lookupview1";
                  
                  abort 3; 
               end; 
               
               call missing (TM_LVL_END_DT, TM_ID, TM_LVL_ST_DT, ETLS_W2GAWDJ); 
            
            end; /* All hash objects have been defined */
            
            /* Read a row from the source table  */ 
            set work.W21KQEYM end = eof;
            
            /* Is the current key value stored in hash h0?  */ 
            rc0 = h0.find(key: HISTRC_AVG_ST_DT);
            FISCAL_START_TM_ID = TM_ID;
            /* Is the current key value stored in hash h1?  */ 
            rc1 = h1.find(key: HISTRC_AVG_END_DT);
            FISCAL_END_TM_ID = ETLS_W2GAWDJ;
            /* Examine success of lookups  */ 
            if ( rc0=0 and rc1=0 ) then 
            do; 
               /* Write row to target  */ 
               output work.W2GAWDK; 
            end; 
            else 
            do; 
               error_total + 1; 
               
               if (rc0 ^= 0) then 
               do; 
                  exception_total + 1; 
                  /* Check: Lookup value not found-Set target columns to missing  */ 
                  call missing (FISCAL_START_TM_ID); 
                  etls_assign_target_value = 1; 
               end; 
               
               if (rc1 ^= 0) then 
               do; 
                  exception_total + 1; 
                  /* Check: Lookup value not found-Set target columns to missing  */ 
                  call missing (FISCAL_END_TM_ID); 
                  etls_assign_target_value = 1; 
               end; 
               
               /* Set target columns to value/missing requested?  */ 
               if (etls_assign_target_value eq 1) then 
                  /* Write row to target  */ 
                  output work.W2GAWDK; 
            
            end; /* One or more lookups failed */
            
            if (eof = 1) then 
            do; 
               put "Source records with errors: " error_total ; 
               put "Total lookup exceptions: " exception_total ; 
            end; 
         
         run; 
         
         %rcSet(&syserr); 
         
         /*---- Map the columns  ----*/ 
         proc datasets lib = work nolist nowarn memtype = (data view);
            delete LOOP_LIST;
         quit;
         
         %put %str(NOTE: Mapping columns ...);
         proc sql;
            create table work.LOOP_LIST as
               select
                  (&MTH_TM_ID) as MTH_TM_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'MTH_TM_ID',
                  BASEL_MODEL_ID,
                  BASEL_SEG_ID,
                  BASEL_MODEL_RLTNP_ID,
                  FISCAL_START_TM_ID,
                  FISCAL_END_TM_ID,
                  (MONOTONIC()) as PRIM_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'PRIM_ID'
            from work.W2GAWDK
            ;
         quit;
         
         %let SYSLAST = work.LOOP_LIST;
         
         proc datasets lib = work nolist nowarn memtype = (data view);
            delete W2GAWDK;
         quit;
         
         %rcSet(&syscc); 
         
         
         
         /**  Step end Lookup **/
         
         /*---- Start of User Written Code  ----*/ 
         
         
         %MACRO PRELOAD;
         
         /*DELETE MONTHLY DATA FROM TARGET TABLE LOOP_LIST_RESULTS*/
         PROC SQL NOPRINT;
         DELETE FROM LOOP_LIST_RESULTS;
         QUIT;
         
         PROC SQL NOPRINT;
         /*PERFORM A COUNT OF VALID CRITERIAS FOR WHICH THE ITERATIVE LOOP WILL EXECUTE*/
         SELECT COUNT(*) INTO :CRITERIA_COUNT FROM LOOP_LIST;
         
         %DO i=1 %TO &CRITERIA_COUNT;
         %PUT PRIM_ID IS &i;
         
         /*FOR EVERY VALUE OF i, SELECT THE SUBQUERY INTO A SAS VARIABLE*/
         SELECT MTH_TM_ID, BASEL_MODEL_ID, BASEL_SEG_ID, BASEL_MODEL_RLTNP_ID, FISCAL_START_TM_ID, FISCAL_END_TM_ID INTO :MTH_TM_ID_X, :BASEL_MODEL_ID_X, :BASEL_SEG_ID_X, :BASEL_MODEL_RLTNP_ID_X, :FISCAL_START_X, :FISCAL_END_X 
         FROM LOOP_LIST WHERE PRIM_ID=&i;
         
         /*GET QUARTER ENDING TIMEKEYS BETWEEN THE FISCAL QUARTERS*/
         SELECT TM_ID INTO :INCLUSION_KEYS SEPARATED BY ',' FROM NZRRAP.TM_DIM WHERE TM_ID BETWEEN &FISCAL_START_X AND &FISCAL_END_X AND TM_YR_SEQ_NUM IN (1,4,7,10) AND TM_LVL='Month'/* AND TM_ID NOT IN (&EXCLUSION_KEYS)*/;
         
         INSERT INTO LOOP_LIST_RESULTS(BASEL_SEG_ID,RPTG_QTR_MTH_TM_ID,BASEL_MODEL_RLTNP_ID,BASEL_MODEL_ID,HISTRC_AVG_SEG_RTO,HISTRC_AVG_FINAL_RPTG_RTO,QTR_ST_MTH_TM_ID,QTR_END_MTH_TM_ID,INSRT_PROCESS_TMSTMP)
         SELECT BASEL_SEG_ID,&MTH_TM_ID_X,BASEL_MODEL_REL_ID,BASEL_MODEL_ID,MEAN(PD_SEGEMENT_RTO),MEAN(PD_FINAL_RPTG_RTO),&FISCAL_START_X,&FISCAL_END_X, &SESSIONTIME FROM NZRRAP.BASEL_PNL_LN_PD_SEG_MTH_RLZ_VAL 
         WHERE BASEL_SEG_ID=&BASEL_SEG_ID_X AND BASEL_MODEL_ID=&BASEL_MODEL_ID_X AND BASEL_MODEL_REL_ID=&BASEL_MODEL_RLTNP_ID_X
         AND MTH_TM_ID IN (&INCLUSION_KEYS)
         GROUP BY BASEL_SEG_ID,BASEL_MODEL_REL_ID,BASEL_MODEL_ID;
         
         %END;
         QUIT;
         %MEND;
         %PRELOAD;
         /*---- End of User Written Code  ----*/ 
         
         /*==========================================================================* 
          * Step:            Table Loader                          A57SWEI7.BK000YZE * 
          * Transform:       Table Loader (Version 2.1)                              * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Table:    User Written - work.LOOP_LIST_RESULTS A57SWEI7.BN000NJ8 * 
          * Target Table:    BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL -    A57SWEI7.BE000446 * 
          *                  NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG                     * 
          *                  _VAL                                                    * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YZE);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let SYSLAST = %nrquote(work.LOOP_LIST_RESULTS); 
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = -1;
         option DBIDIRECTEXEC;
         
         %global etls_tableExist;
         %global etls_numIndex;
         %global etls_lastTable;
         %let etls_tableExist = -1; 
         %let etls_numIndex = -1; 
         %let etls_lastTable = &SYSLAST; 
         
         /*---- Define load data macro  ----*/ 
         
         /* --------------------------------------------------------------
            Load Technique Selection: AppendToExisting - AppendProc
            Constraint and index action selections: 'ASIS','ASIS','ASIS','ASIS'
            Additional options selections... 
               Set unmapped to missing on updates: false 
            -------------------------------------------------------------- */
         %macro etls_loader;
         
            %let etls_tableOptions = ;
            
            /*---- Map the columns  ----*/ 
            proc datasets lib = work nolist nowarn memtype = (data view);
               delete W2GAWEA;
            quit;
            
            %put %str(NOTE: Mapping columns ...);
            proc sql;
               create view work.W2GAWEA as
                  select
                     BASEL_SEG_ID   
                        format = 11.
                        informat = 11.,
                     RPTG_QTR_MTH_TM_ID,
                     BASEL_MODEL_RLTNP_ID,
                     BASEL_MODEL_ID,
                     HISTRC_AVG_SEG_RTO,
                     HISTRC_AVG_FINAL_RPTG_RTO,
                     QTR_ST_MTH_TM_ID,
                     QTR_END_MTH_TM_ID,
                     (%SYSFUNC(DATETIME(),BEST18.)) as INSRT_PROCESS_TMSTMP length = 8
                        format = DATETIME25.6
                        informat = DATETIME25.6,
                     (%SYSFUNC(DATETIME(),BEST18.)) as UPDT_PROCESS_TMSTMP length = 8
                        format = DATETIME25.6
                        informat = DATETIME25.6
               from &etls_lastTable
               ;
            quit;
            
            %let SYSLAST = work.W2GAWEA;
            
            %let etls_lastTable = &SYSLAST; 
            %let etls_tableOptions = ; 
            
            /* Determine if the target table exists  */ 
            %let etls_tableExist = %eval(%sysfunc(exist(NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL, DATA)) or 
                  %sysfunc(exist(NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL, VIEW))); 
            
            /*---- Create a new table  ----*/ 
            %if (&etls_tableExist eq 0) %then 
            %do;  /* if table does not exist  */ 
            
               %put %str(NOTE: Creating table ...);
               
               data NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL
                       (dbnull = (
                                  BASEL_SEG_ID = NO
                                  RPTG_QTR_MTH_TM_ID = NO
                                  BASEL_MODEL_RLTNP_ID = NO
                                  BASEL_MODEL_ID = NO
                                  HISTRC_AVG_SEG_RTO = YES
                                  HISTRC_AVG_FINAL_RPTG_RTO = YES
                                  QTR_ST_MTH_TM_ID = NO
                                  QTR_END_MTH_TM_ID = NO
                                  INSRT_PROCESS_TMSTMP = NO
                                  UPDT_PROCESS_TMSTMP = YES));
                  attrib BASEL_SEG_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_SEG_ID'; 
                  attrib RPTG_QTR_MTH_TM_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'RPTG_QTR_MTH_TM_ID'; 
                  attrib BASEL_MODEL_RLTNP_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_RLTNP_ID'; 
                  attrib BASEL_MODEL_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_ID'; 
                  attrib HISTRC_AVG_SEG_RTO length = 8
                     format = 30.8
                     informat = 30.8
                     label = 'HISTRC_AVG_SEG_RTO'; 
                  attrib HISTRC_AVG_FINAL_RPTG_RTO length = 8
                     format = 30.8
                     informat = 30.8
                     label = 'HISTRC_AVG_FINAL_RPTG_RTO'; 
                  attrib QTR_ST_MTH_TM_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'QTR_ST_MTH_TM_ID'; 
                  attrib QTR_END_MTH_TM_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'QTR_END_MTH_TM_ID'; 
                  attrib INSRT_PROCESS_TMSTMP length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'INSRT_PROCESS_TMSTMP'; 
                  attrib UPDT_PROCESS_TMSTMP length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'UPDT_PROCESS_TMSTMP'; 
                  call missing(of _all_);
                  stop;
               run;
               
               %rcSet(&syserr); 
               
            %end;  /* if table does not exist  */ 
            
            /*---- Append  ----*/ 
            %put %str(NOTE: Appending data ...);
            
            proc append base = NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL (BULKLOAD=YES BL_METHOD=CLILOAD)
               data = &etls_lastTable (&etls_tableOptions)  force ; 
             run; 
            
            %rcSet(&syserr); 
            
            proc datasets lib = work nolist nowarn memtype = (data view);
               delete W2GAWEA;
            quit;
            
         %mend etls_loader;
         %etls_loader;
         
         %let etls_recnt = 0;
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL, DATA)) or 
                  %sysfunc(exist(NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               proc sql noprint;
                  select count(*) into :etls_recnt from NZRRAP.BASEL_PNL_LN_SEG_HIS_QTR_AVG_VAL;
               quit;
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         /*---- Start of Post-Process Code  ----*/ 
         
         %etl_job_end;
         /*---- End of Post-Process Code  ----*/ 
         
         %rcSet(&syserr); 
         %rcSet(&sqlrc); 
         
         %rcSet(&syscc); 
         
         
         
         /**  Step end Table Loader **/
         
      %mend etls_jobW2GAVYM; 
      
      %etls_jobW2GAVYM; 
      
      %let handleName = %etls_getHandle(statusTable=&etls_statusTable,
         handleVariable=etls_handleName, row=&&&etls_controlName);
         
      %let job_rc&handleName. = &job_rc; 
      
      %etls_freeHandle(statusTable=&etls_statusTable, statusVariable=etls_status, 
         handleVariable=etls_handleName, handleName="&handleName", 
         startTimeVariable=etls_startTime, endTimeVariable=etls_endTime, signoff=0, 
         returnCodeVariable=etls_jobRC, returnCodeMacroVariable=job_rc&handleName., setMainJobRc=1); 
         
   %mend etls_processToLoopW2GAVYC;
   
   %local etls_controlTable etls_statusTable etls_controlName 
      etls_processesRunning etls_maxProcesses etls_parameterTable 
      etls_additionalSignonOptions etls_signonRetries;
   %let etls_controlName = L21_; 
   %let etls_statusTable = work.W2KPZCVC; 
   %let etls_parameterTable = work.W2GAWEL; 
   %let &etls_controlName = 0;
   %let etls_controlTable = work.TIME_KEY;
   
   %put %str(NOTE: Creating status table...);
   data &etls_statusTable 
      (keep = etls_handleName etls_machineId etls_startTime etls_endTime etls_status 
              etls_jobRC TM_ID TM_YR_SEQ_NUM TM_LVL_END_DT
      );
      attrib etls_handleName length = $32
         label = 'Name of handle to remote session'; 
      attrib etls_machineId length = $32
         label = 'Name of machine executing the task'; 
      attrib etls_startTime length = 8
         format = nldatmap.
         label = 'Start time of task'; 
      attrib etls_endTime length = 8
         format = nldatmap.
         label = 'End time of task'; 
      attrib etls_status length = $32
         label = 'Current status of task'; 
      attrib etls_jobRC length = 8
         label = 'Return code of task'; 
      attrib TM_ID length = 8
         format = 11.
         informat = 11.
         label = 'TM_ID'; 
      attrib TM_YR_SEQ_NUM length = 8
         format = 11.
         informat = 11.
         label = 'TM_YR_SEQ_NUM'; 
      attrib TM_LVL_END_DT length = 8
         format = DATE9.
         informat = DATE9.
         label = 'TM_LVL_END_DT'; 
      set &etls_controlTable;
   run;
   
   %rcSet(&syserr); 
   
   %put %str(NOTE: Creating parameter table...);
   proc sql; 
      create table &etls_parameterTable as 
         select TM_ID as DEL_MTH_TM_ID, 
                TM_LVL_END_DT as MTH_END_DT, 
                TM_ID as MTH_TM_ID 
         from &etls_controlTable; 
   quit; 
   
   %rcSet(&sqlrc); 
   
   /* Get the number of times to iterate from the number of rows in the source  */ 
   /*  table                                                                    */ 
   proc sql noprint; 
      select count(*) into :&etls_controlName._max from &etls_statusTable;
      %let &etls_controlName._max = &&&etls_controlName._max;
   quit;
   
   %rcSet(&sqlrc); 
   
   %let etls_maxProcesses = 1; 
   
   %if (&etls_maxProcesses > 0) %then 
   %do; 
      %do %until (&&&etls_controlName ge &&&etls_controlName._max); 
      
         %let etls_lastLoopPtr = &&&etls_controlName;
         
         %let etls_processesRunning = 0; 
         
         %do %while(&etls_processesRunning lt &&&etls_controlName._max 
            and &etls_processesRunning lt &etls_maxProcesses 
            and &&&etls_controlName lt &&&etls_controlName._max);
            
            %let &etls_controlName = %eval(&&&etls_controlName+1);
            
            %let job_rcLast=&job_rc; 
            
            %etls_createHandle(statusTable=&etls_statusTable,
                               statusVariable=etls_status,
                               handleVariable=etls_handleName,
                               handlePrefix=&etls_controlName,
                               statusSetting="Running",
                               row=&&&etls_controlName,
                               machineVariable=etls_machineId,
                               startTimeVariable=etls_startTime,
                               signon=0,
                               useGrid=0);
            
         %let etls_processesRunning = 1; 
         %etls_processToLoopW2GAVYC(parameterTable=&etls_parameterTable, row=&&&etls_controlName,
            handleName=%etls_getHandle(statusTable=&etls_statusTable,
            handleVariable=etls_handleName, row=&&&etls_controlName)); 
         
         /* Reset main Job_RC and Trans_RC to max return code of all iterations */ 
         %let job_rcThisIter=&job_rc; 
         %let job_rc=&job_rcLast; 
         %let trans_rc=&job_rcLast; 
         %rcSet(&job_rcThisIter); 
      %end; 
      
   %end; 
   
%end;

proc datasets lib = work nolist nowarn memtype = (data view);
   delete W2GAWEL;
quit;

%mend etls_loopW2GAVY2; 

%etls_loopW2GAVY2; 

%rcSet(&syscc); 



/**  Step end Loop **/

/*==========================================================================* 
 * Step:            Loop End                              A57SWEI7.BK000YZF * 
 * Transform:       Loop End                                                * 
 * Description:                                                             * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YZF);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%rcSet(&syscc); 



/**  Step end Loop End **/

%exitetls_conditionW2J8VPXI:
%mend etls_conditionW2J8VPXI;

%etls_conditionW2J8VPXI;
%rcSet(&syscc); 



/**  Step end Conditional Start **/

%let etls_endTime = %sysfunc(datetime(),datetime.);

