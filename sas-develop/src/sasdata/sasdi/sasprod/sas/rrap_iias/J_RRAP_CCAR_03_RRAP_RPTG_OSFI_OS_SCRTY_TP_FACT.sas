/**************************************************************************** 
 * Job:             J_RRAP_CCAR_03_RRAP_RPTG_OSFI_OS_SCR  A57ZQ4A4.BF0011JZ * 
 *                  TY_TP_FACT                                              * 
 * Description:                                                             * 
 *                                                                          * 
 * Metadata Server: cs2iw503.bns                                            * 
 * Port:            8561                                                    * 
 * Location:        /Projects/RRAP/06 - DLGD/CCAR_ECAP                      * 
 *                                                                          * 
 * Server:          SASApp                                A57ZQ4A4.AT000002 * 
 *                                                                          * 
 * Generated on:    Thursday, July 20, 2017 4:27:14 PM EDT                  * 
 * Generated by:    s2809211                                                * 
 * Version:         SAS Data Integration Studio 4.8                         * 
 ****************************************************************************/ 

/* Generate the process id for job  */ 
%put Process ID: &SYSJOBID;

/* General macro variables  */ 
%let jobID = %quote(A57ZQ4A4.BF0011JZ);
%let etls_jobName = %nrquote(J_RRAP_CCAR_03_RRAP_RPTG_OSFI_OS_SCRTY_TP_FACT);
%let etls_userID = %nrquote(s2809211);

/* Setup to capture return codes  */ 
%global job_rc trans_rc sqlrc syscc;
%let sysrc = 0;
%let job_rc = 0;
%let trans_rc = 0;
%let sqlrc = 0;
%let syscc = 0;
%global etls_stepStartTime; 
/* initialize syserr to 0 */
data _null_; run;

%macro rcSet(error); 
   %if (&error gt &trans_rc) %then 
      %let trans_rc = &error;
   %if (&error gt &job_rc) %then 
      %let job_rc = &error;
%mend rcSet; 

%macro rcSetDS(error); 
   if &error gt input(symget('trans_rc'),12.) then 
      call symput('trans_rc',trim(left(put(&error,12.))));
   if &error gt input(symget('job_rc'),12.) then 
      call symput('job_rc',trim(left(put(&error,12.))));
%mend rcSetDS; 

/* Create metadata macro variables */
%let IOMServer      = %nrquote(SASApp);
%let metaPort       = %nrquote(8561);
%let metaServer     = %nrquote(cs2iw503);

/* Set metadata options */
options metaport       = &metaPort 
        metaserver     = "&metaServer"; 

/* Setup for capturing job status  */ 
%let etls_startTime = %sysfunc(datetime(),datetime.);
%let etls_recordsBefore = 0;
%let etls_recordsAfter = 0;
%let etls_lib = 0;
%let etls_table = 0;

%global etls_debug; 
%macro etls_setDebug; 
   %if %str(&etls_debug) ne 0 %then 
      OPTIONS MPRINT%str(;); 
%mend; 
%etls_setDebug; 

/*==========================================================================* 
 * Step:            CCAR_ECAP_AUTOEXEC                    A57ZQ4A4.BO0016DM * 
 * Transform:       CCAR_ECAP_AUTOEXEC                                      * 
 * Description:                                                             * 
 *==========================================================================*/ 

%let transformID = %quote(A57ZQ4A4.BO0016DM);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0; 
%let _OUTPUT_count = 0; 


%include '/sasdata/sasdi/sasdev/macro/rrap/rrap_autoexec.sas';
%rrap_autoexec(RRAPENV=REVOLVING_CREDIT);


%rcSet(&syserr); 
%rcSet(&sysrc); 
%rcSet(&sqlrc); 

%rcSet(&syscc); 



/**  Step end CCAR_ECAP_AUTOEXEC **/

/*---- Start of User Written Code  ----*/ 


/**************************************************************************** 
 * Job:             J_rrap_stg_osfi_outstanding_security  A57ZQ4A4.BF0000SA * 
 *                  _type_fact                                              * 
 * Description:                                                             * 
 *                                                                          * 
 * Metadata Server: cs2iw503.bns                                            * 
 * Port:            8561                                                    * 
 * Location:        /Projects/RRAP/Staging/Jobs                             * 
 *                                                                          * 
 * Server:          SASApp                                A57ZQ4A4.AT000002 * 
 *                                                                          * 
 * Generated on:    Sunday, September 14, 2014 1:19:22 PM EDT               * 
 * Generated by:    s1928220                                                * 
 * Version:         SAS Data Integration Studio 4.8                         * 
 ****************************************************************************/ 
%rrap_autoexec(RRAPENV=REVOLVING_CREDIT);
/*---- Start of User Written Code  ----*/ 
;
/*%INCLUDE  "/sasdata/sasdev/scripts/rrap/sit/scripts/Pgm_Master_Include_Initialisation_v01.sas";*/

%let datetime_start = %sysfunc(TIME()) ;
%put >>> START TIME: %sysfunc(datetime(),datetime14.);

/*DECLARE THE PATH TO DEFINE AUTOCALL MACRO*/
/*OPTIONS MAUTOSOURCE SASAUTOS='/sasdata/sasdev/macro/rrap';*/

/*INITIALIZATION OF VARIABLES AND EXECUTE AUTOCALL MACRO*/
/*%srrap_autoexec('BASELDB2');*/


libname &LIB. "&DPATH.";;
libname &DB. db2 database=&DB. schema = EDRRAPT authdomain=db2_auth dbsliceparm=(all,10);

%PUT >> Processing_Month_Time_ID = &Processing_Month_Time_ID.;


libname &DB. db2     database=&db.      schema=EDRRAPT authdomain=db2_auth;
libname EDRTLRT db2     database=&db.      schema=EDRTLRT authdomain=db2_auth;


proc sql;
DELETE FROM &DB..BASEL_RPTG_OSFI_OS_SCRTY_TP_FACT
WHERE MTH_TM_ID = &MTH_TM_ID.;
quit;

%global mth_tm_id;
%global tm_lvl_st_dt;
%global tm_lvl_end_dt;
%global dtime;

proc datasets lib=work kill NOLIST;
run;quit;

proc sql noprint;
select tm_id as mth_tm_id, tm_lvl_st_dt, tm_lvl_end_dt , datetime() as dtime 
format datetime25.
into :mth_tm_id, :tm_lvl_st_dt, :tm_lvl_end_dt, :dtime
from  EDRTLRT.TM_DIM
/*where tm_id=(select max(mth_tm_id) from edrrapt.BASEL_NCR_BUS_AGGRTD_FACT); 
KEEP FOR PRODUCTION*/
where tm_id= &MTH_TM_ID.; * FOR TEST ONLY End of April test date 
;
quit;
/*** RRAP_STG_SUM_REPORTING_OSFI_OUTSTANDING_SECURITY_TYPE_FACT_BSTM****/
PROC SQL THREADS;
create table WORK.REPORT_OSFI_OS_SEC_TP_FACT_BSTM as
select 
MTH_TM_ID AS MONTH_TIME_ID
,SRC_SYS_CD LENGTH=50 AS SOURCE_SYSTEM_CODE
,SCRTY_TP_DESC LENGTH=50 AS SECURITY_TYPE_DESCRIPTION
,BASEL_PRD_ABR LENGTH=50 AS BASEL_PRODUCT_ABBREVIATION
,SUM(OS_BAL_AMT /*+ UNADJUSTED_ADD_ON_BAL_AMT*/) FORMAT=17.3 
 AS OUTSTANDING_BALANCE_AMOUNT
FROM &LIB..BASEL_ANALYTCL_BL_INSTRMNT_FACT
WHERE
CONSM_PRD_TREATMNT_CD = 'A' AND
SML_BUS_F = 'N' AND
PIT_STAT_CD IN ('CUR', 'DEF') AND
TRNST_EXCLSN_F = 'N' AND
MTH_TM_ID = &Processing_Month_Time_ID AND
SRC_SYS_CD in ('KS','SPL')
GROUP BY
MTH_TM_ID
,SRC_SYS_CD
,SCRTY_TP_DESC
,BASEL_PRD_ABR
;
QUIT;

PROC SQL THREADS;
DELETE FROM &DB..BASEL_RPTG_OSFI_OS_SCRTY_TP_FACT
WHERE MTH_TM_ID = &MTH_TM_ID.
;
INSERT INTO &DB..BASEL_RPTG_OSFI_OS_SCRTY_TP_FACT 
SELECT *,datetime() format=datetime25.,datetime() format=datetime25.
FROM REPORT_OSFI_OS_SEC_TP_FACT_BSTM
;
quit;
/*---- End of User Written Code  ----*/ 

%put >>> END TIME: %sysfunc(datetime(),datetime14.);
%put >>> Benchmarking PROCESSING TIME:  %sysfunc(putn(%sysevalf(%sysfunc(TIME())-&datetime_start.),mmss.)) (mm:ss) ;


/*---- End of User Written Code  ----*/ 

%let etls_endTime = %sysfunc(datetime(),datetime.);

