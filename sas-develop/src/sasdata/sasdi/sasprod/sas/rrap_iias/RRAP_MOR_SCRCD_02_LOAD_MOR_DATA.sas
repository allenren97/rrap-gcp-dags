/**************************************************************************** 
 * Job:             RRAP_MOR_SCRCD_02_LOAD_MOR_DATA       A57SWEI7.BH0009KH * 
 * Description:                                                             * 
 *                                                                          * 
 * Metadata Server: 10.56.70.48                                             * 
 * Port:            8561                                                    * 
 * Location:        /Projects/RRAP/Jobs/04 - Mortgage/BNS                   * 
 *                                                                          * 
 * Server:          SASApp                                A57SWEI7.AT000002 * 
 *                                                                          * 
 * Generated on:    Thursday, June 29, 2017 2:43:26 PM EDT                  * 
 * Generated by:    owprdsas                                                * 
 * Version:         SAS Data Integration Studio 4.8                         * 
 ****************************************************************************/ 

/* Generate the process id for job  */ 
%put Process ID: &SYSJOBID;

/* General macro variables  */ 
%let jobID = %quote(A57SWEI7.BH0009KH);
%let etls_jobName = %nrquote(RRAP_MOR_SCRCD_02_LOAD_MOR_DATA);
%let etls_userID = %nrquote(owprdsas);

/* Setup to capture return codes  */ 
%global job_rc trans_rc sqlrc syscc;
%let sysrc = 0;
%let job_rc = 0;
%let trans_rc = 0;
%let sqlrc = 0;
%let syscc = 0;
%global etls_stepStartTime; 
/* initialize syserr to 0 */
data _null_; run;

%macro rcSet(error); 
   %if (&error gt &trans_rc) %then 
      %let trans_rc = &error;
   %if (&error gt &job_rc) %then 
      %let job_rc = &error;
%mend rcSet; 

%macro rcSetDS(error); 
   if &error gt input(symget('trans_rc'),12.) then 
      call symput('trans_rc',trim(left(put(&error,12.))));
   if &error gt input(symget('job_rc'),12.) then 
      call symput('job_rc',trim(left(put(&error,12.))));
%mend rcSetDS; 

/* Create metadata macro variables */
%let IOMServer      = %nrquote(SASApp);
%let metaPort       = %nrquote(8561);
%let metaServer     = %nrquote(iw503);

/* Set metadata options */
options metaport       = &metaPort 
        metaserver     = "&metaServer"; 

/* Setup for capturing job status  */ 
%let etls_startTime = %sysfunc(datetime(),datetime.);
%let etls_recordsBefore = 0;
%let etls_recordsAfter = 0;
%let etls_lib = 0;
%let etls_table = 0;

%global etls_debug; 
%macro etls_setDebug; 
   %if %str(&etls_debug) ne 0 %then 
      OPTIONS MPRINT%str(;); 
%mend; 
%etls_setDebug; 

/*==========================================================================* 
 * Step:            AM_rrap_mortgage_autoexec             A57SWEI7.BK000Z7M * 
 * Transform:       AM_rrap_mor_bns_autoexec                                * 
 * Description:     BNS Mortgage jobs imported from IW503 leverage this     * 
 *                   transformation                                         * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000Z7M);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0; 
%let _OUTPUT_count = 0; 


%put WORK LOCATION: %sysfunc(getoption(work));
%rrap_mor_bns_autoexec;

%rcSet(&syserr); 
%rcSet(&sysrc); 
%rcSet(&sqlrc); 

%rcSet(&syscc); 



/**  Step end AM_rrap_mortgage_autoexec **/

/*==========================================================================* 
 * Step:            STEP02_LOAD MOR TABLE TO NETEZZA NOT  A57SWEI7.BK000Z7N * 
 *                   2009                                                   * 
 * Transform:       User Written                                            * 
 * Description:                                                             * 
 *                                                                          * 
 * Target Table:    User Written - work.WPGMTL            A57SWEI7.BN000NQZ * 
 *                                                                          * 
 * User Written:    SourceCode                                              * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000Z7N);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0;
%let _OUTPUT_count = 1;
%let _OUTPUT = work.WPGMTL;
%let _OUTPUT_connect = ;
%let _OUTPUT_engine = ;
%let _OUTPUT_memtype = DATA;
%let _OUTPUT_options = %nrquote();
%let _OUTPUT_alter = %nrquote();
%let _OUTPUT_path = %nrquote(/User Written_A57SWEI7.BN000NQZ%(WorkTable%));
%let _OUTPUT_type = 1;
%let _OUTPUT_label = %nrquote();
%let _OUTPUT_col_count = 0;

%let _OUTPUT1 = work.WPGMTL;
%let _OUTPUT1_connect = ;
%let _OUTPUT1_engine = ;
%let _OUTPUT1_memtype = DATA;
%let _OUTPUT1_options = %nrquote();
%let _OUTPUT1_alter = %nrquote();
%let _OUTPUT1_path = %nrquote(/User Written_A57SWEI7.BN000NQZ%(WorkTable%));
%let _OUTPUT1_type = 1;
%let _OUTPUT1_label = %nrquote();
%let _OUTPUT1_col_count = 0;

/*---- Start of User Written Code  ----*/ 

*===============================================================================================================;
*	Step 2  Write data set to target schema schema;
*	In case of rerun - delete the dataset;

* Input is now pointing to Netezza.  Table MOR_SAMPLE_DEV_SOURCE was manually copied from the MOR library on the 9.3 unix box;

/*
%delete_table(&target_lib..&ds.);

data &target_lib..&ds.(distribute_on='mortgage_no,cis_tm_id' bulkload=yes);
	*set &source_lib..&ds.(keep=&acct_key. &time_key. &time_keyc. where=(&time_keyc. not in ('200907' '200904' '200910' '201001' '201004' '201007')));
   *set &target_lib..MORTGAGE_HIST(keep=&acct_key. &time_key. &time_keyc. where=(&time_keyc. not in ('200907' '200904' '200910' '201001' '201004' '201007')));  
   set &target_lib..&ds._source(keep=&acct_key. &time_key. &time_keyc. where=(&time_keyc. not in ('200907' '200904' '200910' '201001' '201004' '201007')));  
	cis_tm_id=compress(substr(&time_keyc.,3,2)||substr(&time_keyc.,5,2));
*time_key=intnx('month',mdy(substr(&time_keyc.,5,2),1,substr(&time_keyc.,1,4)),1)-1;
run;
*/

/*
%delete_table(&target_lib..MORTGAGE_HIST3);
*/
proc sql;
	connect USING NZUSER as nzcon ;

	*In case if rerun - drop the existing table;

	drop table &target_lib..SCORECARD_VARS_MOR;
	execute ( create table &target_lib3..SCORECARD_VARS_MOR as (
             SELECT MORTGAGE_NO,    
             last_day(PROCESS_DATE) as TIME_KEY,
             to_char(PROCESS_DATE,'YYYYMM') as YYMTH, 
             to_char(PROCESS_DATE,'YYMM') as CIS_TM_ID
             FROM &target_lib3..MORTGAGE_HIST
             where YYMTH not in ('200907','200904','200910','201001','201004','201007') 
                   /* 13Mar - JD - Commmenting out.  Not needed due to filter when building mortgage_hist
                   and date_trunc('year', TIME_KEY ) >= '2010-01-01 00:00:00'
                   */
			) WITH DATA DISTRIBUTE BY (mortgage_no,cis_tm_id)	) by nzcon;

   /*

execute( grant select, insert, update, delete on &target_lib3...SCORECARD_VARS_MOR TO frg_user) by nzcon; */

	*execute (generate statistics on &target_lib3..SCORECARD_VARS_MOR ) by nzcon;
	execute(CALL SYSPROC.ADMIN_CMD (%nrbquote('RUNSTATS 
						ON TABLE ) %nrbquote(&target_lib3..SCORECARD_VARS_MOR) 
							%nrbquote(ON KEY COLUMNS and INDEXES ALL'))) by nzcon;

   disconnect from nzcon;
quit;
*/

/*---- End of User Written Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 
%rcSet(&syscc); 



/**  Step end STEP02_LOAD MOR TABLE TO NETEZZA NOT 2009 **/

%let etls_endTime = %sysfunc(datetime(),datetime.);

