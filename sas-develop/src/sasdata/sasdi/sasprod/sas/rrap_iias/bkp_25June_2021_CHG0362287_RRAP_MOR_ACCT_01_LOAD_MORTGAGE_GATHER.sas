/**************************************************************************** 
 * Job:             RRAP_MOR_ACCT_01_LOAD_MORTGAGE_GATHER A57SWEI7.BH0009JV * 
 * Description:                                                             * 
 *                                                                          * 
 * Metadata Server: 10.56.70.48                                             * 
 * Port:            8561                                                    * 
 * Location:        /Projects/RRAP/Jobs/04 - Mortgage/BNS                   * 
 *                                                                          * 
 * Server:          SASApp                                A57SWEI7.AT000002 * 
 *                                                                          * 
 * Generated on:    Thursday, June 29, 2017 2:43:40 PM EDT                  * 
 * Generated by:    owprdsas                                                * 
 * Version:         SAS Data Integration Studio 4.8                         * 
 ****************************************************************************/ 

/* Generate the process id for job  */ 
%put Process ID: &SYSJOBID;

/* General macro variables  */ 
%let jobID = %quote(A57SWEI7.BH0009JV);
%let etls_jobName = %nrquote(RRAP_MOR_ACCT_01_LOAD_MORTGAGE_GATHER);
%let etls_userID = %nrquote(owprdsas);

/* Setup to capture return codes  */ 
%global job_rc trans_rc sqlrc syscc;
%let sysrc = 0;
%let job_rc = 0;
%let trans_rc = 0;
%let sqlrc = 0;
%let syscc = 0;
%global etls_stepStartTime; 
/* initialize syserr to 0 */
data _null_; run;

%macro rcSet(error); 
   %if (&error gt &trans_rc) %then 
      %let trans_rc = &error;
   %if (&error gt &job_rc) %then 
      %let job_rc = &error;
%mend rcSet; 

%macro rcSetDS(error); 
   if &error gt input(symget('trans_rc'),12.) then 
      call symput('trans_rc',trim(left(put(&error,12.))));
   if &error gt input(symget('job_rc'),12.) then 
      call symput('job_rc',trim(left(put(&error,12.))));
%mend rcSetDS; 

/* Create metadata macro variables */
%let IOMServer      = %nrquote(SASApp);
%let metaPort       = %nrquote(8561);
%let metaServer     = %nrquote(iw503);

/* Set metadata options */
options metaport       = &metaPort 
        metaserver     = "&metaServer"; 

/* Setup for capturing job status  */ 
%let etls_startTime = %sysfunc(datetime(),datetime.);
%let etls_recordsBefore = 0;
%let etls_recordsAfter = 0;
%let etls_lib = 0;
%let etls_table = 0;

%global etls_debug; 
%macro etls_setDebug; 
   %if %str(&etls_debug) ne 0 %then 
      OPTIONS MPRINT%str(;); 
%mend; 
%etls_setDebug; 

/*==========================================================================* 
 * Step:            AM_rrap_mortgage_autoexec             A57SWEI7.BK000Z50 * 
 * Transform:       AM_rrap_mor_bns_autoexec                                * 
 * Description:     BNS Mortgage jobs imported from IW503 leverage this     * 
 *                   transformation                                         * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000Z50);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0; 
%let _OUTPUT_count = 0; 


%put WORK LOCATION: %sysfunc(getoption(work));
%rrap_mor_bns_autoexec;

%rcSet(&syserr); 
%rcSet(&sysrc); 
%rcSet(&sqlrc); 

%rcSet(&syscc); 



/**  Step end AM_rrap_mortgage_autoexec **/

/*==========================================================================* 
 * Step:            Gather Mortgage_HIST                  A57SWEI7.BK000Z51 * 
 * Transform:       User Written                                            * 
 * Description:                                                             * 
 *                                                                          * 
 * Target Table:    User Written - work.WOZUGG            A57SWEI7.BN000NQF * 
 *                                                                          * 
 * User Written:    SourceCode                                              * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000Z51);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0;
%let _OUTPUT_count = 1;
%let _OUTPUT = work.WOZUGG;
%let _OUTPUT_connect = ;
%let _OUTPUT_engine = ;
%let _OUTPUT_memtype = DATA;
%let _OUTPUT_options = %nrquote();
%let _OUTPUT_alter = %nrquote();
%let _OUTPUT_path = %nrquote(/User Written_A57SWEI7.BN000NQF%(WorkTable%));
%let _OUTPUT_type = 1;
%let _OUTPUT_label = %nrquote();
%let _OUTPUT_col_count = 0;

%let _OUTPUT1 = work.WOZUGG;
%let _OUTPUT1_connect = ;
%let _OUTPUT1_engine = ;
%let _OUTPUT1_memtype = DATA;
%let _OUTPUT1_options = %nrquote();
%let _OUTPUT1_alter = %nrquote();
%let _OUTPUT1_path = %nrquote(/User Written_A57SWEI7.BN000NQF%(WorkTable%));
%let _OUTPUT1_type = 1;
%let _OUTPUT1_label = %nrquote();
%let _OUTPUT1_col_count = 0;

/*---- Start of User Written Code  ----*/ 


%delete_table(nzuser.MORTGAGE_HIST); 


proc sql ;  
    connect USING NZUSER as nzcon ;
   /* execute( create table MORTGAGE_HIST as */
	  execute( create table &target_lib3..MORTGAGE_HIST as (
             select *
             from &target_lib3..MORTGAGE_HIST_0
            /* distribute on hash (MORTGAGE_NO) */
			) WITH DATA DISTRIBUTE BY HASH (MORTGAGE_NO) 
           ) by nzcon;
    disconnect from nzcon;
  quit;

  %put &=syserr;
%put &=sqlrc;

proc sql ;   
    connect USING NZUSER as nzcon ;
   /* execute( insert into MORTGAGE_HIST */
      execute( insert into &target_lib3..MORTGAGE_HIST
             select *
             from &target_lib3..MORTGAGE_HIST_1
           ) by nzcon;
    disconnect from nzcon;
  quit;

  %put &=syserr;
%put &=sqlrc;

proc sql;  
    connect USING NZUSER as nzcon ;
  /*  execute( insert into MORTGAGE_HIST */
	  execute( insert into &target_lib3..MORTGAGE_HIST
             select *
             from &target_lib3..MORTGAGE_HIST_2
           ) by nzcon;
    disconnect from nzcon;
  quit;

  %put &=syserr;
%put &=sqlrc;

proc sql;  
    connect USING NZUSER as nzcon ;
   /* execute( insert into MORTGAGE_HIST */
	execute( insert into &target_lib3..MORTGAGE_HIST
             select *
             from &target_lib3..MORTGAGE_HIST_3
           ) by nzcon;
    disconnect from nzcon;
  quit;

  %put &=syserr;
%put &=sqlrc;

proc sql;  
    connect USING NZUSER as nzcon ;
  /*  execute( insert into MORTGAGE_HISTc*/
	execute( insert into &target_lib3..MORTGAGE_HIST
             select *
             from &target_lib3..MORTGAGE_HIST_4
           ) by nzcon;
    disconnect from nzcon;
  quit;

  %put &=syserr;
%put &=sqlrc;

proc sql;  
    connect USING NZUSER as nzcon ;
  /*  execute( insert into MORTGAGE_HIST */
	execute( insert into &target_lib3..MORTGAGE_HIST
             select *
             from &target_lib3..MORTGAGE_HIST_5
           ) by nzcon;
    disconnect from nzcon;
  quit;

  %put &=syserr;
%put &=sqlrc;

proc sql;  
    connect USING NZUSER as nzcon ;
  /*  execute( insert into MORTGAGE_HIST */
	execute( insert into &target_lib3..MORTGAGE_HIST
             select *
             from &target_lib3..MORTGAGE_HIST_6
           ) by nzcon;
    disconnect from nzcon;
  quit;

  %put &=syserr;
%put &=sqlrc;

proc sql;  
    connect USING NZUSER as nzcon ;
   /* execute( insert into MORTGAGE_HIST */
	execute( insert into &target_lib3..MORTGAGE_HIST
             select *
             from &target_lib3..MORTGAGE_HIST_7
           ) by nzcon;
    disconnect from nzcon;
  quit;

  

/* %macro checkds(dsn);
   %if %sysfunc(exist(&dsn)) %then %do;
		proc sql;
    		select count(*) INTO: manul_error_check from &dsn;
		QUIT;

		data _null_;
   if strip(&manul_error_check) = '0' then do;
      abort abend 255;
   end;
   else do;
      put "Job Completed successfully";
   end;
run;
   %end;
   %else %do;
		%abort abend 255;
   %end;
%mend checkds;

%checkds(NZUSER.MORTGAGE_HIST) */


%put &=syserr;
%put &=sqlrc;


proc sql;  
    connect USING NZUSER as nzcon ;
     *execute (generate statistics on &target_lib3..MORTGAGE_HIST) by nzcon;
     execute(CALL SYSPROC.ADMIN_CMD (%nrbquote('RUNSTATS 
						ON TABLE ) %nrbquote(&target_lib3..MORTGAGE_HIST) 
							%nrbquote(ON KEY COLUMNS and INDEXES ALL'))) by nzcon;
    disconnect from nzcon;
  quit;

  %put &=syserr;
%put &=sqlrc;

 
/*
%macro append_partitions(pn=);

%if %sysfunc(exist(&target_lib..MORTGAGE_HIST)) %then %do;
  proc sql;  
    connect to netezza as nzcon (server=cs2iwntzp01 database=&target_lib3  user=&nzuser. password="&nzpassword.");
    execute( insert into MORTGAGE_HIST
             select *
             from MORTGAGE_HIST_&pn
           ) by nzcon;
    disconnect from nzcon;
  quit;
 %end;
 %else %do;
  proc sql;  
    connect to netezza as nzcon (server=cs2iwntzp01 database=&target_lib3  user=&nzuser. password="&nzpassword.");
    execute( create table MORTGAGE_HIST as
             select *
             from MORTGAGE_HIST_&pn
           ) by nzcon;
    disconnect from nzcon;
  quit;
 %end;

%mend;

%macro gather_partitions;

%delete_table(nzuser.MORTGAGE_HIST); 

data _null_;
  *set years_to_process end=eof;
  set &_input end=eof;
  call symput('part'||left(_n_), pn);
  if eof then call symput('rows', _n_);
run;

%do i=0 %to &7;
  %append_partitions(pn=i);
%end;
%mend;
*/
/*---- End of User Written Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 
%rcSet(&syscc); 



/**  Step end Gather Mortgage_HIST **/

%let etls_endTime = %sysfunc(datetime(),datetime.);

