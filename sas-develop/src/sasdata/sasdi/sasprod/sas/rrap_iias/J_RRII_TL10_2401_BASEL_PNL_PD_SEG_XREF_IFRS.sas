/**************************************************************************** 
 * Job:             J_RRII_TL10_2401_BASEL_PNL_PD_SEG_XREF_IFRS             * 
 *                                                              	    * 
 * Description:                                                             * 
 *                                                                          * 
 * Metadata Server: 10.56.70.48                                             * 
 * Port:            8561                                                    * 
 * Location:        /Projects/RRAP/Jobs/02 - Term Loans/2.4                 * 
 *                                                                          * 
 * Server:          SASApp                                 		    * 
 *                                                                          * 
 * Source Tables:   BASEL_MODEL - NZRRAP.BASEL_MODEL                        * 
 *                  BASEL_SEG_IFRS - NZRRAP.BASEL_SEG                       * 
 *                  TM_DIM - NZRRAP.TM_DIM                                  * 
 * Target Tables:   BASEL_PNL_PD_SEG_XREF_IFRS -                            * 
 * Generated on:    Jan 2021                                                * 
 * Generated by:    owprdsas                                                * 
 * Version:         SAS Data Integration Studio 4.8        
 * CREATED ON DEC 2020: Segmentation Changes 
 *			Loads tables with old segments for IFRS
 ****************************************************************************/ 

/* Generate the process id for job  */ 
%put Process ID: &SYSJOBID;

/* General macro variables  */ 
%let jobID = %quote(A57SWEI7.BH0009IQ);
%let etls_jobName = %nrquote(J_RRII_TL10_2401_BASEL_PNL_PD_SEG_XREF_IFRS);
%let etls_userID = %nrquote(owprdsas);

/* Setup to capture return codes  */ 
%global job_rc trans_rc sqlrc syscc;
%let sysrc = 0;
%let job_rc = 0;
%let trans_rc = 0;
%let sqlrc = 0;
%let syscc = 0;
%global etls_stepStartTime; 
/* initialize syserr to 0 */
data _null_; run;

%macro rcSet(error); 
   %if (&error gt &trans_rc) %then 
      %let trans_rc = &error;
   %if (&error gt &job_rc) %then 
      %let job_rc = &error;
%mend rcSet; 

%macro rcSetDS(error); 
   if &error gt input(symget('trans_rc'),12.) then 
      call symput('trans_rc',trim(left(put(&error,12.))));
   if &error gt input(symget('job_rc'),12.) then 
      call symput('job_rc',trim(left(put(&error,12.))));
%mend rcSetDS; 

/* Create metadata macro variables */
%let IOMServer      = %nrquote(SASApp);
%let metaPort       = %nrquote(8561);
%let metaServer     = %nrquote(iw503);

/* Set metadata options */
options metaport       = &metaPort 
        metaserver     = "&metaServer"; 

/* Setup for capturing job status  */ 
%let etls_startTime = %sysfunc(datetime(),datetime.);
%let etls_recordsBefore = 0;
%let etls_recordsAfter = 0;
%let etls_lib = 0;
%let etls_table = 0;

%global etls_debug; 
%macro etls_setDebug; 
   %if %str(&etls_debug) ne 0 %then 
      OPTIONS MPRINT%str(;); 
%mend; 
%etls_setDebug; 

/*---- Start of Pre-Process Code  ----*/ 

%GLOBAL TIME_DIM_ID;
%GLOBAL MTH_TM_ID;
%GLOBAL MTH_END_DT;
%GLOBAL DEL_MTH_TM_ID;
%GLOBAL SESSIONTIME;
%LET SESSIONTIME=%SYSFUNC(DATETIME(),BEST18.);

/*---- End of Pre-Process Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 

/*==========================================================================* 
 * Step:            AM_rrap_spl_autoexec                  A57SWEI7.BK000YXQ * 
 * Transform:       AM_rrap_spl_autoexec                                    * 
 * Description:     Autoexec file for termloan jobs. This points to auto    * 
 *                   call macros                                            * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YXQ);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0; 
%let _OUTPUT_count = 0; 


%put WORK LOCATION: %sysfunc(getoption(work));
%rrap_spl_autoexec;


%rcSet(&syserr); 
%rcSet(&sysrc); 
%rcSet(&sqlrc); 

%rcSet(&syscc); 



/**  Step end AM_rrap_spl_autoexec **/

/*---- Start of User Written Code  ----*/ 

/*CREATE TEMPORARY TABLE TO HOLD THE RESULTS OF ALL QUERIES*/
proc datasets lib = work nolist nowarn memtype = (data view);
delete CRITERIAS_PD_DTL;
quit;

PROC SQL NOPRINT;
CREATE TABLE CRITERIAS_PD_DTL
(
BASEL_ACCT_ID NUM,
MTH_TM_ID NUM,
BASEL_SEG_ID NUM,
BASEL_MODEL_REL_ID NUM,
BASEL_MODEL_ID NUM,
INSRT_PROCESS_TMSTMP NUM INFORMAT=DATETIME. FORMAT=DATETIME.,
UPDT_PROCESS_TMSTMP NUM INFORMAT=DATETIME. FORMAT=DATETIME.
);
QUIT;
/*---- End of User Written Code  ----*/ 

/*==========================================================================* 
 * Step:            Extract run dates from control table  A57SWEI7.BK000YXS * 
 * Transform:       Extract                                                 * 
 * Description:                                                             * 
 *                                                                          * 
 * Source Table:    TM_DIM - NZRRAP.TM_DIM                A57SWEI7.BE000412 * 
 * Target Table:    Extract - work.W1G7Z098               A57SWEI7.BN000NIH * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YXS);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let etls_recnt = 0;
%macro etls_recordCheck; 
   %let etls_recCheckExist = %eval(%sysfunc(exist(NZRRAP.TM_DIM, DATA)) or 
         %sysfunc(exist(NZRRAP.TM_DIM, VIEW))); 
   
   %if (&etls_recCheckExist) %then
   %do;
      proc sql noprint;
         select count(*) into :etls_recnt from NZRRAP.TM_DIM;
      quit;
   %end;
%mend etls_recordCheck;
%etls_recordCheck;

%let SYSLAST = %nrquote(NZRRAP.TM_DIM); 

%let ETLS_SYSLAST = &SYSLAST;
/*---- Start of Pre-Process Code  ----*/ 

%get_model_period_dates(product=spl);
/*%let start_period_dt=31JUL2004;*/
/*%let end_period_dt=31AUG2015;*/
%put Start and End Dates for SPL Models:;
%put start_period_dt: &start_period_dt ;
%put end_period_dt: &end_period_dt;

/*---- End of Pre-Process Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 

%let SYSLAST = &ETLS_SYSLAST;

%global etls_sql_pushDown;
%let etls_sql_pushDown = -1;
option DBIDIRECTEXEC;

/*---- Map the columns  ----*/ 
proc datasets lib = work nolist nowarn memtype = (data view);
   delete W1G7Z098;
quit;

%put %str(NOTE: Mapping columns ...);
proc sql;
   create table work.W1G7Z098 as
      select
         TM_ID,
         TM_LVL_END_DT
   from &SYSLAST
      where TM_LVL='Month' & TM_LVL_END_DT BETWEEN "&start_period_dt"d and "&end_period_dt"d
   ;
quit;

%let SYSLAST = work.W1G7Z098;

%global etls_sql_pushDown;
%let etls_sql_pushDown = &sys_sql_ip_all;

%rcSet(&sqlrc); 

%rcSet(&syscc); 



/**  Step end Extract run dates from control table **/

/*==========================================================================* 
 * Step:            Loop                                  A57SWEI7.BK000YXT * 
 * Transform:       Loop                                                    * 
 * Description:                                                             * 
 *                                                                          * 
 * Source Table:    Extract - work.W1G7Z098               A57SWEI7.BN000NIH * 
 * Target Table:    Loop - work.WVRAFY                    A57SWEI7.BN000NII * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YXT);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let etls_recCheckExist = 0; 
%let etls_recnt = 0; 
%macro etls_recordCheck; 
   %let etls_recCheckExist = %eval(%sysfunc(exist(work.W1G7Z098, DATA)) or 
         %sysfunc(exist(work.W1G7Z098, VIEW))); 
   
   %if (&etls_recCheckExist) %then
   %do;
      %local etls_syntaxcheck; 
      %let etls_syntaxcheck = %sysfunc(getoption(syntaxcheck)); 
      /* Turn off syntaxcheck option to perform following steps  */ 
      options nosyntaxcheck;
      
      proc contents data = work.W1G7Z098 out = work.etls_contents(keep = nobs) noprint; 
      run; 
      
      data _null_; 
         set work.etls_contents (obs = 1); 
         call symput("etls_recnt", left(put(nobs,32.))); 
      run;
      
      proc datasets lib = work nolist nowarn memtype = (data view);
         delete etls_contents;
      quit;
      
      /* Reset syntaxcheck option to previous setting  */ 
      options &etls_syntaxcheck; 
   %end;
%mend etls_recordCheck;
%etls_recordCheck;

%let SYSLAST = %nrquote(work.W1G7Z098); 

/* Capture summary statistics about this step for performance reporting  */ 
%put "DIS_SUMM";
%macro etls_getHandle(statusTable=, handleVariable=, row=); 
   %let etls_dsid = %sysfunc(open(&statusTable)); 
   %if (&etls_dsid = 0) %then 
      %put %sysfunc(sysmsg()); 
   %else 
   %do; 
      %let rc = %sysfunc(fetchobs(&etls_dsid, &row)); 
      %if (&rc ne 0) %then 
         %put %sysfunc(sysmsg()); 
      %else 
      %do; 
         %let etls_varnum = %sysfunc(varnum(&etls_dsid,&handleVariable)); 
         %if (&etls_varnum > 0) %then 
            %sysfunc(getvarc(&etls_dsid,&etls_varnum)); 
         %else 
            %put %sysfunc(sysmsg()); 
      %end; 
      %let rc = %sysfunc(close(&etls_dsid)); 
   %end; 
%mend etls_getHandle; 

%macro etls_freeHandle(statusTable=, statusVariable=, handleVariable=, 
   handleName=, statusSetting="Finished", 
   endTimeVariable=endTime, startTimeVariable=startTime, signoff=1, 
   returnCodeVariable=, returnCodeMacroVariable=, setMainJobRC=1, 
   statusUnknownReturnCode=., startTimeMacroVariable=, endTimeMacroVariable= ); 

   %if (&statusTable ne ) %then 
   %do; 
      %local etls_rcMacroVarExisted; 
      %let etls_rcMacroVarExisted = 0; 
      %if ("&returnCodeMacroVariable" ne "") %then 
      %do; 
         proc sql noprint; 
            select '1' into: etls_rcMacroVarExisted from dictionary.macros 
            where name=upcase("&returnCodeMacroVariable"); 
         quit; 

         %rcSet(&sqlrc); 
         %if (&etls_rcMacroVarExisted = 0) %then 
         %do; 
            %put WARNING%QUOTE(:) Return code from inner job not found.  Setting status to Unknown.;
            %let &returnCodeMacroVariable=&statusUnknownReturnCode; 
         %end; 
         %if (&setMainJobRC eq 1) %then 
            %rcSet(&&&returnCodeMacroVariable); 
      %end; 
      %else 
      %do; 
         %let returnCodeMacroVariable=etls_rcmacvar; 
         %let &returnCodeMacroVariable=&statusUnknownReturnCode; 
      %end; 

      %local etls_startTimeMacroVarExisted; 
      %let etls_startTimeMacroVarExisted = 0; 
      %if ("&startTimeMacroVariable" ne "") %then 
      %do; 
         proc sql noprint; 
            select '1' into: etls_startTimeMacroVarExisted from dictionary.macros 
            where name=upcase("&startTimeMacroVariable"); 
         quit; 

         %rcSet(&sqlrc); 
         %if (&etls_startTimeMacroVarExisted = 0) %then 
         %do; 
            %put WARNING%QUOTE(:) Start time from inner job not found.  No value will be set.;
            %let &startTimeMacroVariable=; 
         %end; 
      %end; 

      %local etls_endTimeMacroVarExisted; 
      %let etls_endTimeMacroVarExisted = 0; 
      %if ("&endTimeMacroVariable" ne "") %then 
      %do; 
         proc sql noprint; 
            select '1' into: etls_endTimeMacroVarExisted from dictionary.macros 
            where name=upcase("&endTimeMacroVariable"); 
         quit; 

         %rcSet(&sqlrc); 
         %if (&etls_endTimeMacroVarExisted = 0) %then 
         %do; 
            %put WARNING%QUOTE(:) End time from inner job not found.  Setting end time to current
      time.;
            %let &endTimeMacroVariable=%sysfunc(datetime()); 
         %end; 
      %end; 

      data &statusTable; 
         modify &statusTable(where=(&handleVariable = &handleName)); 
         %if ("&startTimeMacroVariable" ne "") %then 
         %do; 
            &startTimeVariable = input(symget("&startTimeMacroVariable"),32.);; 
         %end; 
         %if ("&endTimeVariable" ne "") %then 
         %do; 
            %if ("&endTimeMacroVariable" ne "") %then 
            %do; 
               &endTimeVariable = input(symget("&endTimeMacroVariable"),32.);; 
            %end; 
            %else 
               &endTimeVariable = datetime();; 
         %end; 
         %if ("&returnCodeVariable" ne "") %then 
            &returnCodeVariable = input(symget("&returnCodeMacroVariable"),32.);; 
         %if ("&statusVariable" ne "") %then 
         %do; 
            if (symget("etls_rcMacroVarExisted") eq "0") then 
               &statusVariable = "Unknown Status"; 
            else
               &statusVariable = &statusSetting;
         %end;
         call symput('handle',&handleVariable); 
         replace; 
         stop; 
      run; 

      %rcSet(&syserr); 

      %if (&signoff eq 1) %then 
         %etls_signoff(handleName=&handle); 
   %end; 
%mend etls_freeHandle; 

%macro etls_createHandle(statusTable=,
                         statusVariable=,
                         handleVariable=,
                         handlePrefix=rmt,
                         gridOptionSet=,
                         workloadMacroVariable=,
                         row=,
                         machineVariable=,
                         statusSetting="Running", 
                         startTimeVariable=startTime,
                         signon=1,
                         useGrid=1,
                         log=,
                         output=,
                         gridRC=,
                         cmacvar=etls_signonStatus,
                         additionalSignonOptions=,
                         signonRetries= ); 

   %local remoteSessionId; 
   %let remoteSessionId = &handlePrefix.&row; 
   %let &cmacvar = 1; 
   %local etls_machineId; 
   %if (&signon eq 1) %then 

      %etls_signon(handleName=&remoteSessionId,
                   useGrid=&useGrid,
                   machineIdMacroVariable=etls_machineId,
                   gridOptionSet=&etls_gridOptionSet,
                   workloadMacroVariable=&workloadMacroVariable,
                   log=&log,
                   output=&output,
                   cmacvar=&cmacvar,
                   gridRC=&gridRC,
                   additionalSignonOptions=&additionalSignonOptions,
					signonRetries=&signonRetries,
                   gridJobName=DIS_&etls_jobName._&row); 

   %else %let &cmacvar=0; 

   data &statusTable; 
      retain ptr &row; 
      modify &statusTable point = ptr; 
      &handleVariable = "&remoteSessionId"; 
      %if (&signon eq 1) %then 
         &machineVariable = "&etls_machineId";; 
      %if (&&&cmacvar ne 0) %then 
      %do; 
         &statusVariable = "Failed Signon"; 
      %end; 
      %else 
         &statusVariable = &statusSetting;; 
      %if (&startTimeVariable ne ) %then 
         &startTimeVariable = datetime();; 
      replace; 
      stop; 
   run; 

   %rcSet(&syserr); 
%mend etls_createHandle; 

%macro etls_getParameterNames(parameterTable=, parameterVariableMacro=, startingColumnNumber=1);
   %let &parameterVariableMacro = ;
   %let dsid = %sysfunc(open(&parameterTable));
   %if (&dsid gt 0) %then 
   %do; 
      %do i=&startingColumnNumber %to %sysfunc(attrn(&dsid,nvars)); 
         %let &parameterVariableMacro = &&&parameterVariableMacro %sysfunc(varname(&dsid,&i)); 
      %end; 
      %let dsid = %sysfunc(close(&dsid)); 
   %end; 
   %else 
      %put %sysfunc(sysmsg()); 
   %rcSet(&syserr); 
%mend etls_getParameterNames; 

%macro etls_getParameters(parameterTable=, row=, startingColumnNumber=1 , handleName=); 
   data _null_; 
      length vname $256 vtype $1 value $32767; 
      dsid = open("&parameterTable"); 
      if (dsid > 0) then 
      do; 
         do _i = 1 to &row; 
            fetchrc = fetch(dsid); 
         end; 
         do _i=&startingColumnNumber to attrn(dsid,'nvars'); 
            vname = varname(dsid,_i); 
            vtype = vartype(dsid,_i); 
            if (fetchrc = 0) then 
            do; 
               if (vtype = 'C') then 
               do; 
                  value = getvarc(dsid,_i); 
                  value = tranwrd(value,"%","%%"); 
                  value = tranwrd(value,"(","%("); 
                  value = tranwrd(value,")","%)"); 
                  value = tranwrd(value,'"','%"'); 
                  value = tranwrd(value,"'","%'"); 
               end; 
               else 
                  value = left(put(getvarn(dsid,_i),best32.)); 
            end; /* fetchrc = 0 */ 
            
            put ;
            if indexc(trimn(value),"+-*/<>=^~;, '()&%",'"')>0 then 
            do; 
               value='%nrstr('||trim(value)||')'; 
               put "ETLS_DIAG%QUOTE(:) Special characters encountered; References may require: %nrbquote(%)UNQUOTE(&" vname+(-1)').';
               put "NOTE: Special characters encountered; References may require: %nrbquote(%)UNQUOTE(&" vname+(-1)').';
            end; 
            %if %str(&handlename) ne %str() %then 
            %do; 
               value = '%syslput '||trim(vname)||'= '||trim(value)||" / remote = &handleName;"; 
            %end; 
            %else 
            %do; 
               value = '%let '||trim(vname)||'= '||trim(value)||';'; 
            %end; 
            
            put "NOTE: Setting macro variable " vname "with statement:" value ;
            call execute(value); 
         end; /* do i= */ 
         dsid = close(dsid); 
      end; /* dsid > 0 */ 
      else 
      do; 
         put "ERROR%QUOTE(:) Parameter table, &parameterTable., could not be"
              " opened.";
         abort; 
      end; 
      stop; 
   run; 
   
   %rcSet(&syserr); 
%mend etls_getParameters; 

%macro etls_loopW2GDVQS; 
   %local etls_filePrefix; 
   %let etls_filePrefix = ; 
   
   %macro etls_processToLoopW2GDVR2(parameterTable=, row=, handleName=rmt);
      %local etls_parmvars; 
      %etls_getParameterNames(parameterTable=&parameterTable, 
         parameterVariableMacro=etls_parmvars, 
         startingColumnNumber=1); 
      %local &etls_parmvars; 
      %etls_getParameters(parameterTable=&parameterTable, row=&row, 
         startingColumnNumber=1); 
      %let etls_previousFilePrefix = &etls_filePrefix;
      %local etls_filePrefix; 
      %let etls_filePrefix = &etls_previousFilePrefix.&handleName; 
      %macro etls_jobW2GDVRC; 
      
         /*---- Start of User Written Code  ----*/ 
         
         %let MODULE=TL;
         %let BATCH=2.4;
         %etl_job_start(TARGET_TABLE=NZRRAP.BASEL_PNL_PD_SEG_XREF_IFRS);
         
         PROC SQL NOPRINT;
         DELETE FROM CRITERIAS_PD_DTL;
         QUIT;
         /*---- End of User Written Code  ----*/ 
         
         /*==========================================================================* 
          * Step:            Delete                                A57SWEI7.BK000YXV * 
          * Transform:       Delete                                                  * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Table:    Loop - work.WVRAFY                    A57SWEI7.BN000NII * 
          * Target Table:    BASEL_PNL_LN_PD_SEG_ACCT_XREF -       A57SWEI7.BE000442 * 
          *                   NZRRAP.BASEL_PNL_LN_PD_SEG_ACCT_XREF                   * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YXV);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let etls_recCheckExist = 0; 
         %let etls_recnt = 0; 
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(work.WVRAFY, DATA)) or 
                  %sysfunc(exist(work.WVRAFY, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               %local etls_syntaxcheck; 
               %let etls_syntaxcheck = %sysfunc(getoption(syntaxcheck)); 
               /* Turn off syntaxcheck option to perform following steps  */ 
               options nosyntaxcheck;
               
               proc contents data = work.WVRAFY out = work.etls_contents(keep = nobs) noprint; 
               run; 
               
               data _null_; 
                  set work.etls_contents (obs = 1); 
                  call symput("etls_recnt", left(put(nobs,32.))); 
               run;
               
               proc datasets lib = work nolist nowarn memtype = (data view);
                  delete etls_contents;
               quit;
               
               /* Reset syntaxcheck option to previous setting  */ 
               options &etls_syntaxcheck; 
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         %let SYSLAST = %nrquote(work.WVRAFY); 
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = -1;
         option DBIDIRECTEXEC;
         
         %macro etls_sql_delete;
         
            /* Determine if the table exists  */ 
            %let etls_tableExist = %eval(%sysfunc(exist(NZRRAP.BASEL_PNL_PD_SEG_XREF_IFRS, DATA)) or 
                  %sysfunc(exist(NZRRAP.BASEL_PNL_PD_SEG_XREF_IFRS, VIEW))); 
            
            /*---- Create a new table  ----*/ 
            %if (&etls_tableExist eq 0) %then 
            %do;  /* if table does not exist  */ 
            
               %put %str(NOTE: Creating table ...);
               
               data NZRRAP.BASEL_PNL_PD_SEG_XREF_IFRS
                       (dbnull = (
                                  BASEL_ACCT_ID = NO
                                  MTH_TM_ID = NO
                                  BASEL_SEG_ID = NO
                                  BASEL_MODEL_REL_ID = NO
                                  BASEL_MODEL_ID = NO
                                  INSRT_PROCESS_TMSTMP = NO
                                  UPDT_PROCESS_TMSTMP = YES));
                  attrib BASEL_ACCT_ID length = 8
                     format = 20.
                     informat = 20.
                     label = 'BASEL_ACCT_ID'; 
                  attrib MTH_TM_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'MTH_TM_ID'; 
                  attrib BASEL_SEG_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_SEG_ID'; 
                  attrib BASEL_MODEL_REL_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_REL_ID'; 
                  attrib BASEL_MODEL_ID length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_ID'; 
                  attrib INSRT_PROCESS_TMSTMP length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'INSRT_PROCESS_TMSTMP'; 
                  attrib UPDT_PROCESS_TMSTMP length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'UPDT_PROCESS_TMSTMP'; 
                  call missing(of _all_);
                  stop;
               run;
               
               %rcSet(&syserr); 
               
            %end;  /* if table does not exist  */ 
            
            %let dbxrc = %eval(%sysfunc(exist(work.WVRAFY, DATA)) or 
                  %sysfunc(exist(work.WVRAFY, VIEW))); 
            
            /*---- Delete the target table  ----*/ 
            proc sql;
               delete from NZRRAP.BASEL_PNL_PD_SEG_XREF_IFRS
               where
                  BASEL_PNL_PD_SEG_XREF_IFRS.MTH_TM_ID = &DEL_MTH_TM_ID
            ;quit;
         
         %rcSet(&syscc); 
         
         %mend etls_sql_delete;
         
         /* execute etls_sql_delete  */ 
         %etls_sql_delete
         
         %rcSet(&syscc); 
         
         
         
         /**  Step end Delete **/
         
         /*==========================================================================* 
          * Step:            Join                                  A57SWEI7.BK000YXW * 
          * Transform:       Join                                                    * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Tables:   BASEL_MODEL - NZRRAP.BASEL_MODEL      A57SWEI7.BE00041K * 
          *                  BASEL_SEG - NZRRAP.BASEL_SEG          A57SWEI7.BE00041L * 
          * Target Table:    Join - work.CRITERIAS_PD_&MTH_TM_ID   A57SWEI7.BN000NIJ * 
          *==========================================================================*/ 
         
         options VALIDVARNAME = ANY VALIDMEMNAME = EXTEND; 
         
         %let transformID = %quote(A57SWEI7.BK000YXW);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let etls_recnt = 0;
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(NZRRAP."BASEL_MODEL"n, DATA)) or 
                  %sysfunc(exist(NZRRAP."BASEL_MODEL"n, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               proc sql noprint;
                  select count(*) into :etls_recnt from NZRRAP."BASEL_MODEL"n;
               quit;
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         %let ETLS_SYSLAST = &SYSLAST;
         /*---- Start of Pre-Process Code  ----*/ 
         
         %PUT JOIN STARTING WITH MTH_TM_ID AS &MTH_TM_ID;
         /*---- End of Pre-Process Code  ----*/ 
         
         %rcSet(&syserr); 
         %rcSet(&sqlrc); 
         
         %let SYSLAST = &ETLS_SYSLAST;
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = -1;
         option DBIDIRECTEXEC;
         
         proc datasets lib = work nolist nowarn memtype = (data view);
            delete "CRITERIAS_PD_&MTH_TM_ID"n;
         quit;
         
         proc sql;
            create table work."CRITERIAS_PD_&MTH_TM_ID"n as
            select
               BASEL_SEG."BASEL_SEG_ID"n length = 8   
                  format = 11.
                  informat = 11.
                  label = 'BASEL_SEG_ID',
               BASEL_SEG."BIN_CRTRIA_SQL_CD_STRG"n length = 2000   
                  format = $2000.
                  informat = $2000.
                  label = 'BIN_CRTRIA_SQL_CD_STRG',
               &MTH_TM_ID as "MTH_TM_ID"n length = 8   
                  format = 11.
                  informat = 11.
                  label = 'MTH_TM_ID',
               MONOTONIC() as "PRIM_ID"n length = 8   
                  format = 11.
                  informat = 11.
                  label = 'PRIM_ID'
            from
               NZRRAP."BASEL_MODEL"n as BASEL_MODEL, 
               NZRRAP."BASEL_SEG_IFRS"n as BASEL_SEG
            where
               BASEL_MODEL."BASEL_MODEL_ID"n = BASEL_SEG."BASEL_MODEL_ID"n
               and UPCASE(BASEL_MODEL."PD_F"n) = 'Y'
               and TRIM(BASEL_MODEL."MODEL_VER"n) = '3'
               and UPCASE(TRIM(BASEL_MODEL."SRC_SYS_CD"n)) = 'SPL'
               and (BASEL_SEG."SEG_END_DT"n = .
               or BASEL_SEG."SEG_END_DT"n >= &MTH_END_DT)
            order by
               "BASEL_SEG_ID"n
            ;
         quit;
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = &sys_sql_ip_all;
         
         %rcSet(&sqlrc); 
         
         %rcSet(&syscc); 
         
         
         
         /**  Step end Join **/
         
         /*==========================================================================* 
          * Step:            Execute queries and insert into a     A57SWEI7.BK000YXX * 
          *                   temp table                                             * 
          * Transform:       User Written                                            * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Table:    Join -                                A57SWEI7.BN000NIJ * 
          *                   work."CRITERIAS_PD_&MTH_TM_ID"n                        * 
          * Target Table:    User Written -                        A57SWEI7.BN000NIK * 
          *                   work."CRITERIAS_PD_DTL"n                               * 
          *                                                                          * 
          * User Written:    SourceCode                                              * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YXX);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let etls_recCheckExist = 0; 
         %let etls_recnt = 0; 
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(work."CRITERIAS_PD_&MTH_TM_ID"n, DATA)) or 
                  %sysfunc(exist(work."CRITERIAS_PD_&MTH_TM_ID"n, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               %local etls_syntaxcheck; 
               %let etls_syntaxcheck = %sysfunc(getoption(syntaxcheck)); 
               /* Turn off syntaxcheck option to perform following steps  */ 
               options nosyntaxcheck;
               
               proc contents data = work."CRITERIAS_PD_&MTH_TM_ID"n out = work.etls_contents(keep = nobs) noprint; 
               run; 
               
               data _null_; 
                  set work.etls_contents (obs = 1); 
                  call symput("etls_recnt", left(put(nobs,32.))); 
               run;
               
               proc datasets lib = work nolist nowarn memtype = (data view);
                  delete etls_contents;
               quit;
               
               /* Reset syntaxcheck option to previous setting  */ 
               options &etls_syntaxcheck; 
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         %let SYSLAST = %nrquote(work."CRITERIAS_PD_&MTH_TM_ID"n); 
         
         %let _INPUT_count = 1;
         %let _INPUT = work."CRITERIAS_PD_&MTH_TM_ID"n;
         %let _INPUT_connect = ;
         %let _INPUT_engine = ;
         %let _INPUT_memtype = DATA;
         %let _INPUT_options = %nrquote();
         %let _INPUT_alter = %nrquote();
         %let _INPUT_path = %nrquote(/Join_A57SWEI7.BN000NIJ%(WorkTable%));
         %let _INPUT_type = 1;
         %let _INPUT_label = %nrquote();
         
         %let _INPUT1 = work."CRITERIAS_PD_&MTH_TM_ID"n;
         %let _INPUT1_connect = ;
         %let _INPUT1_engine = ;
         %let _INPUT1_memtype = DATA;
         %let _INPUT1_options = %nrquote();
         %let _INPUT1_alter = %nrquote();
         %let _INPUT1_path = %nrquote(/Join_A57SWEI7.BN000NIJ%(WorkTable%));
         %let _INPUT1_type = 1;
         %let _INPUT1_label = %nrquote();
         
         %let _OUTPUT_count = 1;
         %let _OUTPUT = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT_connect = ;
         %let _OUTPUT_engine = ;
         %let _OUTPUT_memtype = DATA;
         %let _OUTPUT_options = %nrquote();
         %let _OUTPUT_alter = %nrquote();
         %let _OUTPUT_path = %nrquote(/User Written_A57SWEI7.BN000NIK%(WorkTable%));
         %let _OUTPUT_type = 1;
         %let _OUTPUT_label = %nrquote();
         /* List of target columns to keep  */ 
         %let _OUTPUT_keep = "BASEL_ACCT_ID"n "MTH_TM_ID"n "BASEL_SEG_ID"n "BASEL_MODEL_REL_ID"n 
                 "BASEL_MODEL_ID"n "INSRT_PROCESS_TMSTMP"n "UPDT_PROCESS_TMSTMP"n;
         %let _OUTPUT_col_count = 7;
         %let _OUTPUT_col0_name = "BASEL_ACCT_ID"n;
         %let _OUTPUT_col0_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT_col0_length = 8;
         %let _OUTPUT_col0_type = ;
         %let _OUTPUT_col0_format = 11.;
         %let _OUTPUT_col0_informat = 11.;
         %let _OUTPUT_col0_label = %nrquote(BASEL_ACCT_ID);
         %let _OUTPUT_col0_exp = ;
         %let _OUTPUT_col0_input_count = 0;
         %let _OUTPUT_col1_name = "MTH_TM_ID"n;
         %let _OUTPUT_col1_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT_col1_length = 8;
         %let _OUTPUT_col1_type = ;
         %let _OUTPUT_col1_format = 11.;
         %let _OUTPUT_col1_informat = 11.;
         %let _OUTPUT_col1_label = %nrquote(MTH_TM_ID);
         %let _OUTPUT_col1_input0 = "MTH_TM_ID"n;
         %let _OUTPUT_col1_exp = ;
         %let _OUTPUT_col1_input = "MTH_TM_ID"n;
         %let _OUTPUT_col1_input_count = 1;
         %let _OUTPUT_col2_name = "BASEL_SEG_ID"n;
         %let _OUTPUT_col2_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT_col2_length = 8;
         %let _OUTPUT_col2_type = ;
         %let _OUTPUT_col2_format = 11.;
         %let _OUTPUT_col2_informat = 11.;
         %let _OUTPUT_col2_label = %nrquote(BASEL_SEG_ID);
         %let _OUTPUT_col2_exp = ;
         %let _OUTPUT_col2_input_count = 0;
         %let _OUTPUT_col3_name = "BASEL_MODEL_REL_ID"n;
         %let _OUTPUT_col3_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT_col3_length = 8;
         %let _OUTPUT_col3_type = ;
         %let _OUTPUT_col3_format = 11.;
         %let _OUTPUT_col3_informat = 11.;
         %let _OUTPUT_col3_label = %nrquote(BASEL_MODEL_REL_ID);
         %let _OUTPUT_col3_exp = ;
         %let _OUTPUT_col3_input_count = 0;
         %let _OUTPUT_col4_name = "BASEL_MODEL_ID"n;
         %let _OUTPUT_col4_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT_col4_length = 8;
         %let _OUTPUT_col4_type = ;
         %let _OUTPUT_col4_format = 11.;
         %let _OUTPUT_col4_informat = 11.;
         %let _OUTPUT_col4_label = %nrquote(BASEL_MODEL_ID);
         %let _OUTPUT_col4_exp = ;
         %let _OUTPUT_col4_input_count = 0;
         %let _OUTPUT_col5_name = "INSRT_PROCESS_TMSTMP"n;
         %let _OUTPUT_col5_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT_col5_length = 8;
         %let _OUTPUT_col5_type = ;
         %let _OUTPUT_col5_format = DATETIME25.6;
         %let _OUTPUT_col5_informat = DATETIME25.6;
         %let _OUTPUT_col5_label = %nrquote(INSRT_PROCESS_TMSTMP);
         %let _OUTPUT_col5_exp = ;
         %let _OUTPUT_col5_input_count = 0;
         %let _OUTPUT_col6_name = "UPDT_PROCESS_TMSTMP"n;
         %let _OUTPUT_col6_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT_col6_length = 8;
         %let _OUTPUT_col6_type = ;
         %let _OUTPUT_col6_format = DATETIME25.6;
         %let _OUTPUT_col6_informat = DATETIME25.6;
         %let _OUTPUT_col6_label = %nrquote(UPDT_PROCESS_TMSTMP);
         %let _OUTPUT_col6_exp = ;
         %let _OUTPUT_col6_input_count = 0;
         
         %let _OUTPUT1 = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT1_connect = ;
         %let _OUTPUT1_engine = ;
         %let _OUTPUT1_memtype = DATA;
         %let _OUTPUT1_options = %nrquote();
         %let _OUTPUT1_alter = %nrquote();
         %let _OUTPUT1_path = %nrquote(/User Written_A57SWEI7.BN000NIK%(WorkTable%));
         %let _OUTPUT1_type = 1;
         %let _OUTPUT1_label = %nrquote();
         /* List of target columns to keep  */ 
         %let _OUTPUT1_keep = "BASEL_ACCT_ID"n "MTH_TM_ID"n "BASEL_SEG_ID"n "BASEL_MODEL_REL_ID"n 
                 "BASEL_MODEL_ID"n "INSRT_PROCESS_TMSTMP"n "UPDT_PROCESS_TMSTMP"n;
         %let _OUTPUT1_col_count = 7;
         %let _OUTPUT1_col0_name = "BASEL_ACCT_ID"n;
         %let _OUTPUT1_col0_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT1_col0_length = 8;
         %let _OUTPUT1_col0_type = ;
         %let _OUTPUT1_col0_format = 11.;
         %let _OUTPUT1_col0_informat = 11.;
         %let _OUTPUT1_col0_label = %nrquote(BASEL_ACCT_ID);
         %let _OUTPUT1_col0_exp = ;
         %let _OUTPUT1_col0_input_count = 0;
         %let _OUTPUT1_col1_name = "MTH_TM_ID"n;
         %let _OUTPUT1_col1_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT1_col1_length = 8;
         %let _OUTPUT1_col1_type = ;
         %let _OUTPUT1_col1_format = 11.;
         %let _OUTPUT1_col1_informat = 11.;
         %let _OUTPUT1_col1_label = %nrquote(MTH_TM_ID);
         %let _OUTPUT1_col1_input0 = "MTH_TM_ID"n;
         %let _OUTPUT1_col1_exp = ;
         %let _OUTPUT1_col1_input = "MTH_TM_ID"n;
         %let _OUTPUT1_col1_input_count = 1;
         %let _OUTPUT1_col2_name = "BASEL_SEG_ID"n;
         %let _OUTPUT1_col2_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT1_col2_length = 8;
         %let _OUTPUT1_col2_type = ;
         %let _OUTPUT1_col2_format = 11.;
         %let _OUTPUT1_col2_informat = 11.;
         %let _OUTPUT1_col2_label = %nrquote(BASEL_SEG_ID);
         %let _OUTPUT1_col2_exp = ;
         %let _OUTPUT1_col2_input_count = 0;
         %let _OUTPUT1_col3_name = "BASEL_MODEL_REL_ID"n;
         %let _OUTPUT1_col3_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT1_col3_length = 8;
         %let _OUTPUT1_col3_type = ;
         %let _OUTPUT1_col3_format = 11.;
         %let _OUTPUT1_col3_informat = 11.;
         %let _OUTPUT1_col3_label = %nrquote(BASEL_MODEL_REL_ID);
         %let _OUTPUT1_col3_exp = ;
         %let _OUTPUT1_col3_input_count = 0;
         %let _OUTPUT1_col4_name = "BASEL_MODEL_ID"n;
         %let _OUTPUT1_col4_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT1_col4_length = 8;
         %let _OUTPUT1_col4_type = ;
         %let _OUTPUT1_col4_format = 11.;
         %let _OUTPUT1_col4_informat = 11.;
         %let _OUTPUT1_col4_label = %nrquote(BASEL_MODEL_ID);
         %let _OUTPUT1_col4_exp = ;
         %let _OUTPUT1_col4_input_count = 0;
         %let _OUTPUT1_col5_name = "INSRT_PROCESS_TMSTMP"n;
         %let _OUTPUT1_col5_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT1_col5_length = 8;
         %let _OUTPUT1_col5_type = ;
         %let _OUTPUT1_col5_format = DATETIME25.6;
         %let _OUTPUT1_col5_informat = DATETIME25.6;
         %let _OUTPUT1_col5_label = %nrquote(INSRT_PROCESS_TMSTMP);
         %let _OUTPUT1_col5_exp = ;
         %let _OUTPUT1_col5_input_count = 0;
         %let _OUTPUT1_col6_name = "UPDT_PROCESS_TMSTMP"n;
         %let _OUTPUT1_col6_table = work."CRITERIAS_PD_DTL"n;
         %let _OUTPUT1_col6_length = 8;
         %let _OUTPUT1_col6_type = ;
         %let _OUTPUT1_col6_format = DATETIME25.6;
         %let _OUTPUT1_col6_informat = DATETIME25.6;
         %let _OUTPUT1_col6_label = %nrquote(UPDT_PROCESS_TMSTMP);
         %let _OUTPUT1_col6_exp = ;
         %let _OUTPUT1_col6_input_count = 0;
         
         /*---- Start of User Written Code  ----*/ 
         
         
         %MACRO INSERT_DATA;
         
         PROC SQL Noprint;
         	select BIN_CRTRIA_SQL_CD_STRG, BASEL_SEG_ID, MTH_TM_ID 
         	into :sql_str1 - :sql_str&SysMaxLong, :seg_id1 - :seg_id&SysMaxLong, :mth_id1 - :mth_id&SysMaxLong
         	FROM CRITERIAS_PD_&MTH_TM_ID;
         	%let total_query = &sqlObs;
         quit;
         
         %PUT TOTAL QUERIES ARE &TOTAL_QUERY;
         
         %DO i=1 %TO &total_query;
         
         	%let BASEL_SEG_ID = &&seg_id&i;
         	%let TM_DIM_ID = &&mth_id&i;
         	%let DT = &SESSIONTIME;
         
         	Proc sql;
         		CONNECT USING NZRRAP AS NETEZCON;
         			insert into CRITERIAS_PD_DTL (
         				 BASEL_ACCT_ID
         		       , MTH_TM_ID
         		       , BASEL_SEG_ID
         			   , BASEL_MODEL_REL_ID
         		       , BASEL_MODEL_ID
         		       , INSRT_PROCESS_TMSTMP
         			   , UPDT_PROCESS_TMSTMP
         			   )
         			   SELECT 
         				 BASEL_ACCT_ID
         		       , MTH_TM_ID
         		       , BASEL_SEG_ID
         			   , BASEL_MODEL_REL_ID
         		       , BASEL_MODEL_ID
         		       , &DT AS INSRT_PROCESS_TMSTMP
         			   , &DT AS UPDT_PROCESS_TMSTMP
         			FROM CONNECTION TO NETEZCON (&&sql_str&i);
         		quit;
         
         %END;
         %MEND;
         %INSERT_DATA;
         /*---- End of User Written Code  ----*/ 
         
         %rcSet(&syserr); 
         %rcSet(&sqlrc); 
         %rcSet(&syscc); 
         
         
         
         /**  Step end Execute queries and insert into a temp table **/
         
         /*==========================================================================* 
          * Step:            Table Loader                          A57SWEI7.BK000YXY * 
          * Transform:       Table Loader (Version 2.1)                              * 
          * Description:                                                             * 
          *                                                                          * 
          * Source Table:    User Written -                        A57SWEI7.BN000NIK * 
          *                   work."CRITERIAS_PD_DTL"n                               * 
          * Target Table:    BASEL_PNL_LN_PD_SEG_ACCT_XREF -       A57SWEI7.BE000442 * 
          *                  NZRRAP."BASEL_PNL_LN_PD_SEG_ACCT_XR                     * 
          *                  EF"n                                                    * 
          *==========================================================================*/ 
         
         %let transformID = %quote(A57SWEI7.BK000YXY);
         %let trans_rc = 0;
         %let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 
         
         %let SYSLAST = %nrquote(work."CRITERIAS_PD_DTL"n); 
         
         %global etls_sql_pushDown;
         %let etls_sql_pushDown = -1;
         option DBIDIRECTEXEC;
         
         %global etls_tableExist;
         %global etls_numIndex;
         %global etls_lastTable;
         %let etls_tableExist = -1; 
         %let etls_numIndex = -1; 
         %let etls_lastTable = &SYSLAST; 
         
         /*---- Define load data macro  ----*/ 
         
         /* --------------------------------------------------------------
            Load Technique Selection: AppendToExisting - AppendProc
            Constraint and index action selections: 'ASIS','ASIS','ASIS','ASIS'
            Additional options selections... 
               Set unmapped to missing on updates: false 
            -------------------------------------------------------------- */
         %macro etls_loader;
         
            %let etls_tableOptions = ;
            
            /*---- Map the columns  ----*/ 
            proc datasets lib = work nolist nowarn memtype = (data view);
               delete W2GDVSB;
            quit;
            
            %put %str(NOTE: Mapping columns ...);
            proc sql;
               create view work.W2GDVSB as
                  select
                     "BASEL_ACCT_ID"n   
                        format = 20.
                        informat = 20.,
                     "MTH_TM_ID"n,
                     "BASEL_SEG_ID"n,
                     "BASEL_MODEL_REL_ID"n,
                     "BASEL_MODEL_ID"n,
                     (%SYSFUNC(DATETIME(),BEST18.)) as "INSRT_PROCESS_TMSTMP"n length = 8
                        format = DATETIME25.6
                        informat = DATETIME25.6,
                     (%SYSFUNC(DATETIME(),BEST18.)) as "UPDT_PROCESS_TMSTMP"n length = 8
                        format = DATETIME25.6
                        informat = DATETIME25.6
               from &etls_lastTable
               ;
            quit;
            
            %let SYSLAST = work.W2GDVSB;
            
            %let etls_lastTable = &SYSLAST; 
            %let etls_tableOptions = ; 
            
            /* Determine if the target table exists  */ 
            %let etls_tableExist = %eval(%sysfunc(exist(NZRRAP."BASEL_PNL_PD_SEG_XREF_IFRS"n, DATA)) or 
                  %sysfunc(exist(NZRRAP."BASEL_PNL_PD_SEG_XREF_IFRS"n, VIEW))); 
            
            /*---- Create a new table  ----*/ 
            %if (&etls_tableExist eq 0) %then 
            %do;  /* if table does not exist  */ 
            
               %put %str(NOTE: Creating table ...);
               
               data NZRRAP."BASEL_PNL_PD_SEG_XREF_IFRS"n
                       (dbnull = (
                                  "BASEL_ACCT_ID"n = NO
                                  "MTH_TM_ID"n = NO
                                  "BASEL_SEG_ID"n = NO
                                  "BASEL_MODEL_REL_ID"n = NO
                                  "BASEL_MODEL_ID"n = NO
                                  "INSRT_PROCESS_TMSTMP"n = NO
                                  "UPDT_PROCESS_TMSTMP"n = YES));
                  attrib "BASEL_ACCT_ID"n length = 8
                     format = 20.
                     informat = 20.
                     label = 'BASEL_ACCT_ID'; 
                  attrib "MTH_TM_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'MTH_TM_ID'; 
                  attrib "BASEL_SEG_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_SEG_ID'; 
                  attrib "BASEL_MODEL_REL_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_REL_ID'; 
                  attrib "BASEL_MODEL_ID"n length = 8
                     format = 11.
                     informat = 11.
                     label = 'BASEL_MODEL_ID'; 
                  attrib "INSRT_PROCESS_TMSTMP"n length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'INSRT_PROCESS_TMSTMP'; 
                  attrib "UPDT_PROCESS_TMSTMP"n length = 8
                     format = DATETIME25.6
                     informat = DATETIME25.6
                     label = 'UPDT_PROCESS_TMSTMP'; 
                  call missing(of _all_);
                  stop;
               run;
               
               %rcSet(&syserr); 
               
            %end;  /* if table does not exist  */ 
            
            /*---- Append  ----*/ 
            %put %str(NOTE: Appending data ...);
            
            proc append base = NZRRAP."BASEL_PNL_PD_SEG_XREF_IFRS"n (BULKLOAD=YES BL_METHOD=CLILOAD)
               data = &etls_lastTable (&etls_tableOptions)  force ; 
             run; 
            
            %rcSet(&syserr); 
            
            proc datasets lib = work nolist nowarn memtype = (data view);
               delete W2GDVSB;
            quit;
            
         %mend etls_loader;
         %etls_loader;
         
         %let etls_recnt = 0;
         %macro etls_recordCheck; 
            %let etls_recCheckExist = %eval(%sysfunc(exist(NZRRAP."BASEL_PNL_PD_SEG_XREF_IFRS"n, DATA)) or 
                  %sysfunc(exist(NZRRAP."BASEL_PNL_PD_SEG_XREF_IFRS"n, VIEW))); 
            
            %if (&etls_recCheckExist) %then
            %do;
               proc sql noprint;
                  select count(*) into :etls_recnt from NZRRAP."BASEL_PNL_PD_SEG_XREF_IFRS"n;
               quit;
            %end;
         %mend etls_recordCheck;
         %etls_recordCheck;
         
         /*---- Start of Post-Process Code  ----*/ 
         
         %etl_job_end;
         /*---- End of Post-Process Code  ----*/ 
         
         %rcSet(&syserr); 
         %rcSet(&sqlrc); 
         
         %rcSet(&syscc); 
         
         
         

   /*---- Start of User Written Code  ----*/ 
         
         %let MODULE=TL;
         %let BATCH=2.4;
         %etl_job_start(TARGET_TABLE=NZRRAP.BASEL_PSNL_LN_INST_FACT_IFRS);
 
		%rcSet(&syserr); 
         %rcSet(&sqlrc); 

		 %MACRO LOAD_DATA_IIAS;

		 PROC SQL NOPRINT;
         	connect using NZRRAP as nzcon;
         	EXECUTE( DELETE FROM &RRAP_DB..BASEL_PSNL_LN_INST_FACT_IFRS WHERE MTH_TM_ID = &MTH_TM_ID.) BY NZCON;       
         QUIT;

		 PROC SQL;
		 	INSERT INTO NZRRAP.BASEL_PSNL_LN_INST_FACT_IFRS (BULKLOAD=YES BL_METHOD=CLILOAD)
			SELECT * FROM NZRRAP.BASEL_PSNL_LN_ANL_BL_INST_FACT
			WHERE MTH_TM_ID = &MTH_TM_ID.
			;
		 QUIT;

		 %MEND;
		 %LOAD_DATA_IIAS;

		PROC SQL;
   		connect using NZRRAP as nzcon;
    	EXECUTE( 
			UPDATE &RRAP_DB..BASEL_PSNL_LN_INST_FACT_IFRS A 
				SET pd_basel_seg_num		= D.seg_num
				,pd_basel_seg_id		= D.basel_seg_id
				,pd_basel_model_rel_id	= D.basel_model_rel_id

			FROM
				(SELECT B.*, C.SEG_NUM 
					FROM &RRAP_DB..BASEL_PNL_PD_SEG_XREF_IFRS_V B 
					JOIN &RRAP_DB..BASEL_SEG_IFRS_V C 
					ON B.BASEL_SEG_ID = C.BASEL_SEG_ID
					WHERE B.MTH_TM_ID = &MTH_TM_ID.) D
			WHERE A.BASEL_ACCT_ID = D.BASEL_ACCT_ID 
			AND A.MTH_TM_ID=D.MTH_TM_ID
			AND A.MTH_TM_ID=&MTH_TM_ID.
		) BY NZCON;       
        QUIT;

		/*---- End of User Written Code  ----*/ 
         
         %rcSet(&syserr); 
         %rcSet(&sqlrc); 
         /*---- Start of Post-Process Code  ----*/ 
         
         %etl_job_end;

         /**  Step end Table Loader **/
         
      %mend etls_jobW2GDVRC; 
      
      %etls_jobW2GDVRC; 
      
      %let handleName = %etls_getHandle(statusTable=&etls_statusTable,
         handleVariable=etls_handleName, row=&&&etls_controlName);
         
      %let job_rc&handleName. = &job_rc; 
      
      %etls_freeHandle(statusTable=&etls_statusTable, statusVariable=etls_status, 
         handleVariable=etls_handleName, handleName="&handleName", 
         startTimeVariable=etls_startTime, endTimeVariable=etls_endTime, signoff=0, 
         returnCodeVariable=etls_jobRC, returnCodeMacroVariable=job_rc&handleName., setMainJobRc=1); 
         
   %mend etls_processToLoopW2GDVR2;
   
   %local etls_controlTable etls_statusTable etls_controlName 
      etls_processesRunning etls_maxProcesses etls_parameterTable 
      etls_additionalSignonOptions etls_signonRetries;
   %let etls_controlName = L22_; 
   %let etls_statusTable = work."WVRAFY"n; 
   %let etls_parameterTable = work.W2GDVSL; 
   %let &etls_controlName = 0;
   %let etls_controlTable = work."W1G7Z098"n;
   
   %put %str(NOTE: Creating status table...);
   data &etls_statusTable 
      (keep = "etls_handleName"n "etls_machineId"n "etls_startTime"n "etls_endTime"n 
              "etls_status"n "etls_jobRC"n "TM_ID"n "TM_LVL_END_DT"n
      );
      attrib "etls_handleName"n length = $32
         label = 'Name of handle to remote session'; 
      attrib "etls_machineId"n length = $32
         label = 'Name of machine executing the task'; 
      attrib "etls_startTime"n length = 8
         format = nldatmap.
         label = 'Start time of task'; 
      attrib "etls_endTime"n length = 8
         format = nldatmap.
         label = 'End time of task'; 
      attrib "etls_status"n length = $32
         label = 'Current status of task'; 
      attrib "etls_jobRC"n length = 8
         label = 'Return code of task'; 
      attrib "TM_ID"n length = 8
         format = 11.
         informat = 11.
         label = 'TM_ID'; 
      attrib "TM_LVL_END_DT"n length = 8
         format = DATE9.
         informat = DATE9.
         label = 'TM_LVL_END_DT'; 
      set &etls_controlTable;
   run;
   
   %rcSet(&syserr); 
   
   %put %str(NOTE: Creating parameter table...);
   proc sql; 
      create table &etls_parameterTable as 
         select TM_ID as DEL_MTH_TM_ID, 
                TM_ID as MTH_TM_ID, 
                TM_LVL_END_DT as MTH_END_DT 
         from &etls_controlTable; 
   quit; 
   
   %rcSet(&sqlrc); 
   
   /* Get the number of times to iterate from the number of rows in the source  */ 
   /*  table                                                                    */ 
   proc sql noprint; 
      select count(*) into :&etls_controlName._max from &etls_statusTable;
      %let &etls_controlName._max = &&&etls_controlName._max;
   quit;
   
   %rcSet(&sqlrc); 
   
   %let etls_maxProcesses = 1; 
   
   %if (&etls_maxProcesses > 0) %then 
   %do; 
      %do %until (&&&etls_controlName ge &&&etls_controlName._max); 
      
         %let etls_lastLoopPtr = &&&etls_controlName;
         
         %let etls_processesRunning = 0; 
         
         %do %while(&etls_processesRunning lt &&&etls_controlName._max 
            and &etls_processesRunning lt &etls_maxProcesses 
            and &&&etls_controlName lt &&&etls_controlName._max);
            
            %let &etls_controlName = %eval(&&&etls_controlName+1);
            
            %let job_rcLast=&job_rc; 
            
            %etls_createHandle(statusTable=&etls_statusTable,
                               statusVariable=etls_status,
                               handleVariable=etls_handleName,
                               handlePrefix=&etls_controlName,
                               statusSetting="Running",
                               row=&&&etls_controlName,
                               machineVariable=etls_machineId,
                               startTimeVariable=etls_startTime,
                               signon=0,
                               useGrid=0);
            
         %let etls_processesRunning = 1; 
         %etls_processToLoopW2GDVR2(parameterTable=&etls_parameterTable, row=&&&etls_controlName,
            handleName=%etls_getHandle(statusTable=&etls_statusTable,
            handleVariable=etls_handleName, row=&&&etls_controlName)); 
         
         /* Reset main Job_RC and Trans_RC to max return code of all iterations */ 
         %let job_rcThisIter=&job_rc; 
         %let job_rc=&job_rcLast; 
         %let trans_rc=&job_rcLast; 
         %rcSet(&job_rcThisIter); 
      %end; 
      
   %end; 
   
%end;

proc datasets lib = work nolist nowarn memtype = (data view);
   delete W2GDVSL;
quit;

%mend etls_loopW2GDVQS; 

%etls_loopW2GDVQS; 

%rcSet(&syscc); 



/**  Step end Loop **/

/*==========================================================================* 
 * Step:            Loop End                              A57SWEI7.BK000YXZ * 
 * Transform:       Loop End                                                * 
 * Description:                                                             * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000YXZ);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%rcSet(&syscc); 



/**  Step end Loop End **/

%let etls_endTime = %sysfunc(datetime(),datetime.);

