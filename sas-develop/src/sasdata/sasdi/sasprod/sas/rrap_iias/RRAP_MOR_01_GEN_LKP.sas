/**************************************************************************** 
 * Job:             RRAP_MOR_01_GEN_LKP                   A57SWEI7.BH0009JS * 
 * Description:     GENL_LKP FROM DB2 IS COPIED TO NZFRG                    * 
 *                                                                          * 
 * Metadata Server: 10.56.70.48                                             * 
 * Port:            8561                                                    * 
 * Location:        /Projects/RRAP/Jobs/04 - Mortgage/BNS                   * 
 *                                                                          * 
 * Server:          SASApp                                A57SWEI7.AT000002 * 
 *                                                                          * 
 * Generated on:    Friday, August 4, 2017 1:23:46 PM EDT                   * 
 * Generated by:    owprdsas                                                * 
 * Version:         SAS Data Integration Studio 4.8                         * 
 ****************************************************************************/ 

/* Generate the process id for job  */ 
%put Process ID: &SYSJOBID;

/* General macro variables  */ 
%let jobID = %quote(A57SWEI7.BH0009JS);
%let etls_jobName = %nrquote(RRAP_MOR_01_GEN_LKP);
%let etls_userID = %nrquote(owprdsas);

/* Setup to capture return codes  */ 
%global job_rc trans_rc sqlrc syscc;
%let sysrc = 0;
%let job_rc = 0;
%let trans_rc = 0;
%let sqlrc = 0;
%let syscc = 0;
%global etls_stepStartTime; 
/* initialize syserr to 0 */
data _null_; run;

%macro rcSet(error); 
   %if (&error gt &trans_rc) %then 
      %let trans_rc = &error;
   %if (&error gt &job_rc) %then 
      %let job_rc = &error;
%mend rcSet; 

%macro rcSetDS(error); 
   if &error gt input(symget('trans_rc'),12.) then 
      call symput('trans_rc',trim(left(put(&error,12.))));
   if &error gt input(symget('job_rc'),12.) then 
      call symput('job_rc',trim(left(put(&error,12.))));
%mend rcSetDS; 

/* Create metadata macro variables */
%let IOMServer      = %nrquote(SASApp);
%let metaPort       = %nrquote(8561);
%let metaServer     = %nrquote(iw503);

/* Set metadata options */
options metaport       = &metaPort 
        metaserver     = "&metaServer"; 

/* Setup for capturing job status  */ 
%let etls_startTime = %sysfunc(datetime(),datetime.);
%let etls_recordsBefore = 0;
%let etls_recordsAfter = 0;
%let etls_lib = 0;
%let etls_table = 0;

%global etls_debug; 
%macro etls_setDebug; 
   %if %str(&etls_debug) ne 0 %then 
      OPTIONS MPRINT%str(;); 
%mend; 
%etls_setDebug; 

/*==========================================================================* 
 * Step:            AM_rrap_mor_bns_autoexec              A57SWEI7.BK000Z4O * 
 * Transform:       AM_rrap_mor_bns_autoexec                                * 
 * Description:     BNS Mortgage jobs imported from IW503 leverage this     * 
 *                   transformation                                         * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000Z4O);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0; 
%let _OUTPUT_count = 0; 


%put WORK LOCATION: %sysfunc(getoption(work));
%rrap_mor_bns_autoexec;

%rcSet(&syserr); 
%rcSet(&sysrc); 
%rcSet(&sqlrc); 

%rcSet(&syscc); 



/**  Step end AM_rrap_mor_bns_autoexec **/

/*==========================================================================* 
 * Step:            User Written                          A57SWEI7.BK000Z4P * 
 * Transform:       User Written                                            * 
 * Description:                                                             * 
 *                                                                          * 
 * Target Table:    User Written - work.W1M7Z7L4          A57SWEI7.BN000NQD * 
 *                                                                          * 
 * User Written:    SourceCode                                              * 
 *==========================================================================*/ 

%let transformID = %quote(A57SWEI7.BK000Z4P);
%let trans_rc = 0;
%let etls_stepStartTime = %sysfunc(datetime(), datetime20.); 

%let _INPUT_count = 0;
%let _OUTPUT_count = 1;
%let _OUTPUT = work.W1M7Z7L4;
%let _OUTPUT_connect = ;
%let _OUTPUT_engine = ;
%let _OUTPUT_memtype = DATA;
%let _OUTPUT_options = %nrquote();
%let _OUTPUT_alter = %nrquote();
%let _OUTPUT_path = %nrquote(/User Written_A57SWEI7.BN000NQD%(WorkTable%));
%let _OUTPUT_type = 1;
%let _OUTPUT_label = %nrquote();
%let _OUTPUT_col_count = 0;

%let _OUTPUT1 = work.W1M7Z7L4;
%let _OUTPUT1_connect = ;
%let _OUTPUT1_engine = ;
%let _OUTPUT1_memtype = DATA;
%let _OUTPUT1_options = %nrquote();
%let _OUTPUT1_alter = %nrquote();
%let _OUTPUT1_path = %nrquote(/User Written_A57SWEI7.BN000NQD%(WorkTable%));
%let _OUTPUT1_type = 1;
%let _OUTPUT1_label = %nrquote();
%let _OUTPUT1_col_count = 0;

/*---- Start of User Written Code  ----*/ 

%let gen_table=EDRTLR.GENL_LKP; 

/******************************************************************************
* Step 2 - Convert input table to a structure that matches what is in Netezza 
*******************************************************************************/
data work.GEN_LKP;
set &gen_table(rename=(GENL_LKP_ID=GENL_LKP_ID_orig EFF_TO_TMSTMP=EFF_TO_TMSTMP_orig  
                        POPN_DATETIME=POPN_DATETIME_orig INTG_LAYER_CLASS_ID=INTG_LAYER_CLASS_ID_orig ));
drop GENL_LKP_ID_orig EFF_TO_TMSTMP_orig POPN_DATETIME_orig INTG_LAYER_CLASS_ID_orig;
GENL_LKP_ID=left(trim(put(GENL_LKP_ID_orig, $6.)));
EFF_TO_TMSTMP=left(trim(put(EFF_TO_TMSTMP_orig, datetime.)));
POPN_DATETIME=left(trim(put(POPN_DATETIME_orig, time.)));
INTG_LAYER_CLASS_ID=left(trim(put(INTG_LAYER_CLASS_ID_orig, $25.)));
run;


/*proc sql NOPRINT;*/
/*delete from NZUSER.GEN_LKP;*/
/*quit;*/

proc sql NOPRINT;
connect using nzuser as nzcon;
execute(delete from &target_lib3..GEN_LKP) by nzcon;
quit;

proc sql noprint;
INSERT INTO NZUSER.GEN_LKP 
(GENL_LKP_ID,
GENL_LKP_CD,
GENL_LKP_DESC,
TBL_NM,
EFF_FROM_TMSTMP,
EFF_TO_TMSTMP,
POPN_DATETIME,
GENL_LKP_TP_NM,
INTG_LAYER_CLASS_ID,
INSRT_PROCESS_TMSTMP,
UPDT_PROCESS_TMSTMP)
SELECT
GENL_LKP_ID,
GENL_LKP_CD,
GENL_LKP_DESC,
TBL_NM,
EFF_FROM_TMSTMP,
EFF_TO_TMSTMP,
POPN_DATETIME,
GENL_LKP_TP_NM,
INTG_LAYER_CLASS_ID,
DATETIME() AS INSRT_PROCESS_TMSTMP,
DATETIME() AS UPDT_PROCESS_TMSTMP 
FROM WORK.GEN_LKP;
QUIT;

/*proc append base=nzuser.GEN_LKP data=work.GEN_LKP force ;*/
/*run;*/
/*---- End of User Written Code  ----*/ 

%rcSet(&syserr); 
%rcSet(&sqlrc); 
%rcSet(&syscc); 



/**  Step end User Written **/

%let etls_endTime = %sysfunc(datetime(),datetime.);

